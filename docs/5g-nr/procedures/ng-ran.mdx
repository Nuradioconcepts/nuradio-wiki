---
title: NG-RAN Procedures
description: 5G NR procedures within the NG-RAN — F1, E1, and Xn interface flows between gNB components.
---

import InterfaceTable from '@site/src/components/InterfaceTable';

# NG-RAN Procedures

<InterfaceTable interfaces={[
  {
    id: 'F1-C',
    fullName: 'F1 Control Plane',
    icon: '🔗',
    plane: 'control',
    protocol: 'F1AP',
    node1: 'gNB-CU-CP',
    node1Variant: 'ran',
    node2: 'gNB-DU',
    node2Variant: 'ran',
    unit: 'Procedure',
    procedures: [
      {
        name: 'F1 Setup',
        domain: 'SETUP',
        dependencies: '[[F1AP]] transport established (SCTP)',
        direction: 'gNB-DU → gNB-CU-CP',
        messages: 'F1 Setup Request / F1 Setup Response',
        trigger: '[[gNB-DU]] initialization or restart',
        success: '[[gNB-CU-CP]] accepts DU with supported cells and capabilities',
        failure: 'F1 Setup Failure — unsupported configuration or congestion',
      },
      {
        name: 'gNB-DU Configuration Update',
        domain: 'CONFIG',
        dependencies: 'F1 Setup complete',
        direction: 'gNB-DU → gNB-CU-CP',
        messages: 'gNB-DU Configuration Update / Acknowledge',
        trigger: 'Cell activation, deactivation, or parameter change at [[gNB-DU]]',
        success: '[[gNB-CU-CP]] updates its cell database',
        failure: 'gNB-DU Configuration Update Failure',
      },
      {
        name: 'gNB-CU Configuration Update',
        domain: 'CONFIG',
        dependencies: 'F1 Setup complete',
        direction: 'gNB-CU-CP → gNB-DU',
        messages: 'gNB-CU Configuration Update / Acknowledge',
        trigger: 'Operator-driven config push or slice configuration change',
        success: '[[gNB-DU]] applies new parameters',
        failure: 'gNB-CU Configuration Update Failure',
      },
      {
        name: 'UE Context Setup',
        domain: 'UE MANAGEMENT',
        dependencies: 'F1 Setup complete; UE admitted at [[gNB-CU-CP]]',
        direction: 'gNB-CU-CP → gNB-DU',
        messages: 'UE Context Setup Request / Response',
        trigger: '[[RRC]] connection establishment or incoming handover',
        success: '[[gNB-DU]] allocates radio resources; C-RNTI assigned',
        failure: 'UE Context Setup Failure — resource unavailable',
      },
      {
        name: 'UE Context Modification',
        domain: 'UE MANAGEMENT',
        dependencies: 'UE context exists at [[gNB-DU]]',
        direction: 'gNB-CU-CP → gNB-DU',
        messages: 'UE Context Modification Request / Response',
        trigger: '[[DRB]] add/modify/release or [[QoS]] change',
        success: '[[gNB-DU]] applies updated bearer and RLC config',
        failure: 'UE Context Modification Failure',
      },
      {
        name: 'UE Context Release',
        domain: 'UE MANAGEMENT',
        dependencies: 'UE context exists at [[gNB-DU]]',
        direction: 'gNB-CU-CP → gNB-DU (or gNB-DU → gNB-CU-CP)',
        messages: 'UE Context Release Command / Complete (or UE Context Release Request)',
        trigger: '[[RRC]] release, handover completion, or radio failure',
        success: '[[gNB-DU]] releases all UE radio resources',
        failure: '—',
      },
      {
        name: 'Paging',
        domain: 'IDLE MODE',
        dependencies: 'Cell served by [[gNB-DU]] is active',
        direction: 'gNB-CU-CP → gNB-DU',
        messages: 'Paging message carried over F1AP',
        trigger: '[[AMF]] pages the [[UE]] via [[gNB]]',
        success: '[[gNB-DU]] transmits paging on [[PDSCH]] in paging occasions',
        failure: 'UE does not respond — paging retried by [[AMF]]',
      },
      {
        name: 'UE Context Suspend / Resume',
        domain: 'INACTIVE MODE',
        dependencies: 'UE context exists; [[RRC_INACTIVE]] configured',
        direction: 'gNB-CU-CP ↔ gNB-DU',
        messages: 'UE Context Suspend Request / Response; UE Context Resume Request / Response',
        trigger: '[[RRC]] suspension or RNA update',
        success: 'Context stored at [[gNB-CU-CP]]; [[gNB-DU]] releases radio resources',
        failure: 'Resume Failure — context expired or security mismatch',
      },
    ],
  },
  {
    id: 'F1-U',
    fullName: 'F1 User Plane',
    icon: '📦',
    plane: 'user',
    protocol: 'GTP-U',
    node1: 'gNB-CU-UP',
    node1Variant: 'ran',
    node2: 'gNB-DU',
    node2Variant: 'ran',
    unit: 'Tunnel',
    procedures: [
      {
        name: 'DL User Data Transfer',
        domain: 'DATA PLANE',
        dependencies: '[[GTP-U]] tunnel established via [[E1AP]] Bearer Context Setup',
        direction: 'gNB-CU-UP → gNB-DU',
        messages: '[[GTP-U]] packets carrying [[PDCP]] PDUs',
        trigger: 'Downlink data arrives at [[gNB-CU-UP]] from [[UPF]]',
        success: '[[gNB-DU]] delivers PDCP PDUs to UE over [[PDSCH]]',
        failure: 'Tunnel failure — [[GTP-U]] error indication sent',
      },
      {
        name: 'UL User Data Transfer',
        domain: 'DATA PLANE',
        dependencies: '[[GTP-U]] tunnel established; [[PRACH]] and UL grants active',
        direction: 'gNB-DU → gNB-CU-UP',
        messages: '[[GTP-U]] packets carrying [[PDCP]] PDUs',
        trigger: 'UE transmits uplink data on [[PUSCH]]',
        success: '[[gNB-CU-UP]] forwards PDCP PDUs to [[UPF]] via [[N3]]',
        failure: 'Tunnel failure — GTP-U error indication sent',
      },
      {
        name: 'End Marker',
        domain: 'HANDOVER',
        dependencies: 'Xn or N2 handover in progress',
        direction: 'gNB-CU-UP → gNB-DU',
        messages: 'GTP-U End Marker packet',
        trigger: 'Handover execution — CU-UP signals last forwarded packet',
        success: '[[gNB-DU]] knows data forwarding to source is complete',
        failure: '—',
      },
    ],
  },
  {
    id: 'E1',
    fullName: 'E1 Interface',
    icon: '⚡',
    plane: 'control',
    protocol: 'E1AP',
    node1: 'gNB-CU-CP',
    node1Variant: 'ran',
    node2: 'gNB-CU-UP',
    node2Variant: 'ran',
    unit: 'Bearer',
    procedures: [
      {
        name: 'E1 Setup',
        domain: 'SETUP',
        dependencies: 'SCTP transport established between [[gNB-CU-CP]] and [[gNB-CU-UP]]',
        direction: 'gNB-CU-UP → gNB-CU-CP',
        messages: 'E1 Setup Request / E1 Setup Response',
        trigger: '[[gNB-CU-UP]] initialization',
        success: '[[gNB-CU-CP]] registers CU-UP with its supported [[S-NSSAI]]s and [[DNN]]s',
        failure: 'E1 Setup Failure — unsupported slices or capacity',
      },
      {
        name: 'Bearer Context Setup',
        domain: 'BEARER MANAGEMENT',
        dependencies: 'E1 Setup complete; UE context established at [[gNB-CU-CP]]',
        direction: 'gNB-CU-CP → gNB-CU-UP',
        messages: 'Bearer Context Setup Request / Response',
        trigger: '[[PDU session]] establishment — [[SMF]] provides N3 tunnel info via [[NGAP]]',
        success: '[[gNB-CU-UP]] allocates [[GTP-U]] tunnels toward [[UPF]] and [[gNB-DU]]',
        failure: 'Bearer Context Setup Failure — resource or configuration error',
      },
      {
        name: 'Bearer Context Modification',
        domain: 'BEARER MANAGEMENT',
        dependencies: 'Bearer context exists at [[gNB-CU-UP]]',
        direction: 'gNB-CU-CP → gNB-CU-UP',
        messages: 'Bearer Context Modification Request / Response',
        trigger: '[[DRB]] add/modify/release, [[QoS]] update, or handover preparation',
        success: '[[gNB-CU-UP]] updates tunnel endpoints or [[QoS]] enforcement rules',
        failure: 'Bearer Context Modification Failure',
      },
      {
        name: 'Bearer Context Release',
        domain: 'BEARER MANAGEMENT',
        dependencies: 'Bearer context exists at [[gNB-CU-UP]]',
        direction: 'gNB-CU-CP → gNB-CU-UP',
        messages: 'Bearer Context Release Command / Complete',
        trigger: '[[PDU session]] release or UE context release',
        success: '[[gNB-CU-UP]] tears down all [[GTP-U]] tunnels for the UE',
        failure: '—',
      },
    ],
  },
  {
    id: 'Xn-C',
    fullName: 'Xn Control Plane',
    icon: '🔄',
    plane: 'control',
    protocol: 'XnAP',
    node1: 'gNB',
    node1Variant: 'ran',
    node2: 'gNB',
    node2Variant: 'ran',
    unit: 'Procedure',
    procedures: [
      {
        name: 'Xn Setup',
        domain: 'SETUP',
        dependencies: 'TNL connectivity between neighbor [[gNB]]s',
        direction: 'gNB → gNB (peer)',
        messages: '[[XnAP]] Setup Request / Response',
        trigger: 'Neighbor relation activated (ANR or O&M configured)',
        success: 'Capabilities, supported [[S-NSSAI]]s, and neighbor cell lists exchanged',
        failure: 'Xn Setup Failure — incompatible configuration',
      },
      {
        name: 'Xn-based HO Preparation',
        domain: 'HANDOVER',
        dependencies: 'Xn Setup complete; [[UE]] measurement report triggers handover decision',
        direction: 'gNB (source) → gNB (target)',
        messages: '[[XnAP]] Handover Request',
        trigger: 'Source [[gNB]] selects target based on A3/A4 measurement event',
        success: 'Target [[gNB]] reserves resources and returns transparent container',
        failure: 'Handover Preparation Failure — target admission rejected',
      },
      {
        name: 'HO Request Acknowledge',
        domain: 'HANDOVER',
        dependencies: 'Xn HO Preparation in progress',
        direction: 'gNB (target) → gNB (source)',
        messages: '[[XnAP]] Handover Request Acknowledge',
        trigger: 'Target [[gNB]] successfully reserves resources',
        success: 'Source [[gNB]] receives RRC container to deliver to [[UE]]',
        failure: '—',
      },
      {
        name: 'SN Status Transfer',
        domain: 'HANDOVER',
        dependencies: '[[RRC]] Reconfiguration sent to [[UE]]',
        direction: 'gNB (source) → gNB (target)',
        messages: '[[XnAP]] SN Status Transfer — [[PDCP]] SN and HFN state',
        trigger: 'After handover command issued, before UE accesses target',
        success: 'Target [[gNB]] can continue [[PDCP]] in-sequence delivery',
        failure: '—',
      },
      {
        name: 'UE Context Release (Xn)',
        domain: 'HANDOVER',
        dependencies: 'Path switch via [[N2]] complete',
        direction: 'gNB (target) → gNB (source)',
        messages: '[[XnAP]] UE Context Release',
        trigger: '[[AMF]] confirms path switch; target instructs source to release',
        success: 'Source [[gNB]] releases all [[UE]] radio resources',
        failure: '—',
      },
      {
        name: 'Secondary Node Addition',
        domain: 'DUAL CONNECTIVITY',
        dependencies: 'Master Node ([[MN]]) [[UE]] context active; Xn Setup to Secondary Node complete',
        direction: 'gNB (MN) → gNB (SN)',
        messages: 'SgNB Addition Request / Acknowledge',
        trigger: 'MN decides to add secondary node for higher throughput',
        success: 'SN allocates [[SCG]] resources; [[SCG]] configuration added to RRC',
        failure: 'SgNB Addition Request Reject — SN resource unavailability',
      },
      {
        name: 'Secondary Node Modification',
        domain: 'DUAL CONNECTIVITY',
        dependencies: 'Dual connectivity active',
        direction: 'gNB (MN) ↔ gNB (SN)',
        messages: 'SgNB Modification Request / Acknowledge',
        trigger: '[[DRB]] move, [[QoS]] update, or [[SCG]] reconfiguration',
        success: 'Updated [[SCG]] configuration delivered to [[UE]]',
        failure: 'Modification Reject',
      },
      {
        name: 'Secondary Node Release',
        domain: 'DUAL CONNECTIVITY',
        dependencies: 'Dual connectivity active',
        direction: 'gNB (MN) → gNB (SN)',
        messages: 'SgNB Release Request / Acknowledge',
        trigger: 'MN decision, UE capability, or [[UE]] mobility',
        success: 'SN releases all SCG resources; UE transitions to single connectivity',
        failure: '—',
      },
    ],
  },
  {
    id: 'NG-C',
    fullName: 'NG Control Plane (N2)',
    icon: '🌐',
    plane: 'control',
    protocol: 'NGAP',
    node1: 'gNB-CU',
    node1Variant: 'ran',
    node2: 'AMF',
    node2Variant: 'core',
    unit: 'Procedure',
    procedures: [
      {
        name: 'NG Setup',
        domain: 'SETUP',
        dependencies: 'SCTP transport established between [[gNB]] and [[AMF]]',
        direction: 'gNB → AMF',
        messages: '[[NGAP]] NG Setup Request / Response',
        trigger: '[[gNB]] initialization or restart',
        success: '[[AMF]] accepts [[gNB]] with supported [[TAC]]s, [[PLMN]]s, and slices',
        failure: 'NG Setup Failure — unsupported [[PLMN]] or overload',
      },
      {
        name: 'AMF Configuration Update',
        domain: 'SETUP',
        dependencies: 'NG Setup complete',
        direction: 'AMF → gNB',
        messages: 'AMF Configuration Update / Acknowledge',
        trigger: '[[PLMN]], [[TAC]], or slice configuration change at [[AMF]]',
        success: '[[gNB]] updates its [[AMF]] profile',
        failure: 'AMF Configuration Update Failure',
      },
      {
        name: 'Initial UE Message',
        domain: 'UE REGISTRATION',
        dependencies: 'UE sends RRC Setup Complete with [[NAS]] message',
        direction: 'gNB → AMF',
        messages: '[[NGAP]] Initial UE Message (carries [[NAS]] Registration Request)',
        trigger: 'First uplink [[NAS]] message from [[UE]] after RRC establishment',
        success: '[[AMF]] starts [[UE]] registration procedure',
        failure: 'No response — [[AMF]] overload or [[PLMN]] mismatch',
      },
      {
        name: 'DL / UL NAS Transport',
        domain: 'UE REGISTRATION',
        dependencies: 'Initial UE Message sent; UE context being established',
        direction: 'AMF ↔ gNB ↔ UE',
        messages: 'Downlink NAS Transport / Uplink NAS Transport',
        trigger: 'Any [[NAS]] message exchange during registration or session management',
        success: '[[NAS]] message delivered transparently to/from [[UE]]',
        failure: '[[gNB]] sends Error Indication if UE context not found',
      },
      {
        name: 'Initial Context Setup',
        domain: 'UE REGISTRATION',
        dependencies: 'Authentication and NAS Security Mode complete',
        direction: 'AMF → gNB',
        messages: 'Initial Context Setup Request / Response',
        trigger: '[[AMF]] completes registration and triggers [[AS]] security setup',
        success: '[[gNB]] establishes [[SRB]]s with AS security; [[DRB]]s if PDU session included',
        failure: 'Initial Context Setup Failure — [[gNB]] rejects configuration',
      },
      {
        name: 'UE Context Release',
        domain: 'UE MANAGEMENT',
        dependencies: 'UE context exists at [[gNB]]',
        direction: 'AMF → gNB (or gNB → AMF)',
        messages: 'UE Context Release Command / Complete (or UE Context Release Request)',
        trigger: '[[UE]] deregistration, idle mode transition, or radio failure',
        success: '[[gNB]] releases all resources; [[AMF]] updated',
        failure: '—',
      },
      {
        name: 'PDU Session Resource Setup',
        domain: 'SESSION MANAGEMENT',
        dependencies: 'Initial Context Setup complete; [[SMF]] provides N2 info',
        direction: 'AMF → gNB',
        messages: 'PDU Session Resource Setup Request / Response',
        trigger: '[[PDU session]] establishment — [[AMF]] relays N2 SM info from [[SMF]]',
        success: '[[gNB]] establishes [[DRB]]s; returns N3 [[GTP-U]] tunnel endpoint to [[AMF]]',
        failure: 'PDU Session Resource Setup Unsuccessful — resource or config error',
      },
      {
        name: 'PDU Session Resource Modify',
        domain: 'SESSION MANAGEMENT',
        dependencies: 'PDU session resources active at [[gNB]]',
        direction: 'AMF → gNB',
        messages: 'PDU Session Resource Modify Request / Response',
        trigger: '[[QoS]] flow add/modify or [[SMF]]-triggered bearer change',
        success: '[[gNB]] updates [[DRB]] or [[QoS]] mapping; returns updated tunnel info',
        failure: 'PDU Session Resource Modify Unsuccessful',
      },
      {
        name: 'PDU Session Resource Release',
        domain: 'SESSION MANAGEMENT',
        dependencies: 'PDU session resources active at [[gNB]]',
        direction: 'AMF → gNB',
        messages: 'PDU Session Resource Release Command / Response',
        trigger: '[[PDU session]] release — [[SMF]] instructs [[AMF]] to tear down N2 resources',
        success: '[[gNB]] releases [[DRB]]s and [[GTP-U]] tunnels; [[N3]] path torn down',
        failure: '—',
      },
      {
        name: 'Handover Required',
        domain: 'N2 HANDOVER',
        dependencies: 'No direct Xn between source and target [[gNB]]',
        direction: 'gNB (source) → AMF',
        messages: '[[NGAP]] Handover Required',
        trigger: '[[UE]] measurement report triggers [[gNB]] handover decision; Xn unavailable',
        success: '[[AMF]] initiates Handover Request to target [[gNB]]',
        failure: 'Handover Preparation Failure — target unavailable',
      },
      {
        name: 'Path Switch Request',
        domain: 'XN HANDOVER',
        dependencies: 'Xn handover executed; [[UE]] attached to target [[gNB]]',
        direction: 'gNB (target) → AMF',
        messages: '[[NGAP]] Path Switch Request / Acknowledge',
        trigger: 'Target [[gNB]] notifies [[AMF]] of [[UE]] arrival after Xn handover',
        success: '[[AMF]]/[[SMF]] switches [[N3]] tunnel to target [[gNB]]; [[UPF]] path updated',
        failure: 'Path Switch Request Failure',
      },
      {
        name: 'Paging',
        domain: 'IDLE MODE',
        dependencies: 'UE registered; TA list known to [[AMF]]',
        direction: 'AMF → gNB',
        messages: '[[NGAP]] Paging (carries 5G-S-TMSI and paging occasion)',
        trigger: 'Downlink data or signaling for UE in [[RRC_IDLE]] or [[RRC_INACTIVE]]',
        success: '[[gNB]] schedules paging on PDCCH/PDSCH in paging occasions',
        failure: 'No paging response — [[AMF]] retries with wider [[TA]] list',
      },
    ],
  },
  {
    id: 'NG-U',
    fullName: 'NG User Plane (N3)',
    icon: '📡',
    plane: 'user',
    protocol: 'GTP-U',
    node1: 'gNB-CU-UP',
    node1Variant: 'ran',
    node2: 'UPF',
    node2Variant: 'core',
    unit: 'Tunnel',
    procedures: [
      {
        name: 'UL / DL User Plane Data',
        domain: 'DATA PLANE',
        dependencies: 'N3 [[GTP-U]] tunnel established via [[NGAP]] PDU Session Resource Setup',
        direction: 'gNB-CU-UP ↔ UPF',
        messages: '[[GTP-U]] packets; user IP packets encapsulated with N-TEID',
        trigger: 'Active [[PDU session]] with UE in [[RRC_CONNECTED]]',
        success: 'IP packets forwarded between UE and [[DN]] via [[UPF]]',
        failure: 'GTP-U Error Indication — tunnel not found',
      },
      {
        name: 'End Marker',
        domain: 'HANDOVER',
        dependencies: 'Handover path switch initiated',
        direction: 'UPF → gNB-CU-UP (source)',
        messages: 'GTP-U End Marker — last packet on old N3 path',
        trigger: '[[UPF]] switches N3 tunnel to target [[gNB]] after Path Switch',
        success: 'Source [[gNB]] knows downlink forwarding to target is complete',
        failure: '—',
      },
      {
        name: 'Downlink Data Buffering',
        domain: 'IDLE MODE',
        dependencies: 'UE in [[RRC_IDLE]]; N3 tunnel toward old [[gNB]] torn down',
        direction: 'UPF (internal)',
        messages: 'UPF buffers DL packets per PDR/FAR; sends Downlink Data Notification to [[SMF]]',
        trigger: 'Downlink packet arrives for [[UE]] with no active N3 path',
        success: '[[SMF]] triggers paging via [[AMF]]; UE re-establishes connection',
        failure: 'Buffer overflow — packets dropped per [[QoS]] policy',
      },
    ],
  },
]} />
