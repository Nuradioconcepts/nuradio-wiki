---
title: UE Procedures
description: 5G NR procedures at the User Equipment — PHY, MAC, RLC, PDCP, RRC, NAS, SDAP, and Application layers.
---

import ProtocolTable from '@site/src/components/ProtocolTable';

# UE Procedures

<ProtocolTable protocols={[
  {
    id: 'PHY',
    fullName: 'Physical',
    icon: '📡',
    plane: 'both',
    endpoints: [{ name: 'gNB DU', variant: 'ran' }],
    interfaces: ['Uu'],
    unit: 'Transport Block',
    procedures: [
      {
        name: 'Cell Search',
        domain: 'CONTROL',
        dependencies: '[[SSB]] transmission from [[gNB]]',
        direction: 'Downlink',
        messages: '[[PSS]], [[SSS]], [[PBCH]] / [[MIB]]',
        trigger: 'UE power-on or [[RRC_IDLE|RRC_IDLE]] camping',
        success: 'Frame timing acquired, Cell ID decoded',
        failure: 'No [[SSB]] detected — cell barred or out of coverage',
      },
      {
        name: 'Random Access (PRACH)',
        domain: 'CONTROL',
        dependencies: 'Cell search complete; [[PRACH]] config from [[SIB1]]',
        direction: 'Uplink → Downlink',
        messages: '[[PRACH]] Preamble (Msg1), [[RAR]] on [[PDSCH]] (Msg2)',
        trigger: 'RRC connection request, [[Handover|handover]], or re-establishment',
        success: '[[RAR]] received with timing advance and UL grant',
        failure: 'No [[RAR]] after max preamble retransmissions → MAC failure',
      },
      {
        name: 'UL Power Control',
        domain: 'CONTROL',
        dependencies: 'Path loss measurement; [[TPC]] commands in [[DCI]]',
        direction: 'Uplink',
        messages: '[[TPC]] commands carried in [[DCI]] (DCI 0_0 / 0_1)',
        trigger: 'Every uplink transmission',
        success: 'Target received power at [[gNB]] achieved',
        failure: 'Max TX power reached without meeting target SINR',
      },
      {
        name: 'CSI Measurement & Reporting',
        domain: 'CONTROL',
        dependencies: '[[CSI-RS]] configuration via [[RRC]]',
        direction: 'DL measurement → UL report',
        messages: '[[CSI-RS]] (DL); UCI with [[CQI]]/[[PMI]]/[[RI]] on [[PUCCH]] or [[PUSCH]] (UL)',
        trigger: 'Periodic timer or aperiodic trigger in [[DCI]]',
        success: '[[CQI]] / [[PMI]] / [[RI]] delivered to [[gNB]] scheduler',
        failure: '[[PUCCH]] / [[PUSCH]] decode failure at [[gNB]]',
      },
      {
        name: 'HARQ',
        domain: 'CONTROL / USER',
        dependencies: 'Initial TB transmission',
        direction: 'DL or UL',
        messages: '[[PDSCH]] / [[PUSCH]] retransmission; ACK/NACK on [[PUCCH]]',
        trigger: '[[CRC]] failure on received transport block',
        success: '[[CRC]] passes on retransmission; ACK sent',
        failure: 'Max [[HARQ]] retransmissions exceeded → [[RLC]] [[ARQ]] triggered',
      },
    ],
  },
  {
    id: 'MAC',
    fullName: 'Medium Access Control',
    icon: '🔲',
    plane: 'both',
    endpoints: [{ name: 'gNB DU', variant: 'ran' }],
    interfaces: ['Uu'],
    unit: 'MAC PDU',
    procedures: [
      {
        name: 'Random Access Procedure',
        domain: 'CONTROL',
        dependencies: 'PHY cell search complete; [[RACH]] config from [[SIB1]] or RRCReconfiguration',
        direction: 'UL → DL → UL → DL',
        messages: 'Msg1 [[PRACH]] preamble, Msg2 [[RAR]], Msg3 RRC Setup Request, Msg4 RRC Setup / Contention Resolution',
        trigger: '[[RRC]] establishment, [[Handover|handover]], SR failure, re-establishment',
        success: 'Contention resolution complete; [[C-RNTI]] assigned',
        failure: 'Max retransmissions exceeded → [[RRC]] failure indication',
      },
      {
        name: 'Scheduling Request (SR)',
        domain: 'CONTROL',
        dependencies: '[[RRC_CONNECTED]]; SR [[PUCCH]] resource configured',
        direction: 'Uplink',
        messages: 'SR bit on [[PUCCH]]',
        trigger: 'UE has UL data but holds no active UL grant',
        success: '[[gNB]] responds with UL grant in [[DCI]] 0_0 / 0_1',
        failure: 'Max SR transmissions → [[RACH]] fallback',
      },
      {
        name: 'Buffer Status Report (BSR)',
        domain: 'CONTROL',
        dependencies: 'Active UL grant',
        direction: 'Uplink',
        messages: 'Short [[BSR]], Long [[BSR]], or Truncated [[BSR]] MAC CE',
        trigger: 'New data in UL buffer or periodic BSR timer expiry',
        success: '[[gNB]] scheduler adjusts UL resource allocation',
        failure: '— (no failure state; grant may not increase)',
      },
      {
        name: 'Discontinuous Reception (DRX)',
        domain: 'CONTROL',
        dependencies: '[[DRX]] config from [[RRC]]',
        direction: 'Downlink',
        messages: '[[DRX]] Command MAC CE; Long / Short DRX cycle config in [[RRC]]',
        trigger: 'Inactivity timer expiry or explicit DRX Command',
        success: 'UE sleeps during off-duration; wakes at on-duration start',
        failure: '— (UE defaults to continuous monitoring if config absent)',
      },
      {
        name: 'Logical Channel Prioritization (LCP)',
        domain: 'CONTROL / USER',
        dependencies: 'UL grant; [[LCP]] config (priority, PBR, BSD) from [[RRC]]',
        direction: 'Uplink',
        messages: 'MAC PDU with multiplexed logical channel data',
        trigger: 'UL grant received from [[gNB]] scheduler',
        success: 'Grant filled with data from highest-priority LCHs first',
        failure: '— (lower-priority channels simply starved if grant is small)',
      },
    ],
  },
  {
    id: 'RLC',
    fullName: 'Radio Link Control',
    icon: '🔀',
    plane: 'both',
    endpoints: [{ name: 'gNB DU', variant: 'ran' }],
    interfaces: ['Uu'],
    unit: 'RLC PDU / SDU',
    procedures: [
      {
        name: 'Segmentation & Reassembly',
        domain: 'CONTROL / USER',
        dependencies: '[[RLC]] SDU from [[PDCP]]; transport block size granted by [[MAC]]',
        direction: 'Uplink / Downlink',
        messages: 'RLC Data PDU with segment offset and last-segment flag',
        trigger: 'SDU size exceeds available transport block size',
        success: 'All segments received; SDU reassembled and delivered to [[PDCP]]',
        failure: 'Missing segment → [[ARQ]] STATUS PDU / retransmission',
      },
      {
        name: 'ARQ Retransmission',
        domain: 'CONTROL / USER',
        dependencies: 'AM mode configured; poll timer active',
        direction: 'Uplink / Downlink',
        messages: '[[RLC]] STATUS PDU (ACK / NACK bitmask); AMD PDU retransmission',
        trigger: 'NACK in STATUS PDU or poll-retransmit timer expiry',
        success: 'Missing PDU received; STATUS ACK sent',
        failure: 'Max retransmissions exceeded → [[RRC]] re-establishment',
      },
      {
        name: 'In-sequence Delivery',
        domain: 'CONTROL / USER',
        dependencies: 'Received [[RLC]] PDUs with sequence numbers',
        direction: 'Downlink / Uplink',
        messages: 'RLC Data PDUs with SN field',
        trigger: 'Out-of-order PDU reception',
        success: 'SDUs delivered to [[PDCP]] in strict SN order',
        failure: 'Reordering timer expiry → deliver buffered PDUs regardless',
      },
      {
        name: 'RLC Re-establishment',
        domain: 'CONTROL',
        dependencies: '[[Handover]] execution or radio link failure',
        direction: 'Control (local + peer)',
        messages: 'Triggered via RRCReconfiguration ([[Handover|handover]]) or RRCReestablishment',
        trigger: '[[Handover]], [[RRC]] re-establishment, or security key refresh',
        success: '[[RLC]] state reset; SN restarted; buffers flushed',
        failure: '— (failure handled at [[RRC]] level)',
      },
    ],
  },
  {
    id: 'PDCP',
    fullName: 'Packet Data Convergence',
    icon: '🔄',
    plane: 'both',
    endpoints: [{ name: 'gNB CU', variant: 'ran' }],
    interfaces: ['Uu'],
    unit: 'PDCP PDU / SDU',
    procedures: [
      {
        name: 'Header Compression (ROHC)',
        domain: 'USER',
        dependencies: '[[ROHC]] config from [[RRC]]; established ROHC context',
        direction: 'Uplink / Downlink',
        messages: '[[PDCP]] PDU carrying compressed [[IP]]/[[UDP]]/RTP headers',
        trigger: '[[IP]] packet arrival at [[PDCP]] sublayer',
        success: 'Header decompressed at peer; full [[IP]] packet reconstructed',
        failure: 'Context desync → [[ROHC]] feedback sent; full header transmitted',
      },
      {
        name: 'Ciphering / Deciphering',
        domain: 'CONTROL / USER',
        dependencies: 'KUPenc ([[DRB|UP]]) or KRRCenc ([[SRB|CP]]) derived after Security Mode Command',
        direction: 'Uplink / Downlink',
        messages: 'Ciphered [[PDCP]] Data PDU',
        trigger: 'Every [[PDCP]] PDU after AS security activation',
        success: 'PDU deciphered; plaintext SDU delivered to upper layer',
        failure: 'Deciphering failure → PDU discarded; possible re-establishment',
      },
      {
        name: 'Integrity Protection',
        domain: 'CONTROL',
        dependencies: 'KRRCint key and algorithm from Security Mode Command',
        direction: 'Downlink / Uplink ([[SRB]]s only)',
        messages: '[[PDCP]] Data PDU with 4-byte MAC-I field',
        trigger: 'Every [[SRB]] PDU transmission',
        success: 'MAC-I verified at receiver; PDU accepted',
        failure: 'MAC-I mismatch → PDU discarded; [[RRC]] failure possible',
      },
      {
        name: 'PDCP Reordering',
        domain: 'CONTROL / USER',
        dependencies: 'COUNT-based sequencing; reordering timer config',
        direction: 'Downlink / Uplink',
        messages: '[[PDCP]] Data PDUs with COUNT value',
        trigger: 'Out-of-order [[PDCP]] PDU reception',
        success: 'In-order delivery of SDUs to upper layer',
        failure: 'Reordering timer expiry → deliver buffered PDUs regardless',
      },
      {
        name: 'PDCP Duplication',
        domain: 'USER',
        dependencies: 'Carrier aggregation or [[PDCP]] duplication config from [[RRC]]',
        direction: 'Uplink / Downlink',
        messages: 'Identical [[PDCP]] PDUs sent on primary and secondary paths',
        trigger: 'Duplication activated for the [[DRB]]',
        success: 'First received copy delivered; duplicate discarded',
        failure: '— (both paths may fail independently)',
      },
      {
        name: 'PDCP Re-establishment',
        domain: 'CONTROL / USER',
        dependencies: '[[Handover]] execution or [[RRC]] re-establishment',
        direction: 'Control (local + peer)',
        messages: 'Triggered via RRCReconfiguration or RRCReestablishment',
        trigger: '[[Handover]], security key change, or radio link failure',
        success: '[[PDCP]] state reset; new keys applied; COUNT restarted',
        failure: '— (failure escalates to [[RRC]] level)',
      },
    ],
  },
  {
    id: 'RRC',
    fullName: 'Radio Resource Control',
    icon: '📋',
    plane: 'control',
    endpoints: [{ name: 'gNB CU', variant: 'ran' }],
    interfaces: ['Uu'],
    unit: 'RRC Message',
    procedures: [
      {
        name: 'RRC Connection Establishment',
        domain: 'CONTROL',
        dependencies: '[[MAC]] [[RACH]] complete; [[C-RNTI]] assigned',
        direction: 'UL → DL → UL',
        messages: 'RRCSetupRequest (UL), RRCSetup (DL), RRCSetupComplete (UL)',
        trigger: 'UE in [[RRC_IDLE]] with MO or MT call',
        success: 'UE transitions to [[RRC_CONNECTED]]; [[SRB]] established',
        failure: 'Max retransmissions → UE returns to [[RRC_IDLE]]',
      },
      {
        name: 'RRC Reconfiguration',
        domain: 'CONTROL',
        dependencies: 'UE in [[RRC_CONNECTED]]',
        direction: 'DL → UL',
        messages: 'RRCReconfiguration (DL), RRCReconfigurationComplete (UL)',
        trigger: '[[DRB]] setup/modification, [[Handover|handover]] preparation, measurement config change',
        success: 'New config applied; [[DRB]]s established or modified',
        failure: 'RRCReconfigurationFailure → [[RRC]] Re-establishment initiated',
      },
      {
        name: 'RRC Re-establishment',
        domain: 'CONTROL',
        dependencies: 'Stored UE AS context; target cell known',
        direction: 'UL → DL → UL',
        messages: 'RRCReestablishmentRequest (UL), RRCReestablishment (DL), RRCReestablishmentComplete (UL)',
        trigger: 'Radio link failure, [[Handover|handover]] failure, or integrity check failure',
        success: '[[RRC]] context restored; [[SRB]] re-established',
        failure: 'No suitable cell found → UE transitions to [[RRC_IDLE]]',
      },
      {
        name: 'Measurement & Reporting',
        domain: 'CONTROL',
        dependencies: 'MeasConfig delivered in RRCReconfiguration',
        direction: 'DL config → UL report',
        messages: 'RRCReconfiguration w/ measConfig (DL); MeasurementReport (UL)',
        trigger: 'A1/A2/A3/A4/A5/B1/B2 event threshold or periodic interval',
        success: 'MeasurementReport received by [[gNB]]; may trigger [[Handover|handover]] decision',
        failure: '— (missed report does not cause failure; [[gNB]] may reconfigure)',
      },
      {
        name: 'Paging',
        domain: 'CONTROL',
        dependencies: 'UE in [[RRC_IDLE]] or [[RRC_INACTIVE]]; registered with [[AMF]]',
        direction: 'Downlink',
        messages: 'Paging message on PCCH / [[PDSCH]]',
        trigger: 'MT call or downlink data arrival at [[AMF]] / [[UPF]]',
        success: 'UE wakes, performs [[RACH]], sends Service Request to [[AMF]]',
        failure: 'No response after retries → [[AMF]] may release UE context',
      },
      {
        name: 'RRC Release',
        domain: 'CONTROL',
        dependencies: 'UE in [[RRC_CONNECTED]]',
        direction: 'Downlink',
        messages: 'RRCRelease (DL)',
        trigger: 'Inactivity timer expiry or explicit network decision',
        success: 'UE moves to [[RRC_IDLE]] or [[RRC_INACTIVE]]',
        failure: '— (UE self-releases after T320 if no response)',
      },
    ],
  },
  {
    id: 'NAS',
    fullName: 'NAS-MM / NAS-SM',
    icon: '💬',
    plane: 'control',
    endpoints: [
      { name: 'AMF', variant: 'core' },
      { name: 'SMF', variant: 'core' },
    ],
    interfaces: ['N1'],
    unit: 'NAS Message',
    procedures: [
      {
        name: 'Registration',
        domain: 'CONTROL',
        dependencies: '[[RRC_CONNECTED]]; [[SUCI]] or [[5G-GUTI]] available',
        direction: 'UL → DL',
        messages: 'Registration Request (UL); Auth Request, Security Mode Command, Registration Accept (DL); Registration Complete (UL)',
        trigger: 'Power-on, tracking area change, periodic registration timer',
        success: '[[5G-GUTI]] assigned; UE registered on [[PLMN]]',
        failure: 'Registration Reject with 5GMM cause (e.g. illegal UE, [[PLMN]] not allowed)',
      },
      {
        name: 'Deregistration',
        domain: 'CONTROL',
        dependencies: 'UE in registered state',
        direction: 'UL or DL initiated',
        messages: 'Deregistration Request, Deregistration Accept',
        trigger: 'UE power-off, SIM removal, or network-initiated release',
        success: 'UE deregistered; all PDU sessions released',
        failure: '— (network cleans up context after implicit detach timer)',
      },
      {
        name: 'Authentication (5G-AKA)',
        domain: 'CONTROL',
        dependencies: '[[SUCI]] or [[5G-GUTI]] in Registration Request; [[AUSF]] reachable',
        direction: 'DL → UL',
        messages: 'Authentication Request with RAND/AUTN (DL); Authentication Response with RES* (UL)',
        trigger: 'Initial registration or periodic re-authentication by [[AMF]]',
        success: 'HRES* verified by [[AUSF]]; KAUSF and KAMF derived',
        failure: 'Authentication Failure with AUTS → resync; or Authentication Reject',
      },
      {
        name: 'Security Mode Command',
        domain: 'CONTROL',
        dependencies: 'Successful authentication; KAMF derived',
        direction: 'DL → UL',
        messages: 'Security Mode Command with selected NAS algorithms (DL); Security Mode Complete (UL)',
        trigger: '[[AMF]] selects NAS ciphering and integrity algorithms',
        success: 'NAS ciphering and integrity protection activated',
        failure: 'Security Mode Reject → [[AMF]] may retry or reject registration',
      },
      {
        name: 'PDU Session Establishment',
        domain: 'CONTROL',
        dependencies: 'Registered state; network slice and [[DNN]] selected',
        direction: 'UL → DL',
        messages: 'PDU Session Establishment Request (UL); PDU Session Establishment Accept with IP address (DL)',
        trigger: 'Application requires data connectivity',
        success: '[[DRB]] established; IP address assigned; [[N3]] [[GTP-U]] tunnel active',
        failure: 'PDU Session Establishment Reject (e.g. insufficient resources, [[DNN]] not allowed)',
      },
      {
        name: 'PDU Session Modification',
        domain: 'CONTROL',
        dependencies: 'Active PDU session',
        direction: 'UL or DL initiated',
        messages: 'PDU Session Modification Request / Command; PDU Session Modification Complete',
        trigger: '[[QoS]] flow change, UE request, or network policy update',
        success: '[[QoS]] rules and [[DRB]] config updated',
        failure: 'PDU Session Modification Reject',
      },
      {
        name: 'PDU Session Release',
        domain: 'CONTROL',
        dependencies: 'Active PDU session',
        direction: 'UL or DL initiated',
        messages: 'PDU Session Release Request / Command; PDU Session Release Complete',
        trigger: 'UE disconnect, application close, or network-initiated release',
        success: 'PDU session and all associated bearers released; IP address returned',
        failure: '— (network forces release after implicit timer)',
      },
    ],
  },
  {
    id: 'SDAP',
    fullName: 'Service Data Adaptation',
    icon: '🔗',
    plane: 'user',
    endpoints: [{ name: 'gNB CU', variant: 'ran' }],
    interfaces: ['Uu'],
    unit: 'SDAP PDU / SDU',
    procedures: [
      {
        name: 'QoS Flow to DRB Mapping',
        domain: 'USER',
        dependencies: 'PDU session established; [[SDAP]] config from [[RRC]]',
        direction: 'Uplink / Downlink',
        messages: '[[SDAP]] Data PDU with [[QFI]] field in header',
        trigger: 'Uplink or downlink data arrival at [[SDAP]] sublayer',
        success: '[[QoS]] flow correctly mapped to [[DRB]] with matching QoS profile',
        failure: '— (unmapped [[QoS]] flows dropped)',
      },
      {
        name: 'Reflective QoS Mapping',
        domain: 'USER',
        dependencies: 'RQI bit set in DL [[SDAP]] header; reflective [[QoS]] enabled',
        direction: 'DL trigger → UL reflection',
        messages: 'DL [[SDAP]] PDU with RQI=1',
        trigger: 'Downlink packet with RQI=1 received by UE',
        success: 'UE applies same [[QFI]] → [[DRB]] mapping for corresponding UL traffic',
        failure: '— (UE uses default mapping if RQI timer expires)',
      },
      {
        name: 'SDAP Header Handling',
        domain: 'USER',
        dependencies: '[[SDAP]] header config (with/without) from [[RRC]] per [[DRB]]',
        direction: 'Uplink / Downlink',
        messages: '[[SDAP]] Data PDU with or without 1-byte header',
        trigger: 'Every user-plane SDU passing through [[SDAP]]',
        success: '[[QFI]] and RDI/RQI bits preserved end-to-end',
        failure: '— (misconfiguration causes [[QFI]] mismatch at peer)',
      },
    ],
  },
  {
    id: 'APPLICATION',
    fullName: 'App, TCP/UDP, IP',
    icon: '⬛',
    plane: 'user',
    endpoints: [{ name: 'DN', variant: 'dn' }],
    interfaces: ['N6'],
    unit: 'IP Payload',
    procedures: [
      {
        name: 'IP Address Assignment',
        domain: 'USER',
        dependencies: 'PDU session established; [[SMF]] allocates address from pool',
        direction: 'Downlink (in NAS)',
        messages: 'PDU Session Establishment Accept carrying IPv4/IPv6/IPv4v6 address',
        trigger: 'PDU session type requires [[IP]] connectivity',
        success: 'UE configured with valid [[IP]] address; routing rules applied',
        failure: 'No address available → PDU Session Establishment Reject',
      },
      {
        name: 'TCP Connection Management',
        domain: 'USER',
        dependencies: '[[IP]] address assigned; [[N6]] path to [[DN]] active',
        direction: 'Uplink / Downlink',
        messages: 'SYN (UL), SYN-ACK (DL), ACK (UL) — [[TCP]] three-way handshake',
        trigger: 'Application initiates [[TCP]] connection to remote server',
        success: '[[TCP]] connection established; data transfer begins',
        failure: 'Connection timeout or RST received',
      },
      {
        name: 'UDP Datagram Transfer',
        domain: 'USER',
        dependencies: '[[IP]] address assigned; [[N6]] path active',
        direction: 'Uplink / Downlink',
        messages: '[[UDP]] datagram',
        trigger: 'Application sends [[UDP]] data (e.g. DNS, VoIP, media streaming)',
        success: 'Datagram delivered to [[DN]]',
        failure: 'Packet loss — no transport-layer recovery (application handles)',
      },
      {
        name: 'DNS Resolution',
        domain: 'USER',
        dependencies: '[[IP]] address assigned; DNS server address from PDU session config',
        direction: 'Uplink query → Downlink response',
        messages: 'DNS Query (UL [[UDP]] port 53); DNS Response (DL)',
        trigger: 'Application requires hostname-to-IP resolution',
        success: '[[IP]] address returned for requested hostname',
        failure: 'NXDOMAIN (no record) or query timeout',
      },
    ],
  },
]} />
