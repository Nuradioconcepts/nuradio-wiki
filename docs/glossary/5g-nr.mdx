---
title: 5G NR Glossary 
description: A complete reference of 5G NR terms — protocols, interfaces, components, signals, channels, and identifiers.
hide_table_of_contents: true
---

import GlossaryTable from '@site/src/components/GlossaryTable';

<div className="hero-panel">
  <span className="hero-panel__eyebrow">Glossary · 5G NR</span>
  <h1>5G NR Glossary</h1>
  <p>Protocols, interfaces, components, signals, channels, identifiers, and key concepts across the 5G NR stack. Click any letter to expand its terms — click a term to see its definition, related entries, and spec reference.</p>
</div>

<GlossaryTable entries={[
  // ── Numbers ──────────────────────────────────────────────────────
  {
    id: '5g-aka',
    term: '5G-AKA',
    fullName: '5G Authentication and Key Agreement',
    type: 'Procedure',
    layer: 'NAS',
    definition: 'The primary 5G authentication mechanism. The network delivers RAND and AUTN to the UE; the UE computes RES*, which the AUSF verifies against HRES*. Successful completion derives KAUSF and KAMF for subsequent NAS and AS security.',
    related: ['AUSF', 'AMF', 'SUCI', '5G-GUTI', 'EAP-AKA'],
    spec: '3GPP TS 33.501',
  },
  {
    id: '5g-guti',
    term: '5G-GUTI',
    fullName: '5G Globally Unique Temporary Identifier',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A temporary identifier assigned to a UE by the AMF after successful registration. Replaces the SUPI on the air interface to prevent subscriber tracking. Composed of GUAMI (identifying the AMF) and 5G-TMSI.',
    related: ['SUCI', 'AMF', 'PLMN', 'NAS'],
    spec: '3GPP TS 23.003',
  },
  {
    id: '5gmm',
    term: '5GMM',
    fullName: '5G Mobility Management',
    type: 'Concept',
    layer: 'NAS',
    definition: 'The NAS sublayer responsible for mobility management in 5G, including registration, deregistration, authentication, and security mode command procedures. All 5GMM procedures terminate at the AMF.',
    related: ['NAS', 'AMF', 'RRC'],
    spec: '3GPP TS 24.501',
  },
  // ── A ────────────────────────────────────────────────────────────
  {
    id: 'amf',
    term: 'AMF',
    fullName: 'Access and Mobility Management Function',
    type: 'Component',
    layer: '5GC',
    definition: 'The 5G Core network function responsible for UE access and mobility management. Terminates N1 (NAS toward UE), N2 (NGAP toward gNB), and interfaces with SMF (N11) and AUSF. Handles registration, authentication coordination, paging, and handover signaling.',
    related: ['SMF', 'UPF', 'AUSF', 'NGAP', 'N1', 'N2'],
    spec: '3GPP TS 23.501',
    configParams: '#OCUDU gNB & CU CP\n amf:\n  addrs: 127.0.1.100\n    bind_addrs: 127.0.10.2\n     supported_tracking_areas:\n      - tac: 7',
    logReference: '#Open5gs\n[AMF][Info] UE Registration Request received\n[AMF][Debug] NAS container decoded: 5GMM Registration Request',
    packetCapture: 'ngap.procedureCode == 21  // InitialUEMessage\nngap.procedureCode == 14  // DownlinkNASTransport',
  },
  {
    id: 'arq',
    term: 'ARQ',
    fullName: 'Automatic Repeat Request',
    type: 'Procedure',
    layer: 'RLC',
    definition: 'A link-layer error correction mechanism in RLC AM mode. The receiver sends STATUS PDUs containing ACK/NACK bitmasks; the transmitter retransmits NACKed PDUs. ARQ operates above HARQ, handling residual errors that HARQ chase-combining fails to correct.',
    related: ['HARQ', 'RLC', 'PDCP'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'ausf',
    term: 'AUSF',
    fullName: 'Authentication Server Function',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5G Core network function that executes UE authentication on behalf of the AMF. Retrieves credentials from the UDM and verifies the UE response in 5G-AKA or EAP-AKA\' procedures. Derives and stores KAUSF.',
    related: ['AMF', '5G-AKA', 'SUCI'],
    spec: '3GPP TS 33.501',
  },
  // ── B ────────────────────────────────────────────────────────────
  {
    id: 'bsr',
    term: 'BSR',
    fullName: 'Buffer Status Report',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'A MAC control element sent by the UE to inform the gNB scheduler of the volume of uplink data waiting in its logical channel buffers. Triggers the gNB to allocate appropriate UL resources. Three types: Short BSR, Long BSR, and Truncated BSR.',
    related: ['MAC', 'SR', 'LCP', 'DRB'],
    spec: '3GPP TS 38.321',
  },
  // ── C ────────────────────────────────────────────────────────────
  {
    id: 'c-rnti',
    term: 'C-RNTI',
    fullName: 'Cell Radio Network Temporary Identifier',
    type: 'Identifier',
    layer: 'MAC',
    definition: 'A cell-specific 16-bit identifier assigned to a UE upon successful contention resolution in the random access procedure. Used to address the UE in PDCCH DCI for scheduling, HARQ feedback, and MAC control signaling.',
    related: ['RNTI', 'MAC', 'DCI', 'RACH'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'cqi',
    term: 'CQI',
    fullName: 'Channel Quality Indicator',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A 4-bit value reported by the UE indicating the downlink channel quality observed during a measurement interval. The gNB scheduler uses CQI to select a Modulation and Coding Scheme (MCS) for PDSCH that the UE can receive with at most 10% BLER.',
    related: ['PMI', 'RI', 'CSI', 'CSI-RS', 'PUCCH'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'crc',
    term: 'CRC',
    fullName: 'Cyclic Redundancy Check',
    type: 'Concept',
    layer: 'PHY',
    definition: 'An error detection code appended to transport blocks at the physical layer. After channel decoding, the receiver verifies the CRC; a failed check triggers a HARQ NACK and retransmission request.',
    related: ['HARQ', 'PHY', 'MAC'],
    spec: '3GPP TS 38.212',
  },
  {
    id: 'csi',
    term: 'CSI',
    fullName: 'Channel State Information',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The collection of measurements describing the current state of the radio channel between UE and gNB — including CQI, PMI, RI, L1-RSRP, and L1-SINR. The UE measures and reports CSI to enable adaptive modulation, rank selection, and beamforming.',
    related: ['CQI', 'PMI', 'RI', 'CSI-RS'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'csi-rs',
    term: 'CSI-RS',
    fullName: 'Channel State Information Reference Signal',
    type: 'Signal',
    layer: 'PHY',
    definition: 'A configurable downlink reference signal transmitted by the gNB for channel measurement. The UE measures CSI-RS to derive and report CQI, PMI, and RI. Also used for beam management (L1 RSRP measurement) and RRM/mobility measurements.',
    related: ['CSI', 'CQI', 'PMI', 'RI', 'SSB'],
    spec: '3GPP TS 38.211',
  },
  // ── D ────────────────────────────────────────────────────────────
  {
    id: 'dci',
    term: 'DCI',
    fullName: 'Downlink Control Information',
    type: 'Concept',
    layer: 'PHY',
    definition: 'Control information carried on the PDCCH, used to schedule PDSCH (DL assignment) or PUSCH (UL grant) and to deliver TPC commands. Multiple DCI formats address different scenarios — DCI 0_0/0_1 for UL, DCI 1_0/1_1 for DL.',
    related: ['PDCCH', 'PDSCH', 'PUSCH', 'TPC', 'C-RNTI'],
    spec: '3GPP TS 38.212',
  },
  {
    id: 'dn',
    term: 'DN',
    fullName: 'Data Network',
    type: 'Component',
    layer: 'External',
    definition: 'An external IP network or service that UEs access via a PDU session. Examples include the public internet or an operator IMS/enterprise network. Connected to the 5GC via the UPF on the N6 interface.',
    related: ['UPF', 'N6', 'DNN', 'SMF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'dnn',
    term: 'DNN',
    fullName: 'Data Network Name',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'An APN-equivalent identifier included by the UE in PDU Session Establishment Requests to select the target Data Network and the corresponding SMF/UPF path. Common examples: "internet", "ims". Configured on the USIM or provisioned via OTA.',
    related: ['PDU Session', 'SMF', 'DN', 'NAS'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'drb',
    term: 'DRB',
    fullName: 'Data Radio Bearer',
    type: 'Concept',
    layer: 'RRC / PDCP',
    definition: 'A radio bearer carrying user-plane data. Each DRB is associated with one or more QoS flows via SDAP mapping. DRB configuration (PDCP, RLC, MAC parameters) is delivered by RRC in RRCSetup or RRCReconfiguration.',
    related: ['SRB', 'QoS', 'QFI', 'PDCP', 'RLC', 'SDAP'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'drx',
    term: 'DRX',
    fullName: 'Discontinuous Reception',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'A UE power-saving mechanism configured by RRC. The UE alternates between an on-duration (monitoring PDCCH) and an off-duration (sleep). Long and Short DRX cycles are supported, balancing latency and power consumption.',
    related: ['MAC', 'RRC', 'C-RNTI'],
    spec: '3GPP TS 38.321',
  },
  // ── E ────────────────────────────────────────────────────────────
  {
    id: 'e1',
    term: 'E1',
    fullName: 'E1 Interface',
    type: 'Interface',
    layer: 'NG-RAN',
    definition: 'The interface between gNB-CU-CP and gNB-CU-UP within a disaggregated gNB. Carries E1AP for bearer context setup, modification, and release — allowing independent scaling of control-plane and user-plane CU functions.',
    related: ['gNB-CU-CP', 'gNB-CU-UP', 'E1AP', 'F1'],
    spec: '3GPP TS 38.460',
  },
  {
    id: 'e1ap',
    term: 'E1AP',
    fullName: 'E1 Application Protocol',
    type: 'Protocol',
    layer: 'NG-RAN',
    definition: 'The application-layer protocol on the E1 interface. Supports gNB-CU-UP registration, bearer context setup/modification/release, and capability exchange between the gNB-CU-CP and gNB-CU-UP.',
    related: ['E1', 'gNB-CU-CP', 'gNB-CU-UP'],
    spec: '3GPP TS 38.463',
  },
  {
    id: 'eap-aka',
    term: "EAP-AKA'",
    fullName: 'EAP Authentication and Key Agreement Prime',
    type: 'Procedure',
    layer: 'NAS',
    definition: "An alternative 5G authentication method based on EAP (Extensible Authentication Protocol). Provides equivalent security to 5G-AKA and is selected when specified by the subscriber profile or AUSF configuration.",
    related: ['5G-AKA', 'AUSF', 'AMF'],
    spec: '3GPP TS 33.501',
  },
  // ── F ────────────────────────────────────────────────────────────
  {
    id: 'f1',
    term: 'F1',
    fullName: 'F1 Interface',
    type: 'Interface',
    layer: 'NG-RAN',
    definition: 'The interface between the gNB-CU and one or more gNB-DUs in a disaggregated gNB. Split into F1-C (control plane, carrying F1AP) and F1-U (user plane, carrying GTP-U tunnels for DRB data).',
    related: ['F1-C', 'F1-U', 'F1AP', 'gNB-CU', 'gNB-DU'],
    spec: '3GPP TS 38.470',
  },
  {
    id: 'f1-c',
    term: 'F1-C',
    fullName: 'F1 Control Plane Interface',
    type: 'Interface',
    layer: 'NG-RAN',
    definition: 'The control-plane portion of the F1 interface. Carries F1AP signaling for F1 setup, gNB-DU/CU configuration updates, UE context management (setup/modification/release), and paging.',
    related: ['F1', 'F1AP', 'gNB-CU', 'gNB-DU'],
    spec: '3GPP TS 38.470',
  },
  {
    id: 'f1-u',
    term: 'F1-U',
    fullName: 'F1 User Plane Interface',
    type: 'Interface',
    layer: 'NG-RAN',
    definition: 'The user-plane portion of the F1 interface. Carries GTP-U tunnels between the gNB-CU-UP and gNB-DU for forwarding user-plane PDU session data.',
    related: ['F1', 'GTP-U', 'gNB-CU-UP', 'gNB-DU'],
    spec: '3GPP TS 38.474',
  },
  {
    id: 'f1ap',
    term: 'F1AP',
    fullName: 'F1 Application Protocol',
    type: 'Protocol',
    layer: 'NG-RAN',
    definition: 'The application-layer protocol on the F1-C interface. Manages the F1 Setup procedure, configuration updates between gNB-CU and gNB-DU, UE context management, and paging message distribution.',
    related: ['F1', 'F1-C', 'gNB-CU', 'gNB-DU'],
    spec: '3GPP TS 38.473',
  },
  // ── G ────────────────────────────────────────────────────────────
  {
    id: 'gnb',
    term: 'gNB',
    fullName: 'Next Generation Node B',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The base station providing 5G NR radio access. Connects to UEs via the Uu air interface and to the 5G Core via NG (N2 control, N3 user). May be deployed as a monolithic unit or disaggregated into CU and DU functional blocks.',
    related: ['gNB-CU', 'gNB-DU', 'Uu', 'N2', 'N3', 'NG-RAN'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'gnb-cu',
    term: 'gNB-CU',
    fullName: 'gNB Central Unit',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The centralized functional unit of a disaggregated gNB. Hosts RRC (CP) and PDCP (CP+UP), and optionally SDAP (UP). Connects to one or more gNB-DUs via F1 and to the 5GC via the NG interface.',
    related: ['gNB-DU', 'gNB-CU-CP', 'gNB-CU-UP', 'F1', 'RRC', 'PDCP'],
    spec: '3GPP TS 38.401',
  },
  {
    id: 'gnb-cu-cp',
    term: 'gNB-CU-CP',
    fullName: 'gNB Central Unit – Control Plane',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The control-plane portion of a gNB-CU. Terminates F1-C (F1AP) toward gNB-DU and the E1 (E1AP) toward gNB-CU-UP. Hosts RRC and the control-plane side of PDCP (integrity protection and ciphering for SRBs).',
    related: ['gNB-CU-UP', 'E1', 'F1-C', 'RRC', 'PDCP'],
    spec: '3GPP TS 38.401',
  },
  {
    id: 'gnb-cu-up',
    term: 'gNB-CU-UP',
    fullName: 'gNB Central Unit – User Plane',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The user-plane portion of a gNB-CU. Terminates F1-U toward gNB-DU and N3 (GTP-U) toward UPF. Hosts SDAP and the user-plane portion of PDCP (ciphering and header compression for DRBs).',
    related: ['gNB-CU-CP', 'E1', 'F1-U', 'SDAP', 'PDCP', 'N3'],
    spec: '3GPP TS 38.401',
  },
  {
    id: 'gnb-du',
    term: 'gNB-DU',
    fullName: 'gNB Distributed Unit',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The distributed (radio-proximate) functional unit of a disaggregated gNB. Hosts RLC, MAC, and PHY layers. Connects to gNB-CU via F1 and serves UEs via the Uu radio interface. Typically co-located with radio hardware.',
    related: ['gNB-CU', 'F1', 'MAC', 'RLC', 'PHY', 'Uu'],
    spec: '3GPP TS 38.401',
  },
  {
    id: 'gtp-u',
    term: 'GTP-U',
    fullName: 'GPRS Tunnelling Protocol – User Plane',
    type: 'Protocol',
    layer: 'Transport',
    definition: 'A UDP-based tunneling protocol used to carry user-plane PDU session data between 5G nodes. In 5G NR, GTP-U tunnels are established on N3 (gNB-CU-UP ↔ UPF), N9 (UPF ↔ UPF), and F1-U (gNB-CU-UP ↔ gNB-DU).',
    related: ['N3', 'F1-U', 'UPF', 'gNB-CU-UP'],
    spec: '3GPP TS 29.281',
  },
  // ── H ────────────────────────────────────────────────────────────
  {
    id: 'handover',
    term: 'Handover',
    fullName: 'Handover (HO)',
    type: 'Procedure',
    layer: 'RRC',
    definition: 'The process of transferring an active UE connection from a source cell/gNB to a target cell/gNB while maintaining service continuity. Can be Xn-based (direct source-to-target coordination) or N2-based (via AMF). Involves measurement, preparation, execution, and completion phases.',
    related: ['RRC', 'Xn', 'N2', 'PDCP', 'AMF'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'harq',
    term: 'HARQ',
    fullName: 'Hybrid Automatic Repeat Request',
    type: 'Procedure',
    layer: 'PHY / MAC',
    definition: 'A PHY/MAC-layer retransmission mechanism that combines Forward Error Correction (FEC) with ARQ. On a failed CRC check, the receiver requests retransmission; the new attempt is chase-combined or incremental-redundancy-combined with prior attempts before decoding.',
    related: ['ARQ', 'CRC', 'PDSCH', 'PUSCH', 'PUCCH'],
    spec: '3GPP TS 38.321',
  },
  // ── I ────────────────────────────────────────────────────────────
  {
    id: 'ip',
    term: 'IP',
    fullName: 'Internet Protocol',
    type: 'Protocol',
    layer: 'Application',
    definition: 'The network-layer protocol for packet addressing and routing. In 5G NR, the UE receives an IPv4, IPv6, or dual-stack address via PDU Session Establishment. The 5G network is transparent to IP-level operations.',
    related: ['TCP', 'UDP', 'SMF', 'N6', 'DN'],
    spec: 'IETF RFC 791 / RFC 8200',
  },
  // ── L ────────────────────────────────────────────────────────────
  {
    id: 'lcp',
    term: 'LCP',
    fullName: 'Logical Channel Prioritization',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'The MAC procedure that governs how UL grant bytes are distributed across logical channels within a MAC PDU. Channels are served in decreasing priority order with configured Prioritized Bit Rate (PBR) and Bucket Size Duration (BSD) parameters.',
    related: ['MAC', 'BSR', 'DRB', 'SRB'],
    spec: '3GPP TS 38.321',
  },
  // ── M ────────────────────────────────────────────────────────────
  {
    id: 'mac',
    term: 'MAC',
    fullName: 'Medium Access Control',
    type: 'Protocol',
    layer: 'MAC',
    definition: 'A Layer-2 sublayer in 5G NR responsible for scheduling (via DCI), HARQ operation, random access (RACH), logical channel multiplexing/demultiplexing, and DRX. Runs between UE and gNB-DU over the Uu interface.',
    related: ['RLC', 'PHY', 'HARQ', 'RACH', 'DRX', 'BSR', 'SR'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'me',
    term: 'ME',
    fullName: 'Mobile Equipment',
    type: 'Component',
    layer: 'UE',
    definition: 'The physical terminal portion of a UE — the hardware and software platform including radio chipset, protocol stack, and application processor. Distinguished from the USIM, which holds subscriber credentials and is removable.',
    related: ['UE', 'USIM'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'mib',
    term: 'MIB',
    fullName: 'Master Information Block',
    type: 'Concept',
    layer: 'RRC',
    definition: 'The first system information block decoded by a UE during cell search. Transmitted on the PBCH within the SSB. Contains the SFN (System Frame Number), subcarrier spacing common, and the offset for SIB1 scheduling on PDSCH.',
    related: ['SSB', 'PBCH', 'SIB1', 'Cell Search'],
    spec: '3GPP TS 38.331',
  },
  // ── N ────────────────────────────────────────────────────────────
  {
    id: 'n1',
    term: 'N1',
    fullName: 'N1 Interface',
    type: 'Interface',
    layer: 'UE ↔ 5GC',
    definition: 'The reference point between the UE and the AMF for NAS signaling. Carries 5GMM (mobility management) and 5GSM (session management) messages. Physically transported transparently through the Uu and N2 interfaces.',
    related: ['AMF', 'NAS', 'Uu', 'N2'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n2',
    term: 'N2',
    fullName: 'N2 Interface',
    type: 'Interface',
    layer: 'NG-RAN ↔ 5GC',
    definition: 'The control-plane interface between the gNB-CU and the AMF. Carries NGAP messages for UE registration context, NAS transport, initial context setup, PDU session resource management, handover, and paging.',
    related: ['AMF', 'NGAP', 'gNB-CU', 'N1'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n3',
    term: 'N3',
    fullName: 'N3 Interface',
    type: 'Interface',
    layer: 'NG-RAN ↔ 5GC',
    definition: 'The user-plane interface between the gNB-CU-UP and the UPF. Carries GTP-U tunnels for PDU session user data. One GTP-U tunnel per PDU session (or per QoS flow in multi-tunnel configurations).',
    related: ['UPF', 'GTP-U', 'gNB-CU-UP', 'N4'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n4',
    term: 'N4',
    fullName: 'N4 Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The interface between the SMF and UPF. Carries PFCP (Packet Forwarding Control Protocol) messages for PDU session establishment, GTP-U tunnel configuration, QoS rule enforcement, and usage reporting.',
    related: ['SMF', 'UPF', 'N3'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n6',
    term: 'N6',
    fullName: 'N6 Interface',
    type: 'Interface',
    layer: '5GC ↔ DN',
    definition: 'The interface between the UPF and the external Data Network. Traffic on N6 is plain IP — no GTP-U tunneling. The UPF performs packet routing, NAT, and any required traffic steering toward the DN.',
    related: ['UPF', 'DN', 'DNN', 'N3'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'nas',
    term: 'NAS',
    fullName: 'Non-Access Stratum',
    type: 'Protocol',
    layer: 'NAS',
    definition: 'The protocol layer between the UE and the 5GC (AMF/SMF) that is transparent to the RAN. Encompasses 5GMM (mobility management) and 5GSM (session management) procedures. NAS messages are carried over the N1 reference point, encapsulated in RRC/NGAP at lower layers.',
    related: ['AMF', 'SMF', 'N1', 'RRC', '5GMM'],
    spec: '3GPP TS 24.501',
  },
  {
    id: 'ng-ran',
    term: 'NG-RAN',
    fullName: 'Next Generation Radio Access Network',
    type: 'Component',
    layer: 'RAN',
    definition: 'The radio access network in a 5G system, consisting of gNBs (5G NR) and ng-eNBs (LTE with 5GC connectivity). NG-RAN nodes connect to the 5GC via the NG interface (N2 control, N3 user) and to each other via Xn.',
    related: ['gNB', 'N2', 'N3', 'AMF', 'UPF', 'Xn'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'ngap',
    term: 'NGAP',
    fullName: 'Next Generation Application Protocol',
    type: 'Protocol',
    layer: 'NG-RAN ↔ 5GC',
    definition: 'The control-plane application protocol on the N2 interface between gNB-CU and AMF. Handles NG Setup, initial UE message forwarding, NAS transport, UE context management (initial context setup, modification, release), paging, and handover signaling.',
    related: ['N2', 'AMF', 'gNB-CU', 'F1AP', 'XnAP'],
    spec: '3GPP TS 38.413',
  },
  // ── P ────────────────────────────────────────────────────────────
  {
    id: 'pbch',
    term: 'PBCH',
    fullName: 'Physical Broadcast Channel',
    type: 'Channel',
    layer: 'PHY',
    definition: 'A physical channel within the SSB that carries the encoded MIB. Uses DMRS-based channel estimation and fixed modulation/coding for robustness at cell edge. Repeated across multiple SSB beams for beam sweep coverage.',
    related: ['SSB', 'MIB', 'PSS', 'SSS'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'pdcp',
    term: 'PDCP',
    fullName: 'Packet Data Convergence Protocol',
    type: 'Protocol',
    layer: 'PDCP',
    definition: 'A Layer-2 sublayer above RLC that provides header compression (ROHC), ciphering, integrity protection, PDCP SN management, in-order delivery, and duplication handling. The CP portion (SRBs) terminates on gNB-CU-CP; the UP portion (DRBs) on gNB-CU-UP.',
    related: ['RLC', 'RRC', 'ROHC', 'DRB', 'SRB', 'SDAP'],
    spec: '3GPP TS 38.323',
  },
  {
    id: 'pdsch',
    term: 'PDSCH',
    fullName: 'Physical Downlink Shared Channel',
    type: 'Channel',
    layer: 'PHY',
    definition: 'The primary downlink data channel, used to carry DRB user data, SIBs, RRC messages (via DCCH), and paging messages. Scheduled dynamically per slot by the gNB scheduler, addressed to a UE via DCI on PDCCH.',
    related: ['DCI', 'HARQ', 'CQI', 'gNB-DU'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'phy',
    term: 'PHY',
    fullName: 'Physical Layer',
    type: 'Protocol',
    layer: 'PHY',
    definition: 'The lowest 5G NR protocol layer. Handles modulation, channel coding, resource mapping, OFDM waveform generation, beamforming, and frame/slot timing. Communicates with MAC via transport channels and with the RF hardware via physical channels.',
    related: ['MAC', 'SSB', 'PRACH', 'PDSCH', 'PUSCH', 'PUCCH', 'Uu'],
    spec: '3GPP TS 38.211–38.213',
  },
  {
    id: 'plmn',
    term: 'PLMN',
    fullName: 'Public Land Mobile Network',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A mobile network uniquely identified by a Mobile Country Code (MCC) and Mobile Network Code (MNC). UEs read the PLMN list from SIB1 during cell selection and include the selected PLMN in NAS Registration Requests.',
    related: ['AMF', 'NAS', 'SIB1', '5G-GUTI'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'pmi',
    term: 'PMI',
    fullName: 'Precoding Matrix Indicator',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A CSI report value that recommends the precoding matrix the gNB should apply for PDSCH MIMO transmission. Drawn from a codebook defined by the transmission mode and antenna configuration.',
    related: ['CQI', 'RI', 'CSI', 'CSI-RS'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'prach',
    term: 'PRACH',
    fullName: 'Physical Random Access Channel',
    type: 'Channel',
    layer: 'PHY',
    definition: 'The uplink physical channel on which UEs transmit random access preambles (Msg1). Time-frequency resources and preamble sequences are configured via SIB1 (for initial access) or RRCReconfiguration (for handover/dedicated RACH). Supports Zadoff-Chu based sequences.',
    related: ['RACH', 'SIB1', 'RAR', 'MAC', 'SSB'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'pss',
    term: 'PSS',
    fullName: 'Primary Synchronization Signal',
    type: 'Signal',
    layer: 'PHY',
    definition: 'One of three synchronization components within the SSB. The UE detects PSS to find the half-frame boundary and derive NID2 (one of three values), giving a coarse Physical Cell ID. PSS always occupies the first symbol of the SSB.',
    related: ['SSS', 'SSB', 'PBCH', 'Cell Search'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'pucch',
    term: 'PUCCH',
    fullName: 'Physical Uplink Control Channel',
    type: 'Channel',
    layer: 'PHY',
    definition: 'The uplink physical channel carrying UCI — HARQ ACK/NACK feedback, CSI reports (CQI/PMI/RI), and Scheduling Requests (SR). Multiple PUCCH formats (0–4) cover different payload sizes and time durations.',
    related: ['PUSCH', 'SR', 'HARQ', 'CQI', 'DCI'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'pusch',
    term: 'PUSCH',
    fullName: 'Physical Uplink Shared Channel',
    type: 'Channel',
    layer: 'PHY',
    definition: 'The primary uplink data channel carrying DRB user data and, when scheduled, multiplexed CSI reports and SR. Allocated by the gNB scheduler via UL DCI grants. Supports HARQ retransmissions.',
    related: ['PUCCH', 'HARQ', 'DCI', 'CQI', 'MAC'],
    spec: '3GPP TS 38.211',
  },
  // ── Q ────────────────────────────────────────────────────────────
  {
    id: 'qfi',
    term: 'QFI',
    fullName: 'QoS Flow Identifier',
    type: 'Identifier',
    layer: 'SDAP',
    definition: 'A 6-bit value uniquely identifying a QoS flow within a PDU session. Carried in the SDAP header for uplink and downlink data. The QFI is assigned by the SMF and preserved end-to-end between UPF and UE via the SDAP layer.',
    related: ['QoS', 'DRB', 'SDAP', 'SMF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'qos',
    term: 'QoS',
    fullName: 'Quality of Service',
    type: 'Concept',
    layer: 'End-to-End',
    definition: 'The framework for differentiating data flows by 5QI, GBR/non-GBR class, priority, and packet delay budget. The SMF provisions QoS rules on the UPF (via N4) and delivers QoS configuration to the gNB and UE (via NGAP and RRC) for DRB setup.',
    related: ['QFI', 'DRB', 'SMF', 'UPF', 'SDAP'],
    spec: '3GPP TS 23.501',
  },
  // ── R ────────────────────────────────────────────────────────────
  {
    id: 'rach',
    term: 'RACH',
    fullName: 'Random Access Channel',
    type: 'Channel',
    layer: 'MAC',
    definition: 'The logical/transport channel supporting the random access procedure. The UE transmits a PRACH preamble; the gNB responds with a RAR containing timing advance, UL grant, and Temp C-RNTI. Used for initial access, handover target access, SR failure recovery, and re-establishment.',
    related: ['PRACH', 'RAR', 'MAC', 'SR', 'C-RNTI'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'rar',
    term: 'RAR',
    fullName: 'Random Access Response',
    type: 'Concept',
    layer: 'MAC',
    definition: 'The network response to a PRACH preamble (Msg1 in the 4-step RACH procedure). Delivered on PDSCH, addressed by RA-RNTI. Contains: Timing Advance command, UL grant for Msg3, and Temporary C-RNTI assignment.',
    related: ['PRACH', 'RACH', 'MAC', 'PDSCH', 'C-RNTI'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'ri',
    term: 'RI',
    fullName: 'Rank Indicator',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A CSI report value indicating the number of independent data streams (transmission rank) that the channel can support. RI=1 for single-layer, RI>1 for spatial multiplexing. The gNB scheduler uses RI for MIMO rank adaptation.',
    related: ['CQI', 'PMI', 'CSI', 'CSI-RS'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'rlc',
    term: 'RLC',
    fullName: 'Radio Link Control',
    type: 'Protocol',
    layer: 'RLC',
    definition: 'A Layer-2 sublayer between MAC and PDCP. Supports three modes: TM (Transparent Mode, for BCCH/CCCH), UM (Unacknowledged Mode, for low-latency streams), and AM (Acknowledged Mode, with ARQ for reliable delivery). Runs between UE and gNB-DU.',
    related: ['MAC', 'PDCP', 'ARQ', 'DRB', 'SRB'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'rnti',
    term: 'RNTI',
    fullName: 'Radio Network Temporary Identifier',
    type: 'Identifier',
    layer: 'MAC',
    definition: 'A 16-bit identifier used to address UEs or broadcast channels on the air interface. Key types: C-RNTI (UE-specific scheduling), RA-RNTI (random access response window), P-RNTI (paging), SI-RNTI (system information broadcast).',
    related: ['C-RNTI', 'MAC', 'DCI'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'rohc',
    term: 'ROHC',
    fullName: 'Robust Header Compression',
    type: 'Protocol',
    layer: 'PDCP',
    definition: 'A header compression protocol used within PDCP to reduce IP/UDP/RTP overhead on the radio interface. Particularly beneficial for VoIP traffic, compressing 40-byte IP/UDP/RTP headers down to 1–4 bytes in steady state.',
    related: ['PDCP', 'IP', 'UDP'],
    spec: 'IETF RFC 3095 / 3GPP TS 38.323',
  },
  {
    id: 'rrc',
    term: 'RRC',
    fullName: 'Radio Resource Control',
    type: 'Protocol',
    layer: 'RRC',
    definition: 'The control-plane protocol between UE and gNB-CU-CP. Manages connection establishment/release, RRCReconfiguration (bearer setup, handover), measurement control and reporting, SIB broadcast, and paging. Terminates on the gNB-CU-CP.',
    related: ['NAS', 'PDCP', 'RRC_IDLE', 'RRC_CONNECTED', 'RRC_INACTIVE', 'DRB', 'SRB'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'rrc-connected',
    term: 'RRC_CONNECTED',
    fullName: 'RRC Connected State',
    type: 'Concept',
    layer: 'RRC',
    definition: 'The UE state in which an RRC connection is active. The UE has an AS context in the gNB and an MM context in the AMF. Active DRBs and SRBs are configured; the UE performs configured measurements and can send/receive data.',
    related: ['RRC_IDLE', 'RRC_INACTIVE', 'RRC', 'SRB', 'DRB'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'rrc-idle',
    term: 'RRC_IDLE',
    fullName: 'RRC Idle State',
    type: 'Concept',
    layer: 'RRC',
    definition: 'The UE state with no active RRC connection and no RAN-stored AS context. The UE performs cell reselection autonomously and monitors paging occasions. Data transmission requires a new RACH procedure and full RRC establishment.',
    related: ['RRC_CONNECTED', 'RRC_INACTIVE', 'RRC', 'Cell Search', 'RACH'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'rrc-inactive',
    term: 'RRC_INACTIVE',
    fullName: 'RRC Inactive State',
    type: 'Concept',
    layer: 'RRC',
    definition: 'A 5G NR state (not present in LTE) in which the UE AS context is suspended and stored in both the UE and the last serving gNB. The UE performs cell reselection and can rapidly resume without full RRC re-establishment, saving both UE battery and network signaling.',
    related: ['RRC_IDLE', 'RRC_CONNECTED', 'RRC'],
    spec: '3GPP TS 38.331',
  },
  // ── S ────────────────────────────────────────────────────────────
  {
    id: 'sdap',
    term: 'SDAP',
    fullName: 'Service Data Adaptation Protocol',
    type: 'Protocol',
    layer: 'SDAP',
    definition: 'A 5G NR-specific layer above PDCP on the user plane (absent in LTE). Maps QoS flows to DRBs and adds a 1-byte header containing the QFI and RDI/RQI bits. Terminates on the gNB-CU-UP and provides reflective QoS mapping capability.',
    related: ['PDCP', 'DRB', 'QFI', 'QoS', 'gNB-CU-UP'],
    spec: '3GPP TS 37.324',
  },
  {
    id: 'sib1',
    term: 'SIB1',
    fullName: 'System Information Block 1',
    type: 'Concept',
    layer: 'RRC',
    definition: 'The first system information block decoded after the MIB. Carries the PLMN identity list, cell barring and access restriction status, Tracking Area Code, RACH configuration, and scheduling information for other SIBs. Transmitted on PDSCH, scheduled per the offset in MIB.',
    related: ['MIB', 'PBCH', 'SSB', 'PRACH', 'PLMN', 'RACH'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'smf',
    term: 'SMF',
    fullName: 'Session Management Function',
    type: 'Component',
    layer: '5GC',
    definition: 'The 5G Core function responsible for PDU session lifecycle management. Selects and controls the UPF via N4/PFCP, allocates UE IP addresses, enforces QoS policy, and interfaces with AMF (N11) and PCF for policy. Handles PDU session establishment, modification, and release.',
    related: ['AMF', 'UPF', 'N4', 'DNN', 'QoS', 'DRB'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'sr',
    term: 'SR',
    fullName: 'Scheduling Request',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'A 1-bit uplink request sent by the UE on PUCCH when it has data to transmit but holds no active UL grant. The gNB responds with a UL grant via DCI. After exhausting configured SR transmissions, the UE falls back to RACH.',
    related: ['MAC', 'PUCCH', 'DCI', 'BSR', 'RACH'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'srb',
    term: 'SRB',
    fullName: 'Signalling Radio Bearer',
    type: 'Concept',
    layer: 'RRC',
    definition: 'A radio bearer used exclusively for RRC and NAS control messages. Three SRBs are defined: SRB0 (CCCH — carries RRCSetupRequest before security), SRB1 (DCCH — RRC/NAS after setup, before security activation), SRB2 (DCCH — NAS after AS security).',
    related: ['DRB', 'RRC', 'NAS', 'PDCP'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'ssb',
    term: 'SSB',
    fullName: 'Synchronization Signal Block',
    type: 'Signal',
    layer: 'PHY',
    definition: 'A periodically transmitted downlink structure consisting of PSS (symbol 0), SSS (symbol 2), and PBCH with DMRS (symbols 1 and 3). Carries MIB and provides cell timing/identity. UEs detect SSBs during initial cell search and use them for RRM measurements and beam management.',
    related: ['PSS', 'SSS', 'PBCH', 'MIB', 'CSI-RS', 'Cell Search'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'sss',
    term: 'SSS',
    fullName: 'Secondary Synchronization Signal',
    type: 'Signal',
    layer: 'PHY',
    definition: 'The second synchronization signal in the SSB (symbol 2). Together with PSS (which provides NID2), SSS provides NID1, completing the Physical Cell ID (PCI = 3 × NID1 + NID2). UEs use PSS+SSS to derive full PCI and confirm cell timing.',
    related: ['PSS', 'SSB', 'PBCH', 'Cell Search'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'suci',
    term: 'SUCI',
    fullName: 'Subscription Concealed Identifier',
    type: 'Identifier',
    layer: 'NAS',
    definition: "A privacy-preserving one-time-use identifier that conceals the subscriber's permanent SUPI using the home network's public key (ECIES). Sent in the initial Registration Request instead of the SUPI/IMSI to prevent IMSI-catching attacks.",
    related: ['5G-GUTI', 'AUSF', 'AMF', 'NAS'],
    spec: '3GPP TS 23.003',
  },
  // ── T ────────────────────────────────────────────────────────────
  {
    id: 'tcp',
    term: 'TCP',
    fullName: 'Transmission Control Protocol',
    type: 'Protocol',
    layer: 'Application',
    definition: 'A reliable, connection-oriented transport protocol providing ordered delivery and congestion control. Operates transparently over the 5G PDU session. TCP retransmissions are independent of HARQ/ARQ — the network does not intercept TCP.',
    related: ['UDP', 'IP', 'DN', 'N6'],
    spec: 'IETF RFC 793',
  },
  {
    id: 'tpc',
    term: 'TPC',
    fullName: 'Transmit Power Control',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A mechanism to adjust UE uplink transmit power on a per-slot basis. TPC commands (delta values) are carried in UL DCI (DCI 0_0/0_1). Combined with open-loop path-loss compensation to maintain target received power at the gNB.',
    related: ['DCI', 'PUSCH', 'PUCCH', 'PHY'],
    spec: '3GPP TS 38.213',
  },
  // ── U ────────────────────────────────────────────────────────────
  {
    id: 'udp',
    term: 'UDP',
    fullName: 'User Datagram Protocol',
    type: 'Protocol',
    layer: 'Application',
    definition: 'A connectionless, minimal-overhead transport protocol. Used for latency-sensitive applications such as VoIP, DNS, video streaming, and gaming. Loss recovery is handled at the application layer; ROHC in PDCP can compress UDP headers.',
    related: ['TCP', 'IP', 'DN', 'ROHC'],
    spec: 'IETF RFC 768',
  },
  {
    id: 'ue',
    term: 'UE',
    fullName: 'User Equipment',
    type: 'Component',
    layer: 'UE',
    definition: 'The end-user terminal in a 5G NR network, consisting of the ME (mobile equipment hardware/software) and a USIM (subscriber credentials). Connects to NG-RAN over the Uu air interface and communicates with the 5GC via NAS signaling on the N1 reference point.',
    related: ['ME', 'USIM', 'gNB', 'Uu', 'NAS'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'upf',
    term: 'UPF',
    fullName: 'User Plane Function',
    type: 'Component',
    layer: '5GC',
    definition: 'The 5G Core user-plane anchor. Terminates N3 (GTP-U from gNB-CU-UP) and N6 (IP toward DN). Performs PDU session anchoring, packet routing and forwarding, QoS enforcement (marking, policing), usage measurement, and downlink buffering for paging triggers. Controlled by SMF via N4/PFCP.',
    related: ['SMF', 'N3', 'N6', 'N4', 'GTP-U', 'QoS'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'usim',
    term: 'USIM',
    fullName: 'Universal Subscriber Identity Module',
    type: 'Component',
    layer: 'UE',
    definition: 'The application running on a UICC (the SIM card) that stores the subscriber permanent identity (SUPI/IMSI), long-term secret key K, and authentication algorithms. Used for 5G-AKA derivation and SIM-based cryptographic operations.',
    related: ['UE', 'ME', 'SUCI', '5G-AKA'],
    spec: '3GPP TS 31.102',
  },
  {
    id: 'uu',
    term: 'Uu',
    fullName: 'Uu Interface (NR Air Interface)',
    type: 'Interface',
    layer: 'UE ↔ NG-RAN',
    definition: 'The 5G NR air interface between a UE and its serving gNB. Carries all protocol layers of the radio stack — PHY, MAC, RLC, PDCP, SDAP (UP), and RRC (CP). Uses OFDM waveforms with flexible numerology (subcarrier spacing 15–240 kHz).',
    related: ['gNB', 'UE', 'PHY', 'MAC', 'RLC', 'PDCP', 'RRC'],
    spec: '3GPP TS 38.300',
  },
  // ── X ────────────────────────────────────────────────────────────
  {
    id: 'xn',
    term: 'Xn',
    fullName: 'Xn Interface',
    type: 'Interface',
    layer: 'NG-RAN',
    definition: 'The interface between two gNBs (or between a gNB and an ng-eNB). Carries XnAP for Xn Setup, UE context retrieval, Xn-based handover preparation and execution, SN Status Transfer, and dual connectivity (MR-DC) procedures.',
    related: ['XnAP', 'gNB', 'Handover', 'F1'],
    spec: '3GPP TS 38.420',
  },
  {
    id: 'xnap',
    term: 'XnAP',
    fullName: 'Xn Application Protocol',
    type: 'Protocol',
    layer: 'NG-RAN',
    definition: 'The application-layer protocol on the Xn interface between gNBs. Supports Xn Setup and configuration exchange, handover preparation (Handover Request/Acknowledge), SN Status Transfer, UE Context Release, and dual connectivity management (SgNB Addition/Modification/Release).',
    related: ['Xn', 'gNB', 'NGAP', 'F1AP'],
    spec: '3GPP TS 38.423',
  },

  // ── Numbers (additional) ─────────────────────────────────────────
  {
    id: '5g-cm',
    term: '5G CM',
    fullName: '5G Connection Management',
    type: 'Concept',
    layer: 'NAS',
    definition: 'The NAS sublayer state machine governing the signaling connection between UE and AMF over N1. Defines two states: CM-IDLE (no NAS signaling connection) and CM-CONNECTED (NAS signaling connection established). Distinct from 5G RM which manages registration.',
    related: ['CM Connected', 'CM Idle', 'AMF', 'NAS', '5G RM'],
    spec: '3GPP TS 24.501',
  },
  {
    id: '5g-core',
    term: '5G Core',
    fullName: '5G Core Network',
    type: 'Component',
    layer: '5GC',
    definition: 'The cloud-native, service-based core network of a 5G system. Consists of network functions (AMF, SMF, UPF, AUSF, UDM, PCF, NRF, NSSF, SEPP, NEF) that communicate over the Service Based Architecture (SBA) using HTTP/2 on the SBI. Separates control and user planes (CUPS).',
    related: ['AMF', 'SMF', 'UPF', 'AUSF', 'UDM', 'PCF', 'NRF', 'SBA', 'SBI', '5GS'],
    spec: '3GPP TS 23.501',
  },
  {
    id: '5g-he-av',
    term: '5G HE AV',
    fullName: '5G Home Environment Authentication Vector',
    type: 'Concept',
    layer: 'NAS',
    definition: 'An authentication vector generated by the UDM/ARPF in the home network for the 5G-AKA procedure. Contains RAND, AUTN, XRES*, and KAUSF. Delivered to the AUSF over N13; not forwarded to the serving network.',
    related: ['5G SE AV', 'AV', 'UDM', 'AUSF', 'ARPF', '5G-AKA', 'RAND', 'AUTN'],
    spec: '3GPP TS 33.501',
  },
  {
    id: '5g-rm',
    term: '5G RM',
    fullName: '5G Registration Management',
    type: 'Concept',
    layer: 'NAS',
    definition: 'The NAS state machine tracking whether a UE is registered with the 5GC (RM-REGISTERED) or not (RM-DEREGISTERED). Registration provides the UE with a 5G-GUTI and associates it with an AMF and Registration Area. Distinct from 5G CM which manages the signaling connection.',
    related: ['RM Registered', 'RM Deregistered', 'AMF', 'NAS', '5G CM', 'Registration'],
    spec: '3GPP TS 24.501',
  },
  {
    id: '5g-se-av',
    term: '5G SE AV',
    fullName: '5G Serving Environment Authentication Vector',
    type: 'Concept',
    layer: 'NAS',
    definition: 'An authentication vector derived by the AUSF from the 5G HE AV and sent to the serving AMF. Contains RAND, AUTN, and HXRES*. The AMF uses RAND and AUTN to challenge the UE, and compares HRES* (derived from the UE response) against HXRES* for local verification.',
    related: ['5G HE AV', 'AV', 'AUSF', 'AMF', '5G-AKA', 'HXRES*', 'RAND', 'AUTN'],
    spec: '3GPP TS 33.501',
  },
  {
    id: '5gs',
    term: '5GS',
    fullName: '5G System',
    type: 'Concept',
    layer: 'End-to-End',
    definition: 'The complete 5G system comprising the UE, NG-RAN (gNBs), and 5G Core Network. Defined by 3GPP Release 15+ architecture. Supports eMBB, URLLC, and mMTC service categories over a common network infrastructure.',
    related: ['5G Core', 'NG-RAN', 'UE', 'AMF', 'SMF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: '5gsm',
    term: '5GSM',
    fullName: '5G Session Management',
    type: 'Concept',
    layer: 'NAS',
    definition: 'The NAS sublayer responsible for PDU session management in 5G, including PDU Session Establishment, Modification, and Release procedures. 5GSM messages are transported within NAS PDUs and terminate at the SMF via the AMF.',
    related: ['5GMM', 'NAS', 'SMF', 'PDU Session', 'PDU Session Establishment'],
    spec: '3GPP TS 24.501',
  },
  {
    id: '5qi',
    term: '5QI',
    fullName: '5G QoS Identifier',
    type: 'Identifier',
    layer: '5GC',
    definition: 'A scalar value that maps to a standardized set of QoS characteristics (packet delay budget, packet error rate, priority, resource type). Pre-defined 5QI values cover GBR flows (e.g., 1=voice, 2=video) and non-GBR flows (e.g., 9=best-effort). Configured by the SMF and enforced end-to-end.',
    related: ['QoS', 'QFI', 'GBR', 'GFBR', 'SMF', 'QoS Flow'],
    spec: '3GPP TS 23.501',
  },
  {
    id: '5g-tmsi',
    term: '5G-TMSI',
    fullName: '5G Temporary Mobile Subscriber Identity',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A 32-bit temporary identifier assigned to the UE by the AMF as part of the 5G-GUTI. Combined with the GUAMI, it forms the full 5G-GUTI. Used to identify the UE within the AMF without exposing the SUPI.',
    related: ['5G-GUTI', 'GUAMI', 'AMF', 'SUPI', '5G-s-TMSI'],
    spec: '3GPP TS 23.003',
  },
  {
    id: '5g-s-tmsi',
    term: '5G-s-TMSI',
    fullName: '5G Short Temporary Mobile Subscriber Identity',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A shortened form of the 5G-GUTI used in paging messages and RRC signaling to reduce overhead. Composed of AMF Set ID (10 bits) + AMF Pointer (6 bits) + 5G-TMSI (32 bits). Allows the UE to identify itself during paging response and RRC Resume without sending the full GUTI.',
    related: ['5G-TMSI', '5G-GUTI', 'S-TMSI', 'AMF', 'Paging'],
    spec: '3GPP TS 23.003',
  },
  // ── A (additional) ───────────────────────────────────────────────
  {
    id: 'abba',
    term: 'ABBA',
    fullName: 'Anti-Bidding down Between Architectures',
    type: 'Concept',
    layer: 'NAS',
    definition: 'A 2-octet parameter included in the NAS Security Mode Command to prevent bidding-down attacks between different 5G security architectures. The UE echoes the received ABBA value in the Security Mode Complete, and the AMF verifies it.',
    related: ['Security', 'NAS', 'AMF', 'KAMF'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'ack',
    term: 'ACK',
    fullName: 'Acknowledgement',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'A positive acknowledgement indicating successful receipt of data. In HARQ, ACK indicates correct CRC on PDSCH/PUSCH decoding. In RLC AM, ACK bitmasks in STATUS PDUs confirm delivery. In TCP, ACK confirms receipt of segments.',
    related: ['NACK', 'HARQ', 'RLC AM', 'ARQ'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'aes',
    term: 'AES',
    fullName: 'Advanced Encryption Standard',
    type: 'Concept',
    layer: 'Security',
    definition: 'A symmetric block cipher used in several 5G security algorithms. NEA2 (NR Encryption Algorithm 2) and NIA2 (NR Integrity Algorithm 2) are based on AES-CTR and AES-CMAC respectively. Also used in EEA2/EIA2 for LTE compatibility.',
    related: ['NEA', 'NIA', 'Security', 'PDCP'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'ak',
    term: 'AK',
    fullName: 'Anonymity Key',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A 48-bit key derived during the AKA procedure used to conceal the Sequence Number (SQN) within the AUTN token. Prevents an attacker from deducing the subscriber\'s synchronization state from eavesdropped authentication attempts.',
    related: ['AUTN', 'SQN', 'Authentication', 'AV', 'RAND'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'apn',
    term: 'APN',
    fullName: 'Access Point Name',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'The LTE/EPC equivalent of the 5G DNN (Data Network Name). Identifies the packet data network and gateway to connect to. In 5G NR, the DNN is used instead, though APNs are interworked for EPC compatibility.',
    related: ['DNN', 'PDU Session', 'SMF'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'arpf',
    term: 'ARPF',
    fullName: 'Authentication Credential Repository and Processing Function',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5GC function co-located with the UDM that stores subscriber long-term credentials (key K) and computes authentication vectors (5G HE AV). The ARPF generates RAND, AUTN, XRES*, and KAUSF for each authentication request from the AUSF.',
    related: ['UDM', 'AUSF', '5G HE AV', 'AK', 'SQN', 'Authentication'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'authentication',
    term: 'Authentication',
    fullName: 'Authentication (5G)',
    type: 'Procedure',
    layer: 'NAS',
    definition: 'The procedure by which the network verifies the identity of a UE, and the UE mutually verifies the network. Primary 5G methods are 5G-AKA and EAP-AKA\'. Triggered during Registration; upon success, derives security keys (KAUSF, KSEAF, KAMF) for NAS and AS protection.',
    related: ['5G-AKA', 'EAP-AKA', 'AUSF', 'AMF', 'SUCI', 'KAMF', 'KSEAF'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'autn',
    term: 'AUTN',
    fullName: 'Authentication Token',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A 128-bit token generated by the ARPF/UDM and included in the authentication challenge sent to the UE. Contains SQN XOR AK, AMF field, and MAC. The UE verifies AUTN to authenticate the network before computing its response.',
    related: ['AK', 'SQN', 'RAND', 'AV', 'Authentication', '5G-AKA'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'av',
    term: 'AV',
    fullName: 'Authentication Vector',
    type: 'Concept',
    layer: 'NAS',
    definition: 'A set of parameters generated by the UDM/ARPF for one authentication exchange. In 5G, the Home Environment AV (5G HE AV) contains RAND, AUTN, XRES*, and KAUSF. The AUSF transforms this into the Serving Environment AV (5G SE AV) by replacing XRES* with HXRES*.',
    related: ['5G HE AV', '5G SE AV', 'RAND', 'AUTN', 'Authentication'],
    spec: '3GPP TS 33.501',
  },
  // ── B (additional) ───────────────────────────────────────────────
  {
    id: 'backhaul',
    term: 'Backhaul',
    fullName: 'Backhaul',
    type: 'Concept',
    layer: 'Transport',
    definition: 'The transport network segment connecting the RAN to the core network. In traditional deployments (Option 1), backhaul carries all RAN-core traffic (N2/N3). In C-RAN/disaggregated deployments, it is distinct from fronthaul and midhaul segments.',
    related: ['Fronthaul', 'Midhaul', 'C-RAN', 'NG-RAN', 'N2', 'N3'],
    spec: '',
  },
  {
    id: 'bbu',
    term: 'BBU',
    fullName: 'Baseband Unit',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The baseband processing unit of a base station, responsible for digital signal processing including channel coding, modulation, and protocol layer processing (PHY/MAC/RLC). In C-RAN architectures, the BBU is centralized and connected to distributed RRHs/RRUs via the fronthaul.',
    related: ['RRH', 'RRU', 'C-RAN', 'Fronthaul', 'gNB-DU'],
    spec: '',
  },
  {
    id: 'bcch',
    term: 'BCCH',
    fullName: 'Broadcast Control Channel',
    type: 'Channel',
    layer: 'MAC',
    definition: 'A downlink logical channel carrying system information (MIB and SIBs) to all UEs in a cell. Maps to the BCH transport channel (for MIB via PBCH) and to DL-SCH (for SIBs via PDSCH).',
    related: ['BCH', 'DL SCH', 'MIB', 'SIB1', 'Logical Channels', 'Transport Channels'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'bch',
    term: 'BCH',
    fullName: 'Broadcast Channel',
    type: 'Channel',
    layer: 'MAC',
    definition: 'A downlink transport channel carrying the MIB from the BCCH logical channel. Mapped to the PBCH physical channel within the SSB. Uses a fixed coding rate and modulation for maximum coverage.',
    related: ['BCCH', 'MIB', 'PBCH', 'SSB', 'Transport Channels'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'bw',
    term: 'BW',
    fullName: 'Bandwidth',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The frequency range allocated for transmission or reception. In 5G NR, channel bandwidth is defined in MHz (e.g., 5–100 MHz for FR1; up to 400 MHz for FR2). UEs may operate on a Bandwidth Part (BWP) smaller than the full channel BW.',
    related: ['BWP', 'FR1', 'FR2', 'PRB', 'numerology'],
    spec: '3GPP TS 38.101-1',
  },
  {
    id: 'bwp',
    term: 'BWP',
    fullName: 'Bandwidth Part',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A contiguous set of PRBs configured by the gNB within the channel bandwidth, using a specific numerology. UEs can be configured with up to four DL BWPs and four UL BWPs, with one active at a time. BWP adaptation supports power saving and flexible numerology coexistence.',
    related: ['BW', 'PRB', 'numerology', 'PointA', 'CRB'],
    spec: '3GPP TS 38.211',
  },
  // ── C (additional) ───────────────────────────────────────────────
  {
    id: 'c-ran',
    term: 'C-RAN',
    fullName: 'Centralized / Cloud RAN',
    type: 'Concept',
    layer: 'NG-RAN',
    definition: 'A RAN architecture in which BBU processing is centralized (pooled) and connected to distributed radio units (RRH/RRU) via a fronthaul link. Enables BBU pooling for resource sharing, coordination, and cost reduction. A key enabler of O-RAN and disaggregated gNB deployments.',
    related: ['BBU', 'RRH', 'RRU', 'Fronthaul', 'O-RAN', 'Split 7.2'],
    spec: '',
  },
  {
    id: 'ca',
    term: 'CA',
    fullName: 'Carrier Aggregation',
    type: 'Concept',
    layer: 'PHY / MAC',
    definition: 'A feature allowing a UE to simultaneously receive/transmit on multiple component carriers — one Primary Cell (PCell) and up to 15 Secondary Cells (SCells) — to increase peak data throughput. Intra-band contiguous, intra-band non-contiguous, and inter-band CA variants are defined.',
    related: ['SC', 'FR1', 'FR2', 'BWP', 'MAC'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'cb',
    term: 'CB',
    fullName: 'Code Block',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A segment of a transport block after TB segmentation in the channel coding chain. When a TB exceeds the maximum code block size, it is split into multiple CBs, each independently encoded with LDPC and appended with its own CRC.',
    related: ['CBG', 'TB', 'TBS', 'CRC', 'PHY'],
    spec: '3GPP TS 38.212',
  },
  {
    id: 'cbg',
    term: 'CBG',
    fullName: 'Code Block Group',
    type: 'Concept',
    layer: 'PHY / MAC',
    definition: 'A group of code blocks within a transport block, used for CBG-based HARQ retransmission. Instead of retransmitting the full TB on a NACK, only the failed CBGs are retransmitted, improving efficiency for large TBs with partial errors.',
    related: ['CB', 'TB', 'HARQ', 'DCI', 'PDSCH'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'cbra',
    term: 'CBRA',
    fullName: 'Contention Based Random Access',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'The 4-step random access procedure where the UE selects a preamble randomly from the contention-based set (Group A or B). Steps: Msg1 (preamble on PRACH), Msg2 (RAR), Msg3 (RRC/NAS on PUSCH), Msg4 (Contention Resolution).',
    related: ['CFRA', 'RACH', 'PRACH', 'RAR', 'CR'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'ccch',
    term: 'CCCH',
    fullName: 'Common Control Channel',
    type: 'Channel',
    layer: 'MAC / RLC',
    definition: 'A logical channel used for control signaling before a UE has been assigned a C-RNTI. Carries RRCSetupRequest and RRCSetupComplete during initial access and re-establishment. Mapped to UL-SCH/DL-SCH and uses RLC TM.',
    related: ['DCCH', 'DTCH', 'SRB', 'RLC TM', 'Logical Channels'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'cce',
    term: 'CCE',
    fullName: 'Control Channel Element',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The basic unit of PDCCH resource allocation. Each CCE consists of 6 REGs (Resource Element Groups). PDCCH candidates are aggregations of 1, 2, 4, 8, or 16 CCEs; higher aggregation levels provide more robust coverage at cell edge.',
    related: ['CORESET', 'REG', 'PDCCH', 'SearchSpace', 'DCI'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'cell-id',
    term: 'Cell ID',
    fullName: 'Physical Cell ID (PCI)',
    type: 'Identifier',
    layer: 'PHY',
    definition: 'A numeric identifier for a 5G NR cell derived from the synchronization signals. PCI = 3 × NID1 + NID2, yielding 1008 unique values (NID1 ∈ {0…335}, NID2 ∈ {0,1,2}). NID2 is encoded in the PSS; NID1 is encoded in the SSS.',
    related: ['PCI', 'PSS', 'SSS', 'Cell Search', 'NCI'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'cell-search',
    term: 'Cell Search',
    fullName: 'Cell Search',
    type: 'Procedure',
    layer: 'PHY',
    definition: 'The procedure by which a UE identifies and synchronizes to a 5G NR cell. The UE scans GSCN candidate frequencies for PSS (yielding NID2 and half-frame timing), then SSS (yielding NID1 and full PCI), and decodes PBCH/MIB for SFN and SIB1 scheduling.',
    related: ['PSS', 'SSS', 'SSB', 'GSCN', 'MIB', 'PCI', 'Cell Selection'],
    spec: '3GPP TS 38.304',
  },
  {
    id: 'cell-selection',
    term: 'Cell Selection',
    fullName: 'Cell Selection',
    type: 'Procedure',
    layer: 'RRC',
    definition: 'The process by which a UE chooses a suitable cell to camp on, based on the S-criterion (Srxlev > 0 and Squal > 0). Performed at power-on, after RRC connection release, or when leaving coverage. The UE reads SIB1 to determine if the cell is suitable and not barred.',
    related: ['Cell Search', 'Reselection', 'SIB1', 'RSRP', 'RSRQ', 'PLMN'],
    spec: '3GPP TS 38.304',
  },
  {
    id: 'cfra',
    term: 'CFRA',
    fullName: 'Contention Free Random Access',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'A random access procedure where the network pre-assigns a dedicated preamble to the UE, eliminating the contention resolution step. Used for handover (Msg1 on target cell) and beam failure recovery. Completes in two steps: Msg1 (dedicated preamble) and Msg2 (RAR).',
    related: ['CBRA', 'RACH', 'PRACH', 'RAR', 'Handover'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'ck',
    term: 'CK',
    fullName: 'Cipher Key',
    type: 'Identifier',
    layer: 'Security',
    definition: 'A 128-bit key derived by the UE and USIM during UMTS/LTE AKA procedures. In 5G inter-working scenarios, CK (together with IK) from a legacy AKA procedure may serve as input to the 5G KAUSF derivation function for backward compatibility.',
    related: ['IK', 'AK', 'KSEAF', 'KAMF', 'Authentication', 'USIM'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'cm-connected',
    term: 'CM Connected',
    fullName: 'Connection Management Connected State',
    type: 'Concept',
    layer: 'NAS',
    definition: 'The NAS Connection Management state in which an N1 NAS signaling connection exists between the UE and the AMF. The UE has an established RRC connection and the AMF holds the UE MM context. Data can be sent without re-establishing a connection.',
    related: ['CM Idle', '5G CM', 'NAS', 'AMF', 'RRC_CONNECTED'],
    spec: '3GPP TS 24.501',
  },
  {
    id: 'cm-idle',
    term: 'CM Idle',
    fullName: 'Connection Management Idle State',
    type: 'Concept',
    layer: 'NAS',
    definition: 'The NAS Connection Management state in which no N1 NAS signaling connection exists between UE and AMF. The UE monitors paging occasions and can initiate a Service Request to transition to CM-CONNECTED. The UE may be in RRC_IDLE or RRC_INACTIVE.',
    related: ['CM Connected', '5G CM', 'NAS', 'AMF', 'Paging', 'RRC_IDLE'],
    spec: '3GPP TS 24.501',
  },
  {
    id: 'coreset',
    term: 'CORESET',
    fullName: 'Control Resource Set',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A configured time-frequency resource region within which PDCCH candidates are monitored. Defined by a bitmap of 6-PRB groups in frequency and 1–3 symbols in time. Up to 3 CORESETs per BWP can be configured. CORESET 0 is configured via MIB for initial access.',
    related: ['SearchSpace', 'PDCCH', 'CCE', 'REG', 'MIB', 'BWP'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'cp-cyclic-prefix',
    term: 'CP (Cyclic Prefix)',
    fullName: 'Cyclic Prefix',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A guard interval prepended to each OFDM symbol, consisting of a copy of the symbol\'s tail. Eliminates inter-symbol interference (ISI) caused by multipath propagation, provided the channel delay spread is shorter than the CP duration. Normal CP (14 symbols/slot) and Extended CP (12 symbols/slot, FR2) are defined.',
    related: ['CP-OFDM', 'OFDM', 'numerology', 'Slot', 'Symbol'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'cp-control-plane',
    term: 'CP (Control Plane)',
    fullName: 'Control Plane',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'The set of protocols and interfaces responsible for signaling, configuration, and control across the 5G stack. Includes NAS (UE–AMF), RRC (UE–gNB-CU-CP), F1-C (gNB-CU-CP–gNB-DU), and NGAP (gNB-CU-CP–AMF). Distinct from the User Plane (UP) which carries data.',
    related: ['UP', 'NAS', 'RRC', 'NGAP', 'F1AP', 'gNB-CU-CP'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'cp-ofdm',
    term: 'CP-OFDM',
    fullName: 'Cyclic Prefix OFDM',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The primary 5G NR waveform for both downlink and uplink transmissions. Uses OFDM with a cyclic prefix inserted before each symbol to mitigate multipath. Used for PDSCH, PDCCH, PUCCH, and PUSCH (when DFT spreading is not applied).',
    related: ['CP (Cyclic Prefix)', 'DFT-s-OFDM', 'OFDM', 'OFDMA', 'numerology'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'cpri',
    term: 'CPRI',
    fullName: 'Common Public Radio Interface',
    type: 'Interface',
    layer: 'Transport',
    definition: 'A fronthaul interface standard between the BBU and RRH carrying IQ samples (split at the boundary between RF and digital baseband processing). Has strict latency and bandwidth requirements. In O-RAN, eCPRI provides a more flexible, Ethernet-based alternative.',
    related: ['eCPRI', 'Fronthaul', 'BBU', 'RRH', 'Split 7.2', 'Open Fronthaul'],
    spec: 'CPRI Specification v7.0',
  },
  {
    id: 'cpt',
    term: 'CPT',
    fullName: 'Control PDU Type',
    type: 'Concept',
    layer: 'RLC',
    definition: 'A field in the RLC control PDU header identifying the type of control PDU. In 5G NR RLC AM, the only defined control PDU type is the STATUS PDU (CPT = 0), which carries ACK/NACK bitmasks for ARQ.',
    related: ['RLC AM', 'ARQ', 'NACK', 'ACK'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'cr',
    term: 'CR',
    fullName: 'Contention Resolution',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'Step 4 of the 4-step contention-based RACH procedure (Msg4). The gNB transmits a Contention Resolution MAC CE containing the UE identity from Msg3. The UE that recognizes its identity considers the RACH successful and the temporary C-RNTI becomes permanent.',
    related: ['CBRA', 'RACH', 'C-RNTI', 'MAC', 'RAR'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'crb',
    term: 'CRB',
    fullName: 'Common Resource Block',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A resource block indexed from a common reference point (Point A) within a carrier. CRBs provide a carrier-level coordinate system, independent of BWP configuration. PRB indices within a BWP are offset from CRB 0 by a configured starting position.',
    related: ['PRB', 'RB', 'BWP', 'PointA', 'Resource Grid'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'cri',
    term: 'CRI',
    fullName: 'CSI-RS Resource Indicator',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A CSI report field identifying the preferred CSI-RS resource (beam) from a configured set. Used for beam selection and beam management. The gNB uses the reported CRI to select the transmission beam for PDSCH to that UE.',
    related: ['CSI', 'CSI-RS', 'CQI', 'PMI', 'PUCCH'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'crs',
    term: 'CRS',
    fullName: 'Cell-specific Reference Signal',
    type: 'Signal',
    layer: 'PHY',
    definition: 'A reference signal defined in LTE (not used in 5G NR) that was transmitted from all antenna ports across the full bandwidth. Removed in 5G NR where CSI-RS and DMRS are used instead. Referenced in 5G NR contexts primarily for LTE/NR coexistence and interference management.',
    related: ['CSI-RS', 'DMRS', 'SSB'],
    spec: '3GPP TS 36.211 (LTE)',
  },
  {
    id: 'cu',
    term: 'CU',
    fullName: 'Central Unit',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The centralized functional entity in a disaggregated gNB, hosting higher-layer protocol processing (RRC, PDCP, SDAP). Can be further split into CU-CP (control plane) and CU-UP (user plane). Connects to DUs via F1 and to the 5GC via NG.',
    related: ['gNB-CU', 'CU-CP', 'CU-UP', 'DU', 'F1', 'gNB'],
    spec: '3GPP TS 38.401',
  },
  {
    id: 'cu-cp',
    term: 'CU-CP',
    fullName: 'Central Unit – Control Plane',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The control-plane portion of the CU. Hosts RRC and control-plane PDCP. Connects to CU-UP via E1, to DU via F1-C (F1AP), and to AMF via N2 (NGAP). Manages UE context and RRC signaling.',
    related: ['CU', 'CU-UP', 'gNB-CU-CP', 'E1', 'F1-C', 'NGAP'],
    spec: '3GPP TS 38.401',
  },
  {
    id: 'cu-up',
    term: 'CU-UP',
    fullName: 'Central Unit – User Plane',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The user-plane portion of the CU. Hosts SDAP and user-plane PDCP (ciphering, header compression). Connects to CU-CP via E1, to DU via F1-U, and to UPF via N3 (GTP-U). Handles the data path for DRBs.',
    related: ['CU', 'CU-CP', 'gNB-CU-UP', 'E1', 'F1-U', 'N3'],
    spec: '3GPP TS 38.401',
  },
  {
    id: 'cups',
    term: 'CUPS',
    fullName: 'Control and User Plane Separation',
    type: 'Concept',
    layer: '5GC',
    definition: 'An architectural principle separating control-plane and user-plane network functions for independent scaling and placement. In 5G, CUPS is realized by separating the SMF (control) from the UPF (user plane), connected via N4/PFCP.',
    related: ['SMF', 'UPF', 'N4', 'PFCP', 'SDN'],
    spec: '3GPP TS 23.501',
  },
  // ── D (additional) ───────────────────────────────────────────────
  {
    id: 'dcch',
    term: 'DCCH',
    fullName: 'Dedicated Control Channel',
    type: 'Channel',
    layer: 'MAC / RLC',
    definition: 'A point-to-point logical channel carrying dedicated control information (RRC messages, NAS PDUs) between the UE and gNB. Carried by SRB1 (before AS security) and SRB2 (after AS security). Uses RLC AM mode.',
    related: ['CCCH', 'DTCH', 'SRB', 'RLC AM', 'Logical Channels'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'dft-s-ofdm',
    term: 'DFT-s-OFDM',
    fullName: 'Discrete Fourier Transform Spread OFDM',
    type: 'Concept',
    layer: 'PHY',
    definition: 'An uplink waveform option in 5G NR where data symbols are DFT-precoded before OFDM modulation. Provides lower PAPR (Peak-to-Average Power Ratio) vs. CP-OFDM, beneficial for power-constrained UEs. Configured per BWP for PUSCH.',
    related: ['CP-OFDM', 'PUSCH', 'PHY'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'dl-sch',
    term: 'DL SCH',
    fullName: 'Downlink Shared Channel',
    type: 'Channel',
    layer: 'MAC',
    definition: 'The main downlink transport channel for carrying user data and RRC/NAS control messages to a UE. Supports HARQ, dynamic link adaptation, and beamforming. Mapped from BCCH (SIBs), PCCH (paging), CCCH, DCCH, and DTCH logical channels. Carried on PDSCH.',
    related: ['PDSCH', 'UL SCH', 'Transport Channels', 'HARQ', 'BCCH', 'PCCH'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'dmrs',
    term: 'DMRS',
    fullName: 'Demodulation Reference Signal',
    type: 'Signal',
    layer: 'PHY',
    definition: 'A UE-specific reference signal used for channel estimation at the receiver for coherent demodulation of PDSCH, PUSCH, PDCCH, and PUCCH. Front-loaded by default (first symbol of allocation) with configurable additional DMRS positions for high-mobility. Orthogonal across layers for MIMO.',
    related: ['CSI-RS', 'SRS', 'PDSCH', 'PUSCH', 'Physical Signals'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'dtch',
    term: 'DTCH',
    fullName: 'Dedicated Traffic Channel',
    type: 'Channel',
    layer: 'MAC / RLC',
    definition: 'A point-to-point logical channel carrying user-plane data for a specific UE. Mapped to DRBs and transported on DL-SCH (downlink) or UL-SCH (uplink). Uses RLC AM or UM mode depending on QoS requirements.',
    related: ['DCCH', 'CCCH', 'DRB', 'RLC AM', 'RLC UM', 'Logical Channels'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'du',
    term: 'DU',
    fullName: 'Distributed Unit',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'The distributed (radio-proximate) functional entity in a disaggregated gNB. Hosts lower-layer protocols (RLC, MAC, lower PHY). Connects to CU via F1 and serves UEs via the Uu radio interface. Typically co-located with radio hardware.',
    related: ['gNB-DU', 'CU', 'F1', 'RLC', 'MAC', 'PHY'],
    spec: '3GPP TS 38.401',
  },
  // ── E (additional) ───────────────────────────────────────────────
  {
    id: 'e2',
    term: 'E2',
    fullName: 'E2 Interface',
    type: 'Interface',
    layer: 'O-RAN',
    definition: 'The O-RAN interface between the Near-RT RIC and E2 Nodes (gNB-CU-CP, gNB-CU-UP, gNB-DU). Carries E2AP messages for RAN telemetry subscription, control, and policy enforcement. Enables real-time RAN optimization by xApps running on the Near-RT RIC.',
    related: ['E2AP', 'RIC', 'O-RAN', 'O1', 'gNB-CU-CP', 'gNB-DU'],
    spec: 'O-RAN WG3 E2GAP',
  },
  {
    id: 'e2ap',
    term: 'E2AP',
    fullName: 'E2 Application Protocol',
    type: 'Protocol',
    layer: 'O-RAN',
    definition: 'The application-layer protocol on the E2 interface. Defines procedures for E2 Setup, RIC Subscription (event triggers and report actions), RIC Indication (telemetry), RIC Control (xApp-initiated RAN modifications), and E2 Node Configuration Update.',
    related: ['E2', 'RIC', 'O-RAN'],
    spec: 'O-RAN WG3 E2AP',
  },
  {
    id: 'ecies',
    term: 'ECIES',
    fullName: 'Elliptic Curve Integrated Encryption Scheme',
    type: 'Concept',
    layer: 'Security',
    definition: 'A public-key encryption scheme used in 5G to conceal the SUPI within a SUCI. The UE encrypts its SUPI using the home network\'s public key (an elliptic curve key). The SIDF in the UDM uses the corresponding private key to recover the SUPI from the received SUCI.',
    related: ['SUCI', 'SUPI', 'SIDF', 'UDM', 'Security'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'ecpri',
    term: 'eCPRI',
    fullName: 'Enhanced Common Public Radio Interface',
    type: 'Interface',
    layer: 'Transport',
    definition: 'An Ethernet/IP-based evolution of CPRI for fronthaul transport in C-RAN and O-RAN architectures. Supports functional splits at higher points (e.g., Split 7.2x) compared to traditional CPRI (Split 8), reducing fronthaul bandwidth requirements. Used in O-RAN Open Fronthaul.',
    related: ['CPRI', 'Fronthaul', 'Open Fronthaul', 'Split 7.2', 'O-RAN'],
    spec: 'eCPRI Specification v2.0',
  },
  {
    id: 'embb',
    term: 'eMBB',
    fullName: 'Enhanced Mobile Broadband',
    type: 'Concept',
    layer: 'End-to-End',
    definition: 'One of the three main 5G NR use case categories targeting high peak data rates (up to 20 Gbps DL, 10 Gbps UL), high spectral efficiency, and support for high user densities. Key enablers: massive MIMO, mmWave (FR2), and carrier aggregation.',
    related: ['URLLC', 'MMTC', 'FR1', 'FR2', 'CA', 'NR'],
    spec: '3GPP TS 22.261',
  },
  {
    id: 'epre',
    term: 'EPRE',
    fullName: 'Energy Per Resource Element',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A measure of the average energy transmitted on a single resource element (RE). Reference signal power levels (e.g., SSB EPRE, CSI-RS EPRE) are configured relative to PDSCH EPRE. Used in power boosting and power allocation calculations.',
    related: ['PRB', 'SSB', 'CSI-RS', 'Power Control'],
    spec: '3GPP TS 38.213',
  },
  // ── F (additional) ───────────────────────────────────────────────
  {
    id: 'fapi',
    term: 'FAPI',
    fullName: 'Functional Application Platform Interface',
    type: 'Interface',
    layer: 'NG-RAN',
    definition: 'A Small Cell Forum interface specification defining the API between the MAC scheduler and the PHY layer. Used in disaggregated and open RAN implementations to decouple the scheduler software from PHY hardware/firmware, enabling multi-vendor integration.',
    related: ['PHY', 'MAC', 'O-RAN', 'gNB-DU', 'Split 7.2'],
    spec: 'Small Cell Forum FAPI',
  },
  {
    id: 'fdd',
    term: 'FDD',
    fullName: 'Frequency Division Duplex',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A duplex scheme using separate paired frequency bands for uplink and downlink transmissions simultaneously. In 5G NR FDD, continuous UL and DL are possible, simplifying HARQ timing. Used in FR1 paired spectrum bands.',
    related: ['TDD', 'FR1', 'BWP', 'numerology', 'Frame'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'fr1',
    term: 'FR1',
    fullName: 'Frequency Range 1',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The sub-6 GHz frequency range for 5G NR, covering 410 MHz – 7125 MHz. Supports subcarrier spacings of 15, 30, and 60 kHz. Channel bandwidths up to 100 MHz are defined. FR1 provides better coverage and building penetration than FR2.',
    related: ['FR2', 'NR-ARFCN', 'BWP', 'numerology', 'BW'],
    spec: '3GPP TS 38.101-1',
  },
  {
    id: 'fr2',
    term: 'FR2',
    fullName: 'Frequency Range 2',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The millimeter-wave frequency range for 5G NR, covering 24.25 GHz – 52.6 GHz. Supports subcarrier spacings of 60 and 120 kHz. Channel bandwidths up to 400 MHz (with CA up to 2 GHz) are defined. Provides very high throughput but limited range and penetration.',
    related: ['FR1', 'NR-ARFCN', 'BWP', 'numerology', 'eMBB'],
    spec: '3GPP TS 38.101-2',
  },
  {
    id: 'frame',
    term: 'Frame',
    fullName: 'Radio Frame',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The fundamental 10 ms time unit in 5G NR. Each frame contains 10 subframes of 1 ms each. Slots per subframe depend on numerology μ: 1 (15 kHz), 2 (30 kHz), 4 (60 kHz), 8 (120 kHz). The SFN repeats every 1024 frames (~10.24 s).',
    related: ['Slot', 'Symbol', 'numerology', 'SFN', 'HRF', 'TDD'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'fronthaul',
    term: 'Fronthaul',
    fullName: 'Fronthaul',
    type: 'Concept',
    layer: 'Transport',
    definition: 'The transport network segment between a centralized BBU/DU and remote radio heads (RRH/RRU/O-RU) in a C-RAN or O-RAN architecture. Carries digitized IQ samples (CPRI) or processed baseband data (eCPRI/Open Fronthaul). Stringent latency, bandwidth, and synchronization requirements.',
    related: ['Backhaul', 'Midhaul', 'CPRI', 'eCPRI', 'Open Fronthaul', 'C-RAN', 'BBU'],
    spec: '',
  },
  // ── G (additional) ───────────────────────────────────────────────
  {
    id: 'gbr',
    term: 'GBR',
    fullName: 'Guaranteed Bit Rate',
    type: 'Concept',
    layer: '5GC',
    definition: 'A QoS resource type for flows requiring a minimum committed throughput, such as VoIP and real-time video. GBR flows have guaranteed resources reserved by the network. Described by GFBR (Guaranteed Flow Bit Rate) and MFBR (Maximum Flow Bit Rate) parameters.',
    related: ['GFBR', '5QI', 'QoS', 'QoS Flow', 'DRB', 'SMF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'gfbr',
    term: 'GFBR',
    fullName: 'Guaranteed Flow Bit Rate',
    type: 'Concept',
    layer: '5GC',
    definition: 'The minimum bit rate guaranteed to a GBR QoS flow across the radio and core network. Configured by the SMF as part of the QoS profile and enforced end-to-end. Distinct from MFBR (Maximum Flow Bit Rate) which caps the peak rate.',
    related: ['GBR', '5QI', 'QoS Flow', 'SMF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'gnb-id',
    term: 'gNB ID',
    fullName: 'gNB Identifier',
    type: 'Identifier',
    layer: 'NG-RAN',
    definition: 'A variable-length (22–32 bit) identifier uniquely identifying a gNB within a PLMN. Together with the PLMN ID, forms the Global gNB ID used in NGAP and Xn messages. Allocated by the operator and configured at deployment.',
    related: ['NCI', 'PLMN', 'GUAMI', 'gNB', 'NGAP'],
    spec: '3GPP TS 38.413',
  },
  {
    id: 'gp',
    term: 'GP',
    fullName: 'Guard Period',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A silent period in a TDD slot between downlink and uplink transmission to allow propagation delay compensation and prevent UL/DL interference. Configured via slot format — symbols between DL and UL symbol regions are designated as flexible/guard symbols.',
    related: ['TDD', 'SFI', 'Slot', 'Frame', 'TA'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'gscn',
    term: 'GSCN',
    fullName: 'Global Synchronization Channel Number',
    type: 'Identifier',
    layer: 'PHY',
    definition: 'A channel raster defined for SSB transmission, coarser than the NR-ARFCN channel raster. UEs scan GSCN positions rather than all possible NR-ARFCN frequencies during cell search, reducing search time. GSCN values map to specific carrier frequencies per band.',
    related: ['NR-ARFCN', 'SSB', 'Cell Search', 'FR1', 'FR2'],
    spec: '3GPP TS 38.104',
  },
  {
    id: 'guami',
    term: 'GUAMI',
    fullName: 'Globally Unique AMF Identifier',
    type: 'Identifier',
    layer: '5GC',
    definition: 'A globally unique identifier for an AMF instance composed of PLMN ID + AMF Region ID (8 bits) + AMF Set ID (10 bits) + AMF Pointer (6 bits). Used in 5G-GUTI construction and in NGAP to route NAS messages to the correct AMF.',
    related: ['AMF', '5G-GUTI', '5G-TMSI', 'PLMN', 'NGAP'],
    spec: '3GPP TS 23.003',
  },
  // ── H (additional) ───────────────────────────────────────────────
  {
    id: 'hres',
    term: 'HRES*',
    fullName: 'Hashed RES*',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A value computed by the AMF as SHA-256(RAND || RES*) after receiving the UE\'s RES* response during 5G-AKA. The AMF compares HRES* against HXRES* received from the AUSF for local authentication verification before forwarding RES* to the AUSF for final verification.',
    related: ['HXRES*', 'RES*', 'RAND', '5G-AKA', 'AMF', 'AUSF'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'hrf',
    term: 'HRF',
    fullName: 'Half Radio Frame',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A 5 ms period corresponding to half of a 10 ms radio frame (5 subframes). SSB bursts are confined within a half-frame. The half-frame index (0 or 1) is indicated by a bit in the PBCH/MIB.',
    related: ['Frame', 'Slot', 'SSB', 'MIB', 'SFN'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'http2',
    term: 'HTTP/2',
    fullName: 'Hypertext Transfer Protocol version 2',
    type: 'Protocol',
    layer: '5GC',
    definition: 'The transport protocol used for Service Based Interface (SBI) communication between 5GC NFs. HTTP/2 over TLS provides multiplexed, low-latency signaling with binary framing. RESTful APIs using JSON encode NF service requests and responses.',
    related: ['SBI', 'SBA', 'TLS', 'NF', 'AMF', 'SMF'],
    spec: '3GPP TS 29.500',
  },
  {
    id: 'hxres',
    term: 'HXRES*',
    fullName: 'Hashed Expected RES*',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A value derived by the AUSF as SHA-256(RAND || XRES*) from the home authentication vector. Sent to the serving AMF as part of the 5G SE AV in place of XRES*, protecting the expected response from exposure to the serving network.',
    related: ['HRES*', 'XRES*', 'RAND', '5G-AKA', 'AUSF', 'AMF', '5G SE AV'],
    spec: '3GPP TS 33.501',
  },
  // ── I (additional) ───────────────────────────────────────────────
  {
    id: 'idle-mode',
    term: 'Idle Mode',
    fullName: 'Idle Mode',
    type: 'Concept',
    layer: 'RRC',
    definition: 'The UE operating state when no RRC connection is active (RRC_IDLE) or when the AS context is suspended (RRC_INACTIVE). The UE performs cell selection/reselection, monitors paging occasions, and manages PLMN selection while minimizing battery consumption.',
    related: ['RRC_IDLE', 'RRC_INACTIVE', 'CM Idle', 'Cell Selection', 'Reselection', 'Paging'],
    spec: '3GPP TS 38.304',
  },
  {
    id: 'ik',
    term: 'IK',
    fullName: 'Integrity Key',
    type: 'Identifier',
    layer: 'Security',
    definition: 'A 128-bit key used for integrity protection in UMTS/LTE procedures. In 5G inter-working, IK (together with CK) derived in a legacy AKA procedure may serve as input to the 5G key derivation function for KAUSF.',
    related: ['CK', 'AK', 'KAMF', 'KSEAF', 'Authentication'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'imei',
    term: 'IMEI',
    fullName: 'International Mobile Equipment Identity',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A 15-digit identifier uniquely identifying mobile equipment hardware, independent of the SIM/USIM. In 5G, replaced by PEI (Permanent Equipment Identifier), which can take the form of IMEI or IMEISV. Used for equipment blacklisting and lawful intercept.',
    related: ['PEI', 'ME', 'UE', 'USIM'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'ims',
    term: 'IMS',
    fullName: 'IP Multimedia Subsystem',
    type: 'Component',
    layer: 'External',
    definition: 'A standards-based framework for delivering IP multimedia services including VoIP, video calling, and messaging. In 5G, IMS traffic is carried over the IMS PDU session (DNN "ims") with GBR QoS flows (5QI 1 for voice). VoNR uses IMS over the 5G radio access.',
    related: ['VoNR', 'SIP', 'PDU Session', 'GBR', '5QI', 'DNN'],
    spec: '3GPP TS 23.228',
  },
  {
    id: 'initial-access',
    term: 'Initial Access',
    fullName: 'Initial Access',
    type: 'Procedure',
    layer: 'PHY / RRC',
    definition: 'The end-to-end procedure by which a UE without an existing connection attaches to a 5G NR cell: (1) Cell Search — detect PSS/SSS/MIB; (2) SIB1 acquisition — read RACH configuration; (3) RACH — obtain UL sync and C-RNTI; (4) RRC Setup — establish SRBs and request Registration.',
    related: ['Cell Search', 'Cell Selection', 'RACH', 'SIB1', 'PRACH', 'RRC', 'Registration'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'ipsec',
    term: 'IPSec',
    fullName: 'Internet Protocol Security',
    type: 'Protocol',
    layer: 'Transport',
    definition: 'A protocol suite providing authentication and encryption at the IP layer. In 5G NR, IPSec secures transport-layer links (e.g., F1, N2, N3 over untrusted networks) and Non-3GPP access via the N3IWF.',
    related: ['TLS', 'N2', 'N3', 'SEPP', 'Security'],
    spec: '3GPP TS 33.501',
  },
  // ── K ────────────────────────────────────────────────────────────
  {
    id: 'kamf',
    term: 'KAMF',
    fullName: 'AMF Key',
    type: 'Identifier',
    layer: 'Security',
    definition: 'A 256-bit key derived from KSEAF and the SUPI at the AMF (and UE). KAMF is the root key for NAS and AS security in 5G. Used to derive KNASint, KNASenc (for NAS), and KgNB (for RRC/UP AS security).',
    related: ['KSEAF', 'KgNB', 'KDF', 'Authentication', 'AMF', 'Security'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'kdf',
    term: 'KDF',
    fullName: 'Key Derivation Function',
    type: 'Concept',
    layer: 'Security',
    definition: 'A cryptographic function used throughout the 5G key hierarchy to derive lower-layer keys from parent keys using HMAC-SHA-256. Specific FC (Function Code) values and input parameters are defined for each derivation (e.g., KAUSF → KSEAF, KSEAF → KAMF, KAMF → KgNB).',
    related: ['KAMF', 'KSEAF', 'KgNB', 'Authentication'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'kgnb',
    term: 'KgNB',
    fullName: 'gNB Key',
    type: 'Identifier',
    layer: 'Security',
    definition: 'A 256-bit key derived from KAMF, passed to the gNB-CU-CP via NGAP Initial Context Setup or Handover. Used by the gNB to derive KRRCint, KRRCenc (RRC message protection) and KUPint, KUPenc (PDCP user-plane protection).',
    related: ['KAMF', 'KDF', 'Security', 'RRC', 'PDCP', 'gNB-CU-CP'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'kseaf',
    term: 'KSEAF',
    fullName: 'SEAF Key',
    type: 'Identifier',
    layer: 'Security',
    definition: 'A 256-bit key derived from KAUSF after successful authentication. Passed to the SEAF (co-located with AMF). Used as input to derive KAMF. Acts as the anchor key between the home network (AUSF) and serving network (AMF).',
    related: ['KAMF', 'KDF', 'AUSF', 'SEAF', 'Authentication'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'ksi',
    term: 'KSI',
    fullName: 'Key Set Identifier',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A 3-bit identifier in NAS messages referencing a previously established security context (KAMF) without re-running full authentication. The UE and AMF match KSI values to identify the current NAS security context.',
    related: ['KAMF', 'NAS', 'Authentication', 'Security', 'AMF'],
    spec: '3GPP TS 24.501',
  },
  {
    id: 'kssb',
    term: 'K_SSB',
    fullName: 'SSB Subcarrier Offset',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A parameter in the MIB (as ssb-SubcarrierOffset) specifying the frequency offset in subcarriers between Point A and the lowest subcarrier of the SSB. Allows fine-grained positioning of the SSB within the channel bandwidth.',
    related: ['SSB', 'PointA', 'MIB', 'GSCN', 'CRB'],
    spec: '3GPP TS 38.331',
  },
  // ── L (additional) ───────────────────────────────────────────────
  {
    id: 'lcid',
    term: 'LCID',
    fullName: 'Logical Channel ID',
    type: 'Identifier',
    layer: 'MAC',
    definition: 'A 5 or 6-bit field in the MAC subheader identifying the logical channel or MAC CE type within a MAC PDU. Values 1–4 are reserved for SRBs; values 5–32 are for DRBs. Special values identify MAC CEs (BSR, PHR, Timing Advance, etc.).',
    related: ['MAC', 'Logical Channels', 'DRB', 'SRB', 'BSR'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'lmax',
    term: 'Lmax',
    fullName: 'Maximum Number of SSBs per Half-Frame',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The maximum number of SS/PBCH blocks that can be transmitted within a half-frame. Lmax depends on carrier frequency: 4 SSBs for FR1 below 3 GHz, 8 SSBs for FR1 3–6 GHz, and 64 SSBs for FR2. Used to dimension the SSB beam sweep and PBCH MIB encoding.',
    related: ['SSB', 'HRF', 'Frame', 'PBCH', 'MIB'],
    spec: '3GPP TS 38.213',
  },
  {
    id: 'logical-channels',
    term: 'Logical Channels',
    fullName: 'Logical Channels',
    type: 'Concept',
    layer: 'MAC',
    definition: 'Channels defined between the RLC and MAC layers, classified by the type of information carried. DL: BCCH, PCCH, CCCH, DCCH, DTCH. UL: CCCH, DCCH, DTCH. Each logical channel maps to a transport channel via MAC multiplexing.',
    related: ['Transport Channels', 'Physical Channels', 'BCCH', 'CCCH', 'DCCH', 'DTCH', 'PCCH'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'lsb',
    term: 'LSB',
    fullName: 'Least Significant Bit',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'The bit in a binary integer with the lowest positional value (2^0). In 5G NR, relevant in SFN encoding — the MIB carries the 6 MSBs of the 10-bit SFN; the 4 LSBs are derived from the PBCH DMRS sequence index.',
    related: ['MSB', 'SFN', 'MIB'],
    spec: '',
  },
  // ── M (additional) ───────────────────────────────────────────────
  {
    id: 'mac-i',
    term: 'MAC-I',
    fullName: 'Message Authentication Code for Integrity',
    type: 'Identifier',
    layer: 'Security',
    definition: 'A 32-bit integrity check value appended to NAS and RRC messages. Computed using the active integrity algorithm (e.g., NIA1/NIA2) over the message content, COUNT, BEARER, and DIRECTION. The receiver verifies MAC-I against XMAC to detect tampering.',
    related: ['XMAC', 'NIA', 'PDCP', 'RRC', 'NAS', 'Security'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'mcs',
    term: 'MCS',
    fullName: 'Modulation and Coding Scheme',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A combined index specifying the modulation order (QPSK, 16QAM, 64QAM, 256QAM) and channel coding rate for PDSCH/PUSCH. Selected by the gNB based on reported CQI. The TBS is computed from MCS, PRB count, and number of layers.',
    related: ['CQI', 'TBS', 'PDSCH', 'PUSCH', 'QAM', 'QPSK'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'measurement-report',
    term: 'Measurement Report',
    fullName: 'Measurement Report',
    type: 'Procedure',
    layer: 'RRC',
    definition: 'An RRC message sent by the UE to the gNB-CU-CP reporting measured signal quality (RSRP, RSRQ, SINR) for serving and neighbor cells/beams. Triggered by configured measurement events (A1–A6 for intra-frequency; B1–B2 for inter-frequency). Used for handover and beam management decisions.',
    related: ['RSRP', 'RSRQ', 'SINR', 'Handover', 'RRC', 'CSI-RS', 'SSB'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'mec',
    term: 'MEC',
    fullName: 'Multi-access Edge Computing',
    type: 'Concept',
    layer: 'External',
    definition: 'A network architecture placing compute and storage at the edge of the network (near the RAN) to reduce latency. In 5G NR, MEC is enabled by local UPF breakouts (N6 to local data networks) steered by the SMF, routing traffic to edge application servers.',
    related: ['UPF', 'SMF', 'N6', 'PDU Session', 'URLLC'],
    spec: 'ETSI GS MEC 003',
  },
  {
    id: 'midhaul',
    term: 'Midhaul',
    fullName: 'Midhaul',
    type: 'Concept',
    layer: 'Transport',
    definition: 'The transport network segment between the CU and DU in a 3-layer disaggregated RAN architecture (CU–DU–RU). Carries F1 interface traffic (F1AP and GTP-U). Less stringent latency requirements than fronthaul.',
    related: ['Fronthaul', 'Backhaul', 'CU', 'DU', 'F1', 'O-RAN'],
    spec: '',
  },
  {
    id: 'mmtc',
    term: 'mMTC',
    fullName: 'Massive Machine-Type Communications',
    type: 'Concept',
    layer: 'End-to-End',
    definition: 'One of the three main 5G NR use case categories targeting extremely high connection density (up to 1 million devices/km²), low-cost devices, and deep coverage. Characterized by infrequent small data transmissions and low power requirements.',
    related: ['eMBB', 'URLLC', 'NR', 'NTN'],
    spec: '3GPP TS 22.261',
  },
  {
    id: 'mo',
    term: 'MO',
    fullName: 'Mobile Originated',
    type: 'Concept',
    layer: 'NAS',
    definition: 'Refers to signaling or data flows initiated by the UE in the uplink direction (e.g., MO call, MO data session, MO Registration Request). Contrasted with MT (Mobile Terminated).',
    related: ['MT', 'NAS', 'PDU Session', 'Registration'],
    spec: '3GPP TS 24.501',
  },
  {
    id: 'msb',
    term: 'MSB',
    fullName: 'Most Significant Bit',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'The bit in a binary integer with the highest positional value. In 5G NR, relevant in SFN encoding — the 6 MSBs of the 10-bit SFN are broadcast in the MIB; the 4 LSBs are derived from the PBCH DMRS sequence.',
    related: ['LSB', 'SFN', 'MIB'],
    spec: '',
  },
  {
    id: 'msi',
    term: 'MSI',
    fullName: 'Minimum System Information',
    type: 'Concept',
    layer: 'RRC',
    definition: 'The subset of system information always broadcast periodically, enabling initial access without on-demand request. Consists of the MIB (on PBCH) and SIB1 (on PDSCH). All other SIBs are classified as Other System Information (OSI) and may be broadcast or requested on-demand.',
    related: ['MIB', 'SIB1', 'OSI', 'SIB', 'BCCH'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'mt',
    term: 'MT',
    fullName: 'Mobile Terminated',
    type: 'Concept',
    layer: 'NAS',
    definition: 'Refers to signaling or data flows directed toward the UE by the network (e.g., MT call setup, MT data delivery via paging and Service Request). Contrasted with MO (Mobile Originated).',
    related: ['MO', 'Paging', 'NAS', 'AMF'],
    spec: '3GPP TS 24.501',
  },
  // ── N (additional) ───────────────────────────────────────────────
  {
    id: 'n7',
    term: 'N7',
    fullName: 'N7 Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The SBI interface between the SMF and the PCF. Carries policy requests and responses for session-level QoS policy. The SMF requests PCC rules for PDU sessions; the PCF delivers QoS rules and charging parameters.',
    related: ['SMF', 'PCF', 'N15', 'QoS', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n8',
    term: 'N8',
    fullName: 'N8 Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The SBI interface between the AMF and the UDM. Used by the AMF to retrieve subscriber access and mobility subscription data and to subscribe to UDM data change notifications.',
    related: ['AMF', 'UDM', 'N10', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n9',
    term: 'N9',
    fullName: 'N9 Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The user-plane interface between two UPF instances (UPF–UPF). Used with multiple UPFs (e.g., I-UPF and PSA-UPF) to carry GTP-U tunnels for PDU session data and for inter-UPF handover scenarios.',
    related: ['UPF', 'N3', 'N4', 'SMF', 'GTP-U'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n10',
    term: 'N10',
    fullName: 'N10 Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The SBI interface between the SMF and the UDM. Used by the SMF to retrieve session management subscription data and to notify the UDM of PDU session establishment/release events.',
    related: ['SMF', 'UDM', 'N8', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n11',
    term: 'N11',
    fullName: 'N11 Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The SBI interface between the AMF and the SMF. Used for PDU session context management — the AMF forwards NAS SM messages to the SMF and receives SM context responses for session establishment, modification, and release.',
    related: ['AMF', 'SMF', 'N1', 'N2', 'PDU Session', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n12',
    term: 'N12',
    fullName: 'N12 Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The SBI interface between the AMF and the AUSF. Used to initiate authentication — the AMF sends an Authentication Request (with SUCI or 5G-GUTI) to the AUSF, which returns a response with the 5G SE AV.',
    related: ['AMF', 'AUSF', 'Authentication', 'SUCI', '5G SE AV', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n13',
    term: 'N13',
    fullName: 'N13 Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The SBI interface between the UDM and the AUSF. The AUSF requests authentication credentials (5G HE AV) from the UDM/ARPF, which generates and returns RAND, AUTN, XRES*, and KAUSF for the requested subscriber.',
    related: ['UDM', 'AUSF', 'ARPF', '5G HE AV', 'Authentication', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'n15',
    term: 'N15',
    fullName: 'N15 Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The SBI interface between the AMF and the PCF. Used to retrieve access and mobility policy (UE route selection policies, network slice selection policies) for a UE.',
    related: ['AMF', 'PCF', 'N7', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'nack',
    term: 'NACK',
    fullName: 'Negative Acknowledgement',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'A signal indicating unsuccessful receipt of data, requesting retransmission. In HARQ, a NACK is sent on PUCCH when PDSCH/PUSCH decoding fails (CRC error). In RLC AM, NACK fields in STATUS PDUs identify missing PDUs for ARQ retransmission.',
    related: ['ACK', 'HARQ', 'RLC AM', 'ARQ', 'PUCCH'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'nai',
    term: 'NAI',
    fullName: 'Network Access Identifier',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A string identifier in "user@realm" format used to route authentication requests to the home network. In 5G, the SUCI may be formatted as an NAI for EAP-based authentication. The realm identifies the home PLMN.',
    related: ['SUCI', 'SUPI', 'Authentication', 'EAP-AKA'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'nci',
    term: 'NCI',
    fullName: 'NR Cell Identity',
    type: 'Identifier',
    layer: 'NG-RAN',
    definition: 'A 36-bit identifier uniquely identifying a cell within a PLMN, composed of the gNB ID (22–32 bits) and the Cell ID (4–14 bits). Part of the Cell Global Identity (CGI), used in NGAP, F1AP, and measurement reports.',
    related: ['gNB ID', 'PCI', 'PLMN', 'gNB', 'NGAP'],
    spec: '3GPP TS 38.413',
  },
  {
    id: 'ndi',
    term: 'NDI',
    fullName: 'New Data Indicator',
    type: 'Concept',
    layer: 'PHY / MAC',
    definition: 'A 1-bit field in DCI that toggles to indicate a new transport block (NDI toggled) vs. a retransmission of a previous one (NDI unchanged). Allows the UE/gNB to distinguish initial transmissions from retransmissions for HARQ soft-buffer management.',
    related: ['HARQ', 'DCI', 'TB', 'MAC'],
    spec: '3GPP TS 38.212',
  },
  {
    id: 'nea',
    term: 'NEA',
    fullName: 'NR Encryption Algorithm',
    type: 'Concept',
    layer: 'Security',
    definition: 'The ciphering algorithms for 5G NR: NEA0 (null/no ciphering), NEA1 (SNOW 3G), NEA2 (AES-CTR), NEA3 (ZUC). Applied at the PDCP layer for both RRC (SRBs) and user-plane (DRBs) ciphering.',
    related: ['NIA', 'AES', 'PDCP', 'Security'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'nef',
    term: 'NEF',
    fullName: 'Network Exposure Function',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5GC network function that securely exposes network capabilities and events to external applications and third parties. Acts as a gateway for Application Functions to interact with 5GC NFs (PCF, UDM, AMF) via northbound APIs, without direct SBI access.',
    related: ['PCF', 'UDM', 'AMF', 'SBA', 'SBI', 'NF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'nf',
    term: 'NF',
    fullName: 'Network Function',
    type: 'Component',
    layer: '5GC',
    definition: 'A functional building block of the 5G Core implementing a set of capabilities over the Service Based Architecture. NFs (AMF, SMF, UPF, AUSF, UDM, PCF, NRF, NSSF, NEF, SEPP) register with the NRF and discover each other via service-based interfaces.',
    related: ['AMF', 'SMF', 'UPF', 'NRF', 'SBA', 'SBI', 'NFV'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'nfv',
    term: 'NFV',
    fullName: 'Network Functions Virtualisation',
    type: 'Concept',
    layer: '5GC / NG-RAN',
    definition: 'An ETSI framework for virtualizing network functions that traditionally ran on proprietary hardware, deploying them as software on commodity servers (COTS). Enables cloud-native deployment of 5GC NFs and disaggregated gNB functions for flexible scaling.',
    related: ['SDN', 'NF', 'SBA', 'CU', 'DU'],
    spec: 'ETSI GS NFV 001',
  },
  {
    id: 'ng-c',
    term: 'NG-C',
    fullName: 'NG Control Plane Interface',
    type: 'Interface',
    layer: 'NG-RAN ↔ 5GC',
    definition: 'The control-plane component of the NG interface between gNB and 5GC, synonymous with the N2 interface. Carries NGAP signaling between gNB-CU-CP and AMF for UE registration, NAS transport, PDU session resource management, and handover.',
    related: ['N2', 'NGAP', 'NG-U', 'AMF', 'gNB-CU-CP'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'ng-u',
    term: 'NG-U',
    fullName: 'NG User Plane Interface',
    type: 'Interface',
    layer: 'NG-RAN ↔ 5GC',
    definition: 'The user-plane component of the NG interface between gNB and 5GC, synonymous with the N3 interface. Carries GTP-U tunnels between gNB-CU-UP and UPF for PDU session user data.',
    related: ['N3', 'GTP-U', 'NG-C', 'UPF', 'gNB-CU-UP'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'nia',
    term: 'NIA',
    fullName: 'NR Integrity Algorithm',
    type: 'Concept',
    layer: 'Security',
    definition: 'The integrity protection algorithms for 5G NR: NIA0 (null), NIA1 (SNOW 3G), NIA2 (AES-CMAC), NIA3 (ZUC). Applied at PDCP for RRC integrity (mandatory) and user-plane integrity (optional). Used to compute and verify MAC-I.',
    related: ['NEA', 'AES', 'MAC-I', 'PDCP', 'Security'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'nr',
    term: 'NR',
    fullName: 'New Radio',
    type: 'Concept',
    layer: 'PHY / RAN',
    definition: 'The 5G radio access technology standardized by 3GPP from Release 15 onward. Supports flexible numerology, massive MIMO, mmWave (FR2), and sub-6 GHz (FR1). Replaces LTE as the primary RAN technology in 5GS.',
    related: ['5GS', 'NG-RAN', 'gNB', 'FR1', 'FR2', 'eMBB', 'URLLC'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'nr-arfcn',
    term: 'NR-ARFCN',
    fullName: 'NR Absolute Radio Frequency Channel Number',
    type: 'Identifier',
    layer: 'PHY',
    definition: 'A channel numbering scheme mapping a carrier frequency to an integer value, covering the full 5G NR frequency range in increments of 5 kHz. Used in SIB/RRC configuration for cell and neighbor frequencies.',
    related: ['GSCN', 'FR1', 'FR2', 'Cell Search', 'SIB1'],
    spec: '3GPP TS 38.104',
  },
  {
    id: 'nrf',
    term: 'NRF',
    fullName: 'NF Repository Function',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5GC network function maintaining a repository of all registered NF instances and their capabilities. NFs register with the NRF on startup and discover other NFs via NRF queries, enabling service-based discovery in the SBA.',
    related: ['NF', 'SBA', 'SBI', 'AMF', 'SMF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'nssf',
    term: 'NSSF',
    fullName: 'Network Slice Selection Function',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5GC function selecting appropriate Network Slice Instance(s) for a UE based on requested S-NSSAI, subscription data, and operator policy. Assists the AMF in determining which AMF and SMF to use for a given network slice.',
    related: ['AMF', 'SMF', 'NF', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'ntn',
    term: 'NTN',
    fullName: 'Non-Terrestrial Network',
    type: 'Concept',
    layer: 'NG-RAN',
    definition: 'A network using spaceborne or airborne vehicles (LEO/MEO/GEO satellites, HAPS) as base stations or relays to extend 5G NR coverage to unserved areas. NTN introduces large propagation delays, Doppler shift, and wide coverage areas, addressed by 3GPP NR enhancements.',
    related: ['NR', 'NG-RAN', 'TA', 'PRACH'],
    spec: '3GPP TS 38.811',
  },
  {
    id: 'numerology',
    term: 'Numerology',
    fullName: 'Numerology (μ)',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A set of OFDM parameters defined by the subcarrier spacing (SCS): SCS = 15 × 2^μ kHz (μ = 0…4 for NR). Higher μ gives wider SCS, shorter symbol duration, and more slots per subframe — suitable for higher frequencies and lower latency. Numerology is configured per BWP.',
    related: ['BWP', 'Slot', 'Symbol', 'Frame', 'FR1', 'FR2', 'OFDM'],
    spec: '3GPP TS 38.211',
  },
  // ── O ────────────────────────────────────────────────────────────
  {
    id: 'o-ran',
    term: 'O-RAN',
    fullName: 'Open Radio Access Network',
    type: 'Concept',
    layer: 'NG-RAN',
    definition: 'An industry initiative (O-RAN Alliance) defining open, interoperable interfaces for disaggregated RAN. Introduces Open Fronthaul (CUS/M-plane between O-DU and O-RU), E2 interface (Near-RT RIC to E2 Nodes), O1 (management), and A1 (Non-RT RIC policy) interfaces.',
    related: ['E2', 'O1', 'Open Fronthaul', 'RIC', 'Split 7.2', 'C-RAN'],
    spec: 'O-RAN Alliance Specifications',
  },
  {
    id: 'o1',
    term: 'O1',
    fullName: 'O1 Interface',
    type: 'Interface',
    layer: 'O-RAN',
    definition: 'The O-RAN management interface between the SMO (Service Management and Orchestration) framework and O-RAN managed elements (O-CU, O-DU, O-RU, Near-RT RIC). Carries YANG-model-based configuration (NETCONF), performance management, and fault supervision.',
    related: ['O-RAN', 'E2', 'gNB-CU', 'gNB-DU', 'RIC'],
    spec: 'O-RAN WG10 O1 Specification',
  },
  {
    id: 'ofdm',
    term: 'OFDM',
    fullName: 'Orthogonal Frequency Division Multiplexing',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The multi-carrier modulation scheme used as the 5G NR air interface waveform. Data is modulated on multiple closely spaced orthogonal subcarriers. Provides robustness to multipath fading (via cyclic prefix) and enables flexible resource allocation in time (slots) and frequency (PRBs).',
    related: ['CP-OFDM', 'DFT-s-OFDM', 'OFDMA', 'numerology', 'CP (Cyclic Prefix)', 'PRB'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'ofdma',
    term: 'OFDMA',
    fullName: 'Orthogonal Frequency Division Multiple Access',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The multi-access extension of OFDM where different subsets of subcarriers (resource blocks) are assigned to different UEs simultaneously. Enables efficient multiplexing of many UEs in the time-frequency resource grid in both downlink and uplink.',
    related: ['OFDM', 'PRB', 'Resource Grid', 'Scheduling', 'DCI'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'open-fronthaul',
    term: 'Open Fronthaul',
    fullName: 'Open Fronthaul',
    type: 'Interface',
    layer: 'O-RAN',
    definition: 'The O-RAN Alliance specification for the fronthaul interface between O-DU and O-RU. Defined using eCPRI as transport, specifying C-plane (control), U-plane (user data), S-plane (synchronization), and M-plane (management) protocols. Supports Split 7.2x functional split.',
    related: ['eCPRI', 'CPRI', 'Split 7.2', 'Fronthaul', 'O-RAN'],
    spec: 'O-RAN WG4 CUS Plane Specification',
  },
  {
    id: 'osi',
    term: 'OSI',
    fullName: 'Other System Information',
    type: 'Concept',
    layer: 'RRC',
    definition: 'System information beyond Minimum System Information (MIB and SIB1). Includes SIB2–SIB9+. May be broadcast periodically or provided on-demand when a UE sends an RRCSystemInfoRequest. Scheduling is indicated in SIB1.',
    related: ['MSI', 'SIB', 'SIB1', 'SIB2', 'SIB3', 'BCCH'],
    spec: '3GPP TS 38.331',
  },
  // ── P (additional) ───────────────────────────────────────────────
  {
    id: 'p-polling',
    term: 'P (Polling)',
    fullName: 'Poll Bit',
    type: 'Concept',
    layer: 'RLC',
    definition: 'A 1-bit field in the RLC AM data PDU header. When set (P=1), it requests the receiver to send a STATUS PDU (ACK/NACK) for outstanding AM PDUs. Triggered by polling timer expiry, configured PDU/byte count thresholds, or an empty transmission buffer.',
    related: ['RLC AM', 'ARQ', 'ACK', 'NACK', 'CPT'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'paging',
    term: 'Paging',
    fullName: 'Paging',
    type: 'Procedure',
    layer: 'RRC / NAS',
    definition: 'A network-initiated procedure to alert a UE in CM-IDLE or RRC_INACTIVE of an incoming MT call or data. The AMF triggers paging toward the last known gNB, which broadcasts a Paging message at the UE\'s Paging Occasion. The UE responds with a Service Request.',
    related: ['PCCH', 'PCH', 'PO', 'PF', 'CM Idle', 'RRC_IDLE', 'AMF', 'MT'],
    spec: '3GPP TS 38.304',
  },
  {
    id: 'pcch',
    term: 'PCCH',
    fullName: 'Paging Control Channel',
    type: 'Channel',
    layer: 'MAC',
    definition: 'A downlink logical channel carrying paging messages and system information change notifications to UEs in idle/inactive state. Maps to the PCH transport channel, which is scheduled on PDSCH.',
    related: ['PCH', 'Paging', 'PO', 'PF', 'Logical Channels'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'pcf',
    term: 'PCF',
    fullName: 'Policy Control Function',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5GC function providing unified policy framework. Delivers PCC (Policy and Charging Control) rules to the SMF (N7) and access/mobility policies to the AMF (N15). Interfaces with UDR for policy data retrieval.',
    related: ['SMF', 'AMF', 'UDR', 'N7', 'N15', 'QoS', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'pch',
    term: 'PCH',
    fullName: 'Paging Channel',
    type: 'Channel',
    layer: 'MAC',
    definition: 'A downlink transport channel carrying paging messages from the PCCH logical channel. Mapped to PDSCH. Addressed with P-RNTI on PDCCH to alert all UEs monitoring a given Paging Occasion.',
    related: ['PCCH', 'Paging', 'DL SCH', 'PDSCH', 'Transport Channels'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'pci',
    term: 'PCI',
    fullName: 'Physical Cell Identity',
    type: 'Identifier',
    layer: 'PHY',
    definition: 'A numeric identifier (0–1007) for a 5G NR cell derived from PSS (NID2: 0, 1, or 2) and SSS (NID1: 0–335). PCI = 3 × NID1 + NID2. Used for cell detection, DMRS sequence generation, and neighbor cell differentiation.',
    related: ['PSS', 'SSS', 'NCI', 'Cell Search', 'Cell ID'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'pdcch',
    term: 'PDCCH',
    fullName: 'Physical Downlink Control Channel',
    type: 'Channel',
    layer: 'PHY',
    definition: 'The physical channel carrying DCI. Transmitted in CORESETs within configured Search Spaces. Carries scheduling assignments (DL, UL), TPC commands, and SFI. The UE blindly decodes PDCCH candidates at configured aggregation levels.',
    related: ['DCI', 'CORESET', 'SearchSpace', 'CCE', 'PDSCH', 'Physical Channels'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'pdr',
    term: 'PDR',
    fullName: 'Packet Detection Rule',
    type: 'Concept',
    layer: '5GC',
    definition: 'A rule provisioned by the SMF to the UPF via PFCP defining how to match incoming/outgoing packets to a PDU session QoS flow. Specifies match criteria (SDF filter, QFI), precedence, and associated Forwarding Action Rules (FAR) and QoS Enforcement Rules (QER).',
    related: ['PFCP', 'UPF', 'SMF', 'SDF', 'QoS Flow'],
    spec: '3GPP TS 29.244',
  },
  {
    id: 'pdu',
    term: 'PDU',
    fullName: 'Protocol Data Unit',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'A unit of data at a given protocol layer. Each layer adds headers/trailers to the SDU from above, forming a PDU passed to the layer below. For example: an RLC PDU = RLC header + PDCP PDU(s); a MAC PDU = MAC subheaders + RLC PDU(s).',
    related: ['SDU', 'MAC', 'RLC', 'PDCP', 'SDAP'],
    spec: '',
  },
  {
    id: 'pdu-session',
    term: 'PDU Session',
    fullName: 'PDU Session',
    type: 'Concept',
    layer: '5GC',
    definition: 'An end-to-end association between the UE and a Data Network (identified by DNN). Provides IP connectivity and may contain one or more QoS flows mapped to DRBs. Established, modified, and released via NAS 5GSM procedures. The SMF manages the session; UPF anchors the user plane.',
    related: ['PDU Session Establishment', 'DNN', 'QoS Flow', 'DRB', 'SMF', 'UPF', '5GSM'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'pdu-session-establishment',
    term: 'PDU Session Establishment',
    fullName: 'PDU Session Establishment',
    type: 'Procedure',
    layer: 'NAS / 5GC',
    definition: 'The procedure by which a UE requests and the network establishes a new PDU session. The UE sends a PDU Session Establishment Request (NAS SM) via the AMF to the SMF. The SMF selects a UPF, allocates a UE IP address, provisions QoS rules, and returns N2 SM information to set up the data path.',
    related: ['PDU Session', 'SMF', 'UPF', 'AMF', '5GSM', 'QoS Flow', 'DRB'],
    spec: '3GPP TS 23.502',
  },
  {
    id: 'pei',
    term: 'PEI',
    fullName: 'Permanent Equipment Identifier',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'The 5G equivalent of IMEI — a permanent identifier for the UE hardware platform. Can take the form of IMEI or IMEISV. Reported to the AMF during registration when requested; used for equipment checking against black/grey/white lists.',
    related: ['IMEI', 'ME', 'UE', 'AMF', 'Registration'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'per',
    term: 'PER',
    fullName: 'Packed Encoding Rules',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'An ASN.1 encoding standard used for compact binary encoding of protocol messages. In 5G NR, NGAP, F1AP, XnAP, E1AP, and E2AP messages are encoded using APER (Aligned PER), producing compact bit-aligned encodings for efficient transport.',
    related: ['NGAP', 'F1AP', 'E2AP', 'XnAP'],
    spec: 'ITU-T X.691',
  },
  {
    id: 'pf',
    term: 'PF',
    fullName: 'Paging Frame',
    type: 'Concept',
    layer: 'RRC',
    definition: 'The radio frame in which a UE\'s Paging Occasion (PO) occurs. Determined by the paging cycle and UE identity (5G-S-TMSI). The UE derives its PF from SIB1 paging configuration and monitors the corresponding PDCCH.',
    related: ['PO', 'Paging', 'SFN', 'PCCH', 'RNTI'],
    spec: '3GPP TS 38.304',
  },
  {
    id: 'pfcp',
    term: 'PFCP',
    fullName: 'Packet Forwarding Control Protocol',
    type: 'Protocol',
    layer: '5GC',
    definition: 'The protocol on the N4 interface between SMF and UPF. Used to establish, modify, and delete PFCP sessions corresponding to PDU sessions. The SMF provisions PDRs, FARs, and QoS Enforcement Rules (QERs) on the UPF via PFCP.',
    related: ['SMF', 'UPF', 'N4', 'PDR', 'CUPS'],
    spec: '3GPP TS 29.244',
  },
  {
    id: 'phr',
    term: 'PHR',
    fullName: 'Power Headroom Report',
    type: 'Concept',
    layer: 'MAC',
    definition: 'A MAC control element sent by the UE reporting the difference between its maximum transmit power and the estimated power required for the current PUSCH transmission. Enables the gNB scheduler to optimize UL resource allocation and MCS selection.',
    related: ['Power Headroom', 'Power Control', 'PUSCH', 'MAC', 'BSR'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'physical-channels',
    term: 'Physical Channels',
    fullName: 'Physical Channels',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The set of channels at the physical layer that carry information from higher protocol layers. Downlink: PBCH, PDCCH, PDSCH. Uplink: PRACH, PUCCH, PUSCH. Each physical channel has defined time-frequency resource mappings, reference signals, and coding/modulation rules.',
    related: ['Transport Channels', 'Logical Channels', 'PDSCH', 'PDCCH', 'PBCH', 'PUSCH', 'PUCCH', 'PRACH'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'physical-signals',
    term: 'Physical Signals',
    fullName: 'Physical Signals',
    type: 'Concept',
    layer: 'PHY',
    definition: 'Reference and synchronization signals at the physical layer that do not carry higher-layer information. Downlink: PSS, SSS, DMRS, CSI-RS, PTRS. Uplink: DMRS, SRS, PTRS. Used for synchronization, channel estimation, beamforming, and measurements.',
    related: ['PSS', 'SSS', 'DMRS', 'CSI-RS', 'SRS', 'Physical Channels'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'po',
    term: 'PO',
    fullName: 'Paging Occasion',
    type: 'Concept',
    layer: 'RRC',
    definition: 'A specific set of PDCCH monitoring symbols within a Paging Frame at which the UE listens for paging messages. Each UE has a unique PO derived from its 5G-S-TMSI, the paging cycle, and the number of active UEs.',
    related: ['PF', 'Paging', 'PCCH', 'PCH', 'RNTI'],
    spec: '3GPP TS 38.304',
  },
  {
    id: 'point-a',
    term: 'PointA',
    fullName: 'Point A',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A carrier-level frequency reference point from which CRBs and SSB subcarrier offset (K_SSB) are defined. Point A is the lowest subcarrier of CRB 0 for a given numerology, broadcast in SIB1 as absoluteFrequencyPointA (NR-ARFCN).',
    related: ['CRB', 'PRB', 'BWP', 'NR-ARFCN', 'Resource Grid', 'K_SSB'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'power-control',
    term: 'Power Control',
    fullName: 'Power Control',
    type: 'Procedure',
    layer: 'PHY / MAC',
    definition: 'The mechanism regulating UE transmit power for PUSCH, PUCCH, PRACH, and SRS. Combines open-loop control (path-loss based) with closed-loop adjustment (TPC commands in DCI). Minimizes inter-cell interference while maintaining required received SINR at the gNB.',
    related: ['TPC', 'PHR', 'Power Headroom', 'PUSCH', 'PUCCH', 'DCI'],
    spec: '3GPP TS 38.213',
  },
  {
    id: 'power-headroom',
    term: 'Power Headroom',
    fullName: 'Power Headroom',
    type: 'Concept',
    layer: 'MAC / PHY',
    definition: 'The margin between a UE\'s maximum transmit power (P_CMAX) and the power it currently requires for PUSCH. Reported via PHR MAC CE. Positive headroom indicates spare capacity; negative indicates the UE cannot achieve the desired power level.',
    related: ['PHR', 'Power Control', 'TPC', 'PUSCH', 'MAC'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'prb',
    term: 'PRB',
    fullName: 'Physical Resource Block',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The basic frequency-domain resource allocation unit: 12 consecutive subcarriers. Time-domain resources are allocated in slots. PRB count per BWP depends on channel bandwidth and numerology (e.g., 52 PRBs for 20 MHz at 15 kHz SCS).',
    related: ['RB', 'CRB', 'VRB', 'BWP', 'Resource Grid', 'numerology'],
    spec: '3GPP TS 38.211',
  },
  // ── Q (additional) ───────────────────────────────────────────────
  {
    id: 'qam',
    term: 'QAM',
    fullName: 'Quadrature Amplitude Modulation',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A modulation scheme combining amplitude and phase modulation. 5G NR supports 16-QAM (4 bits/symbol), 64-QAM (6 bits/symbol), and 256-QAM (8 bits/symbol) for PDSCH/PUSCH. Higher-order QAM gives higher spectral efficiency but requires better channel conditions (higher SNR).',
    related: ['QPSK', 'MCS', 'CQI', 'PDSCH', 'PUSCH'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'qos-flow',
    term: 'QoS Flow',
    fullName: 'QoS Flow',
    type: 'Concept',
    layer: '5GC / SDAP',
    definition: 'The finest granularity of QoS differentiation within a PDU session. Identified by a QFI and characterized by a 5QI, ARP, and optionally GBR parameters. QoS flows are mapped to DRBs by the SDAP layer in the gNB-CU-UP and UE.',
    related: ['QFI', '5QI', 'GBR', 'DRB', 'SDAP', 'PDU Session', 'SMF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'qpsk',
    term: 'QPSK',
    fullName: 'Quadrature Phase Shift Keying',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A phase modulation scheme encoding 2 bits per symbol across 4 phase states (0°, 90°, 180°, 270°). In 5G NR, QPSK is the minimum modulation order for PDSCH/PUSCH and is used for control channels (PDCCH, PBCH). Robust at low SNR.',
    related: ['QAM', 'MCS', 'CQI', 'PDCCH'],
    spec: '3GPP TS 38.211',
  },
  // ── R (additional) ───────────────────────────────────────────────
  {
    id: 'ra',
    term: 'RA',
    fullName: 'Random Access',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'The procedure by which a UE obtains uplink time synchronization and an identity (C-RNTI) from the gNB. Triggered on initial access, handover, re-establishment, SR failure, and DL data arrival in RRC_INACTIVE. Can be contention-based (CBRA) or contention-free (CFRA).',
    related: ['RACH', 'CBRA', 'CFRA', 'PRACH', 'RAR', 'C-RNTI'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'rac',
    term: 'RAC',
    fullName: 'Registration Area Code',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A code identifying a Registration Area — a group of tracking areas over which a UE in RRC_INACTIVE can move without triggering a RAN Notification Area Update. Configured by the gNB in RRCRelease for RNA-based location management.',
    related: ['RNA', 'TAC', 'RRC_INACTIVE', 'Registration'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'rand',
    term: 'RAND',
    fullName: 'Random Challenge',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A 128-bit random number generated by the ARPF/UDM as the challenge value in the AKA authentication procedure. Sent to the UE along with AUTN. The UE uses RAND with secret key K to compute RES*, CK, IK, and AK via the MILENAGE or TUAK algorithms.',
    related: ['AUTN', 'AV', 'AK', 'Authentication', 'USIM'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'random-access',
    term: 'Random Access',
    fullName: 'Random Access Procedure',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'The MAC-layer procedure for UE initial synchronization and resource acquisition. The 4-step CBRA procedure: Msg1 (PRACH preamble), Msg2 (RAR with TA and UL grant), Msg3 (RRC/NAS message via PUSCH), Msg4 (Contention Resolution). A 2-step CFRA variant uses a pre-assigned preamble.',
    related: ['RA', 'RACH', 'PRACH', 'RAR', 'CBRA', 'CFRA', 'Timing Advance Command'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'rapid',
    term: 'RAPID',
    fullName: 'Random Access Preamble Identifier',
    type: 'Identifier',
    layer: 'MAC',
    definition: 'A 6-bit identifier in the RAR MAC subheader matching the preamble index transmitted by the UE in Msg1. A UE identifies its RAR response by finding the RAPID matching the preamble it transmitted.',
    related: ['RAR', 'RACH', 'PRACH', 'CBRA'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'rb',
    term: 'RB',
    fullName: 'Resource Block',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The unit of frequency-domain resource allocation: 12 consecutive subcarriers. PRBs, CRBs, and VRBs are all 12-subcarrier wide but differ in reference point. Scheduling grants specify RB start positions and lengths via RIV or bitmap in DCI.',
    related: ['PRB', 'CRB', 'VRB', 'RBG', 'Resource Grid', 'DCI'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'rbg',
    term: 'RBG',
    fullName: 'Resource Block Group',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A group of consecutive PRBs used as the unit of Type 0 resource allocation. RBG size (P) depends on BWP bandwidth. A bitmap in DCI indicates which RBGs are allocated, enabling non-contiguous allocation for frequency-selective scheduling.',
    related: ['PRB', 'RB', 'Resource Allocation', 'DCI', 'Scheduling'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'reg',
    term: 'REG',
    fullName: 'Resource Element Group',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A group of 12 resource elements (1 PRB × 1 OFDM symbol) used as the building block for PDCCH mapping. Six REGs form one CCE. REGs carry PDCCH DMRS and DCI bits according to the interleaving pattern configured in the CORESET.',
    related: ['CCE', 'CORESET', 'PDCCH', 'PRB', 'Symbol'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'registration',
    term: 'Registration',
    fullName: 'Registration Procedure',
    type: 'Procedure',
    layer: 'NAS',
    definition: 'The NAS procedure by which a UE attaches to the 5GC, obtains a 5G-GUTI, establishes security, and optionally provisions PDU sessions. Types: Initial Registration (first attach), Mobility Registration Update (TAU equivalent), Periodic Registration Update.',
    related: ['AMF', '5G-GUTI', 'SUCI', 'Authentication', 'RM Registered', 'Initial Access'],
    spec: '3GPP TS 23.502',
  },
  {
    id: 'res',
    term: 'RES*',
    fullName: 'Extended Authentication Response',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'The extended authentication response computed by the UE during 5G-AKA. Derived from RAND, secret key K, and SQN using the f2 algorithm, then extended to 128 bits via KDF. The UE sends RES* to the AMF, which hashes it (HRES*) for local verification.',
    related: ['XRES*', 'HRES*', 'RAND', 'Authentication', '5G-AKA'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'reselection',
    term: 'Reselection',
    fullName: 'Cell Reselection',
    type: 'Procedure',
    layer: 'RRC',
    definition: 'The autonomous procedure by which a UE in idle or inactive mode changes its serving cell based on measurement criteria. Triggered when a neighbor cell meets the R-criterion. Configured via SIB2–SIB5.',
    related: ['Cell Selection', 'SIB2', 'SIB3', 'RSRP', 'RSRQ', 'RNA', 'Idle Mode'],
    spec: '3GPP TS 38.304',
  },
  {
    id: 'resource-allocation',
    term: 'Resource Allocation',
    fullName: 'Resource Allocation',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The method by which the gNB assigns time-frequency resources to a UE for PDSCH/PUSCH. Three types: Type 0 (RBG bitmap, non-contiguous), Type 1 (start RB + length via RIV, contiguous), and Type 2 (VRB-to-PRB interleaving). Signaled in DCI.',
    related: ['RBG', 'RIV', 'VRB', 'PRB', 'DCI', 'Scheduling'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'resource-grid',
    term: 'Resource Grid',
    fullName: 'Resource Grid',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The two-dimensional time-frequency grid in 5G NR. The frequency axis is divided into subcarriers (12 per PRB); the time axis into OFDM symbols. One Resource Element (RE) = one subcarrier × one symbol. Spans one antenna port and one numerology.',
    related: ['PRB', 'Symbol', 'numerology', 'BWP', 'PointA', 'OFDMA'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'ric',
    term: 'RIC',
    fullName: 'RAN Intelligent Controller',
    type: 'Component',
    layer: 'O-RAN',
    definition: 'An O-RAN component providing intelligent, programmable control of the RAN. Two types: Non-RT RIC (>1 s control loop, in SMO, hosts rApps via A1) and Near-RT RIC (10 ms–1 s loop, hosts xApps, connects to E2 Nodes via E2 interface). Enables AI/ML-driven RAN optimization.',
    related: ['E2', 'O-RAN', 'O1', 'gNB-CU-CP', 'gNB-DU'],
    spec: 'O-RAN Alliance Specifications',
  },
  {
    id: 'riv',
    term: 'RIV',
    fullName: 'Resource Indicator Value',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A compact encoding of the starting PRB (RBstart) and contiguous length (LRBs) for Type 1 resource allocation in DCI. If (LRBs−1) ≤ ⌊N_BWP/2⌋: RIV = N_BWP × (LRBs−1) + RBstart; otherwise: RIV = N_BWP × (N_BWP−LRBs+1) + (N_BWP−1−RBstart).',
    related: ['Resource Allocation', 'SLIV', 'DCI', 'PRB', 'BWP'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'rlc-am',
    term: 'RLC AM',
    fullName: 'RLC Acknowledged Mode',
    type: 'Protocol',
    layer: 'RLC',
    definition: 'The RLC mode providing reliable delivery via ARQ. The transmitter retransmits NACKed PDUs identified by STATUS PDUs from the receiver. Used for SRBs (always) and DRBs requiring guaranteed delivery.',
    related: ['RLC', 'RLC UM', 'RLC TM', 'ARQ', 'NACK'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'rlc-tm',
    term: 'RLC TM',
    fullName: 'RLC Transparent Mode',
    type: 'Protocol',
    layer: 'RLC',
    definition: 'The simplest RLC mode with no headers, no segmentation, and no retransmission. PDUs are passed through without modification. Used for BCCH (system information) and CCCH (initial RRC signaling before C-RNTI assignment).',
    related: ['RLC', 'RLC AM', 'RLC UM', 'BCCH', 'CCCH'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'rlc-um',
    term: 'RLC UM',
    fullName: 'RLC Unacknowledged Mode',
    type: 'Protocol',
    layer: 'RLC',
    definition: 'An RLC mode providing in-sequence delivery and duplicate detection without ARQ retransmission. Used for delay-sensitive DRBs (e.g., voice/streaming) where retransmission would add unacceptable latency.',
    related: ['RLC', 'RLC AM', 'RLC TM', 'DRB', 'DTCH'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'rmsi',
    term: 'RMSI',
    fullName: 'Remaining Minimum System Information',
    type: 'Concept',
    layer: 'RRC',
    definition: 'An earlier term for SIB1 used in pre-Release 15 5G NR specifications. In current 3GPP specifications this is referred to simply as SIB1.',
    related: ['SIB1', 'MSI', 'MIB'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'rna',
    term: 'RNA',
    fullName: 'RAN-based Notification Area',
    type: 'Concept',
    layer: 'RRC',
    definition: 'A set of cells configured by the network within which a UE in RRC_INACTIVE can move without triggering a RAN Notification Area Update. Configured in RRCRelease and identified by an RNA ID.',
    related: ['RRC_INACTIVE', 'Reselection', 'TAC', 'Registration'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'rqi',
    term: 'RQI',
    fullName: 'Reflective QoS Indication',
    type: 'Concept',
    layer: 'SDAP',
    definition: 'A 1-bit flag in the SDAP DL data PDU header. When set by the gNB, it instructs the UE to derive its UL QoS flow-to-DRB mapping by reflecting (mirroring) the DL QFI for packets with the same 5-tuple, simplifying UL QoS rule provisioning.',
    related: ['SDAP', 'QFI', 'QoS Flow', 'RRC'],
    spec: '3GPP TS 37.324',
  },
  {
    id: 'rrh',
    term: 'RRH',
    fullName: 'Remote Radio Head',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'A remote radio unit deployed close to the antenna handling RF amplification, filtering, and analog/digital conversion. Connected to the BBU via a fronthaul link (CPRI or eCPRI). Performs limited baseband processing; bulk PHY/MAC runs in the centralized BBU.',
    related: ['BBU', 'RRU', 'C-RAN', 'Fronthaul', 'CPRI', 'eCPRI'],
    spec: '',
  },
  {
    id: 'rru',
    term: 'RRU',
    fullName: 'Remote Radio Unit',
    type: 'Component',
    layer: 'NG-RAN',
    definition: 'A term often used interchangeably with RRH. A remote radio unit deployed at the antenna site connected to the baseband (BBU/DU) via a fronthaul interface. In O-RAN terminology, the RRU is called the O-RU.',
    related: ['RRH', 'BBU', 'C-RAN', 'Fronthaul', 'O-RAN'],
    spec: '',
  },
  {
    id: 'rsrp',
    term: 'RSRP',
    fullName: 'Reference Signal Received Power',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The average received power measured on resource elements carrying SSB DMRS (SS-RSRP) or CSI-RS (CSI-RSRP). Reported in dBm. Used for cell selection, reselection, and Measurement Reports for handover.',
    related: ['RSRQ', 'SINR', 'SSB', 'CSI-RS', 'Measurement Report', 'Cell Selection'],
    spec: '3GPP TS 38.215',
  },
  {
    id: 'rsrq',
    term: 'RSRQ',
    fullName: 'Reference Signal Received Quality',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A measure of signal quality defined as N × RSRP / RSSI (N = number of RBs, RSSI = total received power). Reported in dB. Accounts for interference and noise, providing a fuller picture than RSRP alone for cell reselection and handover decisions.',
    related: ['RSRP', 'SINR', 'Measurement Report', 'Cell Selection'],
    spec: '3GPP TS 38.215',
  },
  // ── S (additional) ───────────────────────────────────────────────
  {
    id: 's-tmsi',
    term: 'S-TMSI',
    fullName: 'Short Temporary Mobile Subscriber Identity',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'In 5G NR, equivalent to 5G-S-TMSI. A shortened form of the 5G-GUTI used in paging and RRC. Composed of AMF Set ID (10 bits) + AMF Pointer (6 bits) + 5G-TMSI (32 bits).',
    related: ['5G-s-TMSI', '5G-TMSI', '5G-GUTI', 'Paging', 'AMF'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'sba',
    term: 'SBA',
    fullName: 'Service Based Architecture',
    type: 'Concept',
    layer: '5GC',
    definition: 'The 5GC architectural paradigm where NFs expose and consume services via standardized RESTful APIs over HTTP/2. Each NF offers services registered in the NRF. NFs discover and interact without point-to-point coupling, enabling modular, cloud-native deployment.',
    related: ['SBI', 'NRF', 'HTTP/2', 'NF', 'AMF', 'SMF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'sbi',
    term: 'SBI',
    fullName: 'Service Based Interface',
    type: 'Interface',
    layer: '5GC',
    definition: 'The common interface bus through which 5GC NFs communicate in the Service Based Architecture. Each NF exposes service-based APIs (e.g., Namf, Nsmf, Nudm) over HTTP/2 and TLS. Enables any authorized NF to consume any other NF\'s services.',
    related: ['SBA', 'HTTP/2', 'TLS', 'NRF', 'NF', 'AMF', 'SMF'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'sc',
    term: 'SC',
    fullName: 'Subcarrier',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The individual frequency-domain carrier within an OFDM waveform. In 5G NR, subcarriers are spaced at 15 × 2^μ kHz (where μ is the numerology). Twelve consecutive subcarriers form one Resource Block (PRB). Each subcarrier carries one modulation symbol per OFDM symbol period.',
    related: ['numerology', 'PRB', 'Resource Grid', 'OFDM', 'BWP'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'scheduling',
    term: 'Scheduling',
    fullName: 'MAC Scheduling',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'The gNB MAC function determining which UEs are allocated radio resources (PRBs) in each slot, the MCS, and the number of HARQ processes. Considers CQI, BSR, QoS priority, and DRX state. Dynamic scheduling (per-slot DCI) is complemented by SPS.',
    related: ['DCI', 'BSR', 'CQI', 'PDCCH', 'SPS', 'LCP', 'HARQ'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'sctp',
    term: 'SCTP',
    fullName: 'Stream Control Transmission Protocol',
    type: 'Protocol',
    layer: 'Transport',
    definition: 'A reliable transport protocol carrying NGAP, F1AP, XnAP, E1AP, and other 3GPP application protocols. Supports multi-homing, multi-streaming, and message boundary preservation. Provides superior reliability for signaling compared to TCP.',
    related: ['NGAP', 'F1AP', 'XnAP', 'TCP'],
    spec: 'IETF RFC 4960',
  },
  {
    id: 'sdf',
    term: 'SDF',
    fullName: 'Service Data Flow',
    type: 'Concept',
    layer: '5GC',
    definition: 'An aggregate of packet flows between a UE and an application server, characterized by a 5-tuple (source/destination IP, ports, protocol). Matched by SDF filters in UPF PDRs and mapped to QoS flows by SMF/PCF policy.',
    related: ['PDR', 'QoS Flow', 'UPF', 'SMF', 'PCF'],
    spec: '3GPP TS 29.244',
  },
  {
    id: 'sdn',
    term: 'SDN',
    fullName: 'Software Defined Networking',
    type: 'Concept',
    layer: '5GC / NG-RAN',
    definition: 'An architectural approach separating the network control plane from the data forwarding plane. In 5G, SDN principles underpin CUPS (separating SMF from UPF) and the O-RAN RIC architecture, enabling centralized, programmable network control.',
    related: ['NFV', 'CUPS', 'RIC', 'SMF', 'UPF'],
    spec: '',
  },
  {
    id: 'sdu',
    term: 'SDU',
    fullName: 'Service Data Unit',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'A unit of data received from a higher protocol layer. An SDU at one layer becomes the payload of a PDU at the layer below after header encapsulation. For example, a PDCP SDU is an IP packet; the PDCP PDU = PDCP header + PDCP SDU.',
    related: ['PDU', 'MAC', 'RLC', 'PDCP', 'SDAP'],
    spec: '',
  },
  {
    id: 'seaf',
    term: 'SEAF',
    fullName: 'Security Anchor Function',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5GC function co-located with the AMF in the serving network. Receives KSEAF from the AUSF after successful authentication and derives KAMF. Acts as the serving-network anchor for the UE security context.',
    related: ['AMF', 'AUSF', 'KSEAF', 'KAMF', 'Authentication', 'Security'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'search-space',
    term: 'SearchSpace',
    fullName: 'Search Space',
    type: 'Concept',
    layer: 'PHY / MAC',
    definition: 'A set of time-frequency occasions within a CORESET at which the UE monitors PDCCH candidates. Up to 10 Search Spaces can be configured per BWP. Each specifies a CORESET, aggregation levels, number of candidates, periodicity, and DCI formats to blindly decode.',
    related: ['CORESET', 'PDCCH', 'CCE', 'DCI', 'BWP'],
    spec: '3GPP TS 38.213',
  },
  {
    id: 'security',
    term: 'Security',
    fullName: '5G Security',
    type: 'Concept',
    layer: 'NAS / RRC',
    definition: 'The set of mechanisms protecting the 5G system: (1) Authentication — 5G-AKA or EAP-AKA\' to verify UE and network; (2) NAS security — ciphering and integrity of NAS messages; (3) AS security — RRC and UP integrity/ciphering at the gNB; (4) SUPI concealment via SUCI.',
    related: ['Authentication', 'NEA', 'NIA', 'MAC-I', 'PDCP', 'NAS', 'KAMF', 'KgNB'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'sepp',
    term: 'SEPP',
    fullName: 'Security Edge Protection Proxy',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5GC function providing security protection for inter-PLMN SBI signaling. Acts as a proxy at the network edge, applying message filtering, topology hiding, and TLS/IPSec for roaming traffic over the N32 interface.',
    related: ['SBI', 'PLMN', 'NF', 'IPSec', 'TLS'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'sfi',
    term: 'SFI',
    fullName: 'Slot Format Indicator',
    type: 'Concept',
    layer: 'PHY / MAC',
    definition: 'A DCI (format 2_0) mechanism allowing the gNB to dynamically indicate TDD slot formats (DL, UL, flexible symbol allocation) to a group of UEs. Enables fast UL/DL switching within a TDD pattern for dynamic traffic.',
    related: ['TDD', 'Slot', 'DCI', 'GP', 'Frame'],
    spec: '3GPP TS 38.213',
  },
  {
    id: 'sfn',
    term: 'SFN',
    fullName: 'System Frame Number',
    type: 'Identifier',
    layer: 'PHY / RRC',
    definition: 'A 10-bit counter (0–1023) identifying the radio frame. Increments every 10 ms, cycling every 10.24 s. The 6 MSBs are broadcast in the MIB via PBCH; the 4 LSBs are derived from the PBCH DMRS sequence index. Used for timing reference and paging offset calculation.',
    related: ['Frame', 'MIB', 'PBCH', 'HRF', 'PF'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'si',
    term: 'SI',
    fullName: 'Segment Indication',
    type: 'Concept',
    layer: 'RLC',
    definition: 'A 2-bit field in the RLC AM and UM data PDU header indicating the segmentation status of the PDU: 00 = complete SDU (no segmentation), 01 = first segment, 10 = last segment, 11 = middle segment. Allows the receiver to identify and reassemble segmented RLC SDUs.',
    related: ['SO', 'SN', 'RLC AM', 'RLC UM', 'PDU'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'sib',
    term: 'SIB',
    fullName: 'System Information Block',
    type: 'Concept',
    layer: 'RRC',
    definition: 'A container within System Information carrying specific categories of configuration data. SIB1 is always broadcast; SIB2–SIBn contain idle mode parameters, cell reselection configuration, neighbor lists, and other optional parameters.',
    related: ['SI', 'SIB1', 'SIB2', 'SIB3', 'SIB4', 'SIB5', 'MSI', 'OSI', 'MIB'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'sib2',
    term: 'SIB2',
    fullName: 'System Information Block Type 2',
    type: 'Concept',
    layer: 'RRC',
    definition: 'Contains cell reselection parameters for intra-frequency NR neighbor cells, including s-NonIntraSearchP/Q thresholds, t-ReselectionNR, and speed-dependent scaling parameters.',
    related: ['SIB', 'SIB3', 'Reselection', 'RSRP', 'Cell Selection'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'sib3',
    term: 'SIB3',
    fullName: 'System Information Block Type 3',
    type: 'Concept',
    layer: 'RRC',
    definition: 'Contains a list of intra-frequency NR neighbor cells with cell-specific reselection parameters (cell individual offset), allowing the gNB to bias reselection toward or away from specific cells.',
    related: ['SIB', 'SIB2', 'SIB4', 'Reselection', 'NR-ARFCN'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'sib4',
    term: 'SIB4',
    fullName: 'System Information Block Type 4',
    type: 'Concept',
    layer: 'RRC',
    definition: 'Contains inter-frequency NR neighbor cell configuration including carrier frequencies, reselection thresholds, and cell individual offsets for inter-frequency NR neighbors.',
    related: ['SIB', 'SIB3', 'SIB5', 'Reselection', 'NR-ARFCN'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'sib5',
    term: 'SIB5',
    fullName: 'System Information Block Type 5',
    type: 'Concept',
    layer: 'RRC',
    definition: 'Contains inter-RAT neighbor cell configuration for LTE E-UTRA cells, enabling idle mode reselection from NR to LTE. Includes EUTRA carrier frequencies, thresholds, and reselection priorities.',
    related: ['SIB', 'SIB4', 'Reselection'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'sidf',
    term: 'SIDF',
    fullName: 'Subscription Identifier De-concealing Function',
    type: 'Component',
    layer: '5GC',
    definition: 'A function co-located with the UDM/ARPF in the home network. Decrypts the SUCI to recover the SUPI using the home network\'s private key (ECIES). Ensures the subscriber permanent identity is only revealed in the home network.',
    related: ['SUCI', 'SUPI', 'UDM', 'ECIES'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'sinr',
    term: 'SINR',
    fullName: 'Signal to Interference plus Noise Ratio',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A measure of signal quality: the ratio of received signal power to the sum of interference from other cells/users plus thermal noise. A key metric for link adaptation, scheduling, and mobility. Higher SINR allows higher MCS and more spatial layers.',
    related: ['RSRP', 'RSRQ', 'MCS', 'CQI', 'Measurement Report'],
    spec: '3GPP TS 38.215',
  },
  {
    id: 'sip',
    term: 'SIP',
    fullName: 'Session Initiation Protocol',
    type: 'Protocol',
    layer: 'Application',
    definition: 'An application-layer signaling protocol for establishing, modifying, and terminating multimedia sessions (VoIP, video, messaging). Used by IMS over the 5G PDU session. SIP messages are handled by P-CSCF and S-CSCF in the IMS core.',
    related: ['IMS', 'VoNR', 'PDU Session'],
    spec: 'IETF RFC 3261',
  },
  {
    id: 'sliv',
    term: 'SLIV',
    fullName: 'Start and Length Indicator Value',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A compact encoding of the starting OFDM symbol (S) and number of consecutive symbols (L) for PDSCH/PUSCH time-domain resource allocation in DCI. Analogous to RIV but for the time domain.',
    related: ['RIV', 'DCI', 'PDSCH', 'PUSCH', 'Symbol'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'slot',
    term: 'Slot',
    fullName: 'Slot',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The basic scheduling unit in 5G NR, consisting of 14 OFDM symbols (normal CP). Duration depends on numerology: 1 ms (15 kHz), 0.5 ms (30 kHz), 0.25 ms (60 kHz), 0.125 ms (120 kHz). In TDD, slots are configured as all-DL, all-UL, or mixed.',
    related: ['Symbol', 'Frame', 'numerology', 'SFI', 'TDD'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'smtc',
    term: 'SMTC',
    fullName: 'SSB-based Measurement Timing Configuration',
    type: 'Concept',
    layer: 'RRC',
    definition: 'An RRC-configured time window specifying when the UE measures SSBs of neighbor cells. Defines a periodicity (5/10/20/40/80/160 ms), duration (1–5 ms), and offset to align UE measurement activity with neighbor SSB transmission periods.',
    related: ['SSB', 'CSI-RS', 'Measurement Report', 'RSRP', 'RRC'],
    spec: '3GPP TS 38.331',
  },
  {
    id: 'sn',
    term: 'SN',
    fullName: 'Sequence Number',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'A counter field in RLC and PDCP PDU headers enabling reordering, duplicate detection, and retransmission. RLC SN sizes: 6 or 12 bits (AM/UM). PDCP SN sizes: 12 or 18 bits. Also used as abbreviation for "Secondary Node" in Dual Connectivity.',
    related: ['RLC AM', 'RLC UM', 'PDCP', 'ARQ', 'SO'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'snn',
    term: 'SNN',
    fullName: 'Serving Network Name',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A text string identifying the serving network, used as KDF input during 5G authentication. Format: "5G:mnc<MNC>.mcc<MCC>.3gppnetwork.org". Prevents attacks where a compromised serving network reuses home-network authentication vectors.',
    related: ['Authentication', 'KDF', 'KAMF', 'KSEAF', 'PLMN'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'so',
    term: 'SO',
    fullName: 'Segment Offset',
    type: 'Concept',
    layer: 'RLC',
    definition: 'A field in the RLC AM and UM PDU headers (when segmentation is applied) indicating the byte offset of the first byte of the current segment within the original SDU. Enables the receiver to reassemble segmented SDUs.',
    related: ['SN', 'RLC AM', 'RLC UM', 'PDU'],
    spec: '3GPP TS 38.322',
  },
  {
    id: 'split-7-2',
    term: 'Split 7.2',
    fullName: 'Functional Split Option 7.2',
    type: 'Concept',
    layer: 'NG-RAN',
    definition: 'A RAN functional split point dividing the PHY layer between O-DU (upper PHY: coding, scrambling, modulation mapping) and O-RU (lower PHY: IFFT, CP addition, precoding). Split 7.2x is the O-RAN standard choice for the Open Fronthaul, carried over eCPRI.',
    related: ['Open Fronthaul', 'eCPRI', 'CPRI', 'O-RAN', 'Fronthaul', 'BBU'],
    spec: 'O-RAN WG4 CUS Plane Specification',
  },
  {
    id: 'sps',
    term: 'SPS',
    fullName: 'Semi-Persistent Scheduling',
    type: 'Procedure',
    layer: 'MAC',
    definition: 'A resource allocation scheme where the gNB pre-allocates periodic radio resources via RRC configuration. After activation by DCI, the UE uses allocated resources periodically without per-slot DCI grants. Reduces PDCCH overhead for periodic traffic like VoNR.',
    related: ['DCI', 'VoNR', 'Scheduling', 'PDCCH', 'MAC'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'sqn',
    term: 'SQN',
    fullName: 'Sequence Number (Authentication)',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A 48-bit counter maintained in both the USIM and the ARPF/UDM, incremented with each AKA authentication. Concealed by AK within the AUTN token. The UE checks the received SQN is within an acceptable window to prevent replay attacks.',
    related: ['AK', 'AUTN', 'Authentication', 'USIM', 'ARPF'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'sres',
    term: 'SRES',
    fullName: 'Authentication Response (2G/3G)',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'The authentication response in 2G/3G AKA procedures, computed by the USIM using key K and RAND. In 5G NR, replaced by RES*. May appear in 5G inter-working scenarios involving legacy network access.',
    related: ['RES*', 'RAND', 'Authentication', 'USIM', 'AV'],
    spec: '3GPP TS 33.102',
  },
  {
    id: 'srs',
    term: 'SRS',
    fullName: 'Sounding Reference Signal',
    type: 'Signal',
    layer: 'PHY',
    definition: 'An uplink reference signal transmitted by the UE on configured PUSCH resources, allowing the gNB to estimate uplink channel quality across the BWP bandwidth. Used for UL link adaptation, UL scheduling, and reciprocity-based DL beamforming in TDD.',
    related: ['DMRS', 'CSI-RS', 'PUSCH', 'TDD', 'Physical Signals'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'ssc',
    term: 'SSC',
    fullName: 'Session and Service Continuity',
    type: 'Concept',
    layer: '5GC',
    definition: 'A mode defining how a PDU session behaves during UE mobility. SSC Mode 1: UPF anchor and UE IP remain fixed. Mode 2: session released and re-established at a new UPF. Mode 3: multi-homed routing while maintaining connectivity.',
    related: ['PDU Session', 'UPF', 'SMF', 'Handover', 'DNN'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'supi',
    term: 'SUPI',
    fullName: 'Subscription Permanent Identifier',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'The permanent subscriber identity in 5G, equivalent to IMSI in legacy systems. Can be IMSI format (MCC+MNC+MSIN) or NAI format. Never sent in clear text over the air — always concealed within a SUCI using ECIES.',
    related: ['SUCI', '5G-GUTI', 'SIDF', 'USIM'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'symbol',
    term: 'Symbol',
    fullName: 'OFDM Symbol',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The fundamental time-domain unit in 5G NR. Each OFDM symbol carries modulated data on all subcarriers simultaneously. Symbol duration = 1/SCS plus the cyclic prefix (e.g., ~71.4 µs at 15 kHz). A normal CP slot contains 14 symbols.',
    related: ['Slot', 'CP (Cyclic Prefix)', 'numerology', 'Frame', 'Resource Grid'],
    spec: '3GPP TS 38.211',
  },
  {
    id: 'sync-signals',
    term: 'Sync Signals',
    fullName: 'Synchronization Signals',
    type: 'Signal',
    layer: 'PHY',
    definition: 'Refers collectively to PSS and SSS within the SSB. Together they provide slot/frame timing, Physical Cell ID (NID1 + NID2), and the starting point for MIB decoding during cell search.',
    related: ['PSS', 'SSS', 'SSB', 'Cell Search', 'PCI'],
    spec: '3GPP TS 38.211',
  },
  // ── T (additional) ───────────────────────────────────────────────
  {
    id: 'ta',
    term: 'TA',
    fullName: 'Timing Advance',
    type: 'Concept',
    layer: 'MAC / PHY',
    definition: 'The uplink timing offset applied by the UE to compensate for propagation delay, ensuring UL transmissions arrive at the gNB aligned with the DL frame timing. Critical for OFDMA orthogonality — misaligned UL transmissions cause inter-user interference.',
    related: ['Timing Advance Command', 'RACH', 'RAR', 'MAC', 'GP'],
    spec: '3GPP TS 38.213',
  },
  {
    id: 'tac',
    term: 'TAC',
    fullName: 'Tracking Area Code',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'A 24-bit code identifying a Tracking Area — an administrative area grouping cells for paging and location management. Broadcast in SIB1. When a UE moves to a cell with a TAC not in its Tracking Area List, it performs a Mobility Registration Update.',
    related: ['PLMN', 'Registration', 'Paging', 'SIB1', 'AMF'],
    spec: '3GPP TS 23.003',
  },
  {
    id: 'tb',
    term: 'TB',
    fullName: 'Transport Block',
    type: 'Concept',
    layer: 'MAC / PHY',
    definition: 'The basic unit of data delivery between MAC and PHY layers. One or two TBs (two codewords in MIMO) are delivered per TTI. Each TB is independently channel-coded (CRC + LDPC) and protected by HARQ. TB size is determined by MCS and allocated PRBs.',
    related: ['TBS', 'MCS', 'HARQ', 'CB', 'CRC', 'Transport Channels'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'tbs',
    term: 'TBS',
    fullName: 'Transport Block Size',
    type: 'Concept',
    layer: 'PHY',
    definition: 'The size in bits of a Transport Block, determined by the MCS index, number of allocated PRBs, OFDM symbols, and DMRS overhead. Computed using the TBS table and procedure in TS 38.214. TBS determines the data throughput per TTI.',
    related: ['TB', 'MCS', 'PRB', 'Slot', 'HARQ'],
    spec: '3GPP TS 38.214',
  },
  {
    id: 'tdd',
    term: 'TDD',
    fullName: 'Time Division Duplex',
    type: 'Concept',
    layer: 'PHY',
    definition: 'A duplex scheme using the same frequency for both uplink and downlink, separated in time. TDD slots are configured via SIB1 (static), RRC (semi-static), or DCI SFI (dynamic). A guard period separates DL and UL symbols. Used in unpaired spectrum.',
    related: ['FDD', 'SFI', 'Slot', 'Frame', 'GP', 'numerology'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'teid',
    term: 'TEID',
    fullName: 'Tunnel Endpoint Identifier',
    type: 'Identifier',
    layer: 'Transport',
    definition: 'A 32-bit identifier in GTP-U headers identifying the tunnel endpoint at the receiving node. Each node allocates a local TEID per tunnel (per PDU session). Exchanged during PDU session establishment via NGAP N2 SM information and N4 PFCP.',
    related: ['GTP-U', 'N3', 'F1-U', 'N9', 'PDU Session', 'UPF'],
    spec: '3GPP TS 29.281',
  },
  {
    id: 'timing-advance-command',
    term: 'Timing Advance Command',
    fullName: 'Timing Advance Command',
    type: 'Concept',
    layer: 'MAC',
    definition: 'A MAC CE sent by the gNB in the RAR (Msg2) or as a standalone TA Command MAC CE to adjust the UE\'s UL transmission timing. The 11-bit TA index encodes the required timing advance in multiples of 16 × Tc.',
    related: ['TA', 'RACH', 'RAR', 'MAC'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'tls',
    term: 'TLS',
    fullName: 'Transport Layer Security',
    type: 'Protocol',
    layer: 'Transport',
    definition: 'A cryptographic protocol providing authentication and encryption for network communications. In 5G, TLS (1.2 or 1.3) secures SBI interfaces between 5GC NFs and protects the N32 inter-PLMN interface. Provides certificate-based mutual authentication.',
    related: ['HTTP/2', 'SBI', 'SBA', 'IPSec', 'SEPP'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'transport-channels',
    term: 'Transport Channels',
    fullName: 'Transport Channels',
    type: 'Concept',
    layer: 'MAC',
    definition: 'Channels at the MAC/PHY interface defined by how data is transferred. DL: BCH, PCH, DL-SCH. UL: RACH, UL-SCH. Each transport channel maps to a physical channel and uses specific coding, interleaving, and resource mapping.',
    related: ['Logical Channels', 'Physical Channels', 'BCH', 'PCH', 'DL SCH', 'UL SCH', 'RACH'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'tti',
    term: 'TTI',
    fullName: 'Transmission Time Interval',
    type: 'Concept',
    layer: 'PHY / MAC',
    definition: 'The time duration of one HARQ transmission unit. In 5G NR, the TTI is one slot (14 symbols) by default; mini-slot (2, 4, or 7 symbols) scheduling is supported for URLLC, reducing HARQ round-trip time.',
    related: ['Slot', 'HARQ', 'numerology', 'URLLC', 'TB'],
    spec: '3GPP TS 38.211',
  },
  // ── U (additional) ───────────────────────────────────────────────
  {
    id: 'uci',
    term: 'UCI',
    fullName: 'Uplink Control Information',
    type: 'Concept',
    layer: 'PHY / MAC',
    definition: 'Control information transmitted by the UE in the uplink: HARQ ACK/NACK feedback, CSI reports (CQI, PMI, RI, CRI), and Scheduling Requests. Carried on PUCCH (standalone) or multiplexed onto PUSCH when UL data is available.',
    related: ['PUCCH', 'PUSCH', 'HARQ', 'CQI', 'SR'],
    spec: '3GPP TS 38.212',
  },
  {
    id: 'udm',
    term: 'UDM',
    fullName: 'Unified Data Management',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5GC function managing subscriber data and credentials. Co-located with ARPF (authentication) and SIDF (SUCI de-concealment). Provides subscription data to AMF (N8) and SMF (N10), generates authentication credentials for AUSF (N13).',
    related: ['ARPF', 'SIDF', 'AUSF', 'UDR', 'AMF', 'N8', 'N10', 'N13'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'udr',
    term: 'UDR',
    fullName: 'Unified Data Repository',
    type: 'Component',
    layer: '5GC',
    definition: 'A 5GC database function storing subscriber data, policy data, and application data. Accessed by UDM (subscription data), PCF (policy data), and NEF (application data). Provides a centralized data store accessed via the Nudr SBI service.',
    related: ['UDM', 'PCF', 'NEF', 'SBA'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'ul-sch',
    term: 'UL SCH',
    fullName: 'Uplink Shared Channel',
    type: 'Channel',
    layer: 'MAC',
    definition: 'The main uplink transport channel carrying UE user data and control messages. Supports HARQ, dynamic link adaptation, and power control. Mapped from CCCH, DCCH, and DTCH logical channels. Carried on PUSCH.',
    related: ['PUSCH', 'DL SCH', 'CCCH', 'DCCH', 'DTCH', 'Transport Channels', 'HARQ'],
    spec: '3GPP TS 38.321',
  },
  {
    id: 'up',
    term: 'UP',
    fullName: 'User Plane',
    type: 'Concept',
    layer: 'Multiple',
    definition: 'The set of protocols, entities, and interfaces responsible for forwarding user data (IP packets) through the network. RAN UP path: UE → PDCP/SDAP → gNB-CU-UP → F1-U → gNB-DU. Core UP: UPF. Distinct from the Control Plane (CP) which carries signaling.',
    related: ['CP (Control Plane)', 'UPF', 'gNB-CU-UP', 'PDCP', 'SDAP', 'N3'],
    spec: '3GPP TS 38.300',
  },
  {
    id: 'urllc',
    term: 'URLLC',
    fullName: 'Ultra-Reliable Low-Latency Communications',
    type: 'Concept',
    layer: 'End-to-End',
    definition: 'One of the three main 5G NR use case categories targeting very high reliability (99.999%) and very low latency (<1 ms end-to-end). Enabled by mini-slot transmission, fast HARQ, configured grant PUSCH, and flexible numerology. Applications: industrial automation, V2X, remote surgery.',
    related: ['eMBB', 'MMTC', 'TTI', 'HARQ', 'SPS', 'MEC'],
    spec: '3GPP TS 22.261',
  },
  // ── V ────────────────────────────────────────────────────────────
  {
    id: 'vonr',
    term: 'VoNR',
    fullName: 'Voice over NR',
    type: 'Concept',
    layer: 'Application',
    definition: 'Delivery of voice calls natively over a 5G NR radio connection via the IMS subsystem. Uses a GBR QoS flow (5QI 1 for voice) with SPS scheduling to carry RTP/UDP/IP speech packets. Requires an IMS PDU session (DNN "ims").',
    related: ['IMS', 'SIP', 'GBR', '5QI', 'SPS', 'ROHC'],
    spec: '3GPP TS 23.501',
  },
  {
    id: 'vrb',
    term: 'VRB',
    fullName: 'Virtual Resource Block',
    type: 'Concept',
    layer: 'PHY',
    definition: 'An intermediate resource block type indexed within a BWP. VRBs can be mapped to PRBs non-interleaved (VRB n → PRB n) or interleaved (bundle-based interleaver for frequency diversity). Signaled in DCI for PDSCH/PUSCH allocation.',
    related: ['PRB', 'RB', 'CRB', 'Resource Allocation', 'DCI', 'BWP'],
    spec: '3GPP TS 38.211',
  },
  // ── X (additional) ───────────────────────────────────────────────
  {
    id: 'xmac',
    term: 'XMAC',
    fullName: 'Expected Message Authentication Code',
    type: 'Identifier',
    layer: 'Security',
    definition: 'The MAC-I value expected by the receiver, computed locally over the received message using the active integrity key and algorithm. The receiver compares the received MAC-I against XMAC; a mismatch indicates integrity failure and triggers an error procedure.',
    related: ['MAC-I', 'NIA', 'PDCP', 'RRC', 'Security'],
    spec: '3GPP TS 33.501',
  },
  {
    id: 'xres',
    term: 'XRES*',
    fullName: 'Expected Extended Authentication Response',
    type: 'Identifier',
    layer: 'NAS',
    definition: 'The expected authentication response computed by the UDM/ARPF during 5G-AKA authentication vector generation. Derived from RAND and K, extended to 128 bits via KDF. Stored by the AUSF and compared against the UE\'s RES* to complete authentication.',
    related: ['RES*', 'HXRES*', 'RAND', 'Authentication', '5G-AKA', 'AUSF'],
    spec: '3GPP TS 33.501',
  },
  // ── Context / State entries ──────────────────────────────────────
  {
    id: 'bearer-context',
    term: 'Bearer Context',
    fullName: 'Bearer Context',
    type: 'Concept',
    layer: 'NG-RAN',
    definition: 'The set of information representing one or more DRBs associated with a PDU session, maintained in the gNB-CU-UP and managed via the E1 interface. Includes PDCP and SDAP configuration, QoS parameters, security keys, and F1-U tunnel identifiers.',
    related: ['E1AP', 'gNB-CU-UP', 'DRB', 'PDU Session', 'UE Context'],
    spec: '3GPP TS 38.463',
  },
  {
    id: 'ue-context',
    term: 'UE Context',
    fullName: 'UE Context',
    type: 'Concept',
    layer: 'NG-RAN',
    definition: 'The complete set of UE-related information stored in a gNB (CU and DU) and the AMF to manage an active RRC connection. In the gNB: RRC configuration, DRB/SRB parameters, security keys, and PDCP state. In the AMF: NAS MM context, 5G-GUTI, and PDU session information.',
    related: ['Initial Context', 'Bearer Context', 'NGAP', 'F1AP', 'RRC'],
    spec: '3GPP TS 38.413',
  },
  {
    id: 'initial-context',
    term: 'Initial Context',
    fullName: 'Initial Context Setup',
    type: 'Procedure',
    layer: 'NG-RAN',
    definition: 'An NGAP procedure in which the AMF sends an Initial Context Setup Request to the gNB-CU-CP to establish the UE AS context, including security keys (KgNB), UE capability, and PDU session resource configuration. The gNB establishes SRBs and DRBs and returns an Initial Context Setup Response.',
    related: ['NGAP', 'UE Context', 'KgNB', 'AMF', 'gNB-CU-CP', 'PDU Session'],
    spec: '3GPP TS 38.413',
  },
  {
    id: 'rm-registered',
    term: 'RM Registered',
    fullName: 'Registration Management Registered State',
    type: 'Concept',
    layer: 'NAS',
    definition: 'The 5G RM state in which the UE has successfully registered with the 5GC and holds a valid 5G-GUTI and Registration Area. The AMF maintains a UE MM context. The UE can initiate PDU sessions and receive MT paging.',
    related: ['RM Deregistered', '5G RM', 'Registration', 'AMF', '5G-GUTI'],
    spec: '3GPP TS 24.501',
  },
  {
    id: 'rm-deregistered',
    term: 'RM Deregistered',
    fullName: 'Registration Management Deregistered State',
    type: 'Concept',
    layer: 'NAS',
    definition: 'The 5G RM state in which the UE is not registered with the 5GC. The AMF does not hold a valid UE MM context. The UE must perform an Initial Registration to obtain a 5G-GUTI and access network services.',
    related: ['RM Registered', '5G RM', 'Registration', 'AMF'],
    spec: '3GPP TS 24.501',
  },
]} />
